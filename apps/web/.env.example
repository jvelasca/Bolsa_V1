# Vacío en dev = proxy Vite /api → :8000 (recomendado para acceso desde la LAN).
# En otro PC usa http://IP-DE-ESTE-PC:5173 (no localhost).
VITE_API_URL=http://localhost:8000

# Acceso (opcional — si vacío, login desactivado en dev)
# APP_PASSWORD=mi-clave-segura
