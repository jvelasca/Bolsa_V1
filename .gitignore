node_modules/
dist/
.turbo/
.env
.env.local
.env.*.local
*.log
logs/**
!logs/**/.gitkeep
# Logs runtime en profundidad (p.ej. apps/api-python/logs, packages/py/application/logs)
**/logs/*.jsonl
**/logs/
!**/logs/.gitkeep
.DS_Store
coverage/
.vite/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.import_linter_cache/
*.egg-info/
.venv/
venv/
.venv_backup_*/

# Prisma
packages/database/prisma/migrations/**/migration_lock.toml

# F2b filings locales (PDF/TXT + index; no versionar)
data/filings/

# IDE / agent local
.idea/
.cursor/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/settings.json

.cache/

# Secrets / local overrides
*.pem
.secrets/
.env.*
!.env.example
!apps/web/.env.example

# Cachés de herramientas (generadas en local, no versionar)
.eslintcache
.prettiercache
.nyc_output/
eslint-output/

# Logs de gestores de paquete (más allá de *.log)
pnpm-debug.log*
npm-debug.log*
yarn-error.log*
yarn-debug.log*

# Fusiones / conflictos
*.orig
*.rej

# Base de datos local / volcados (Backup/restore manual)
*.dump
*.sql.gz
*.sql.bak
db-backups/
pgdata/
.postgres/
.postgres-data/

# Artefactos de test / reportería
test-results/
playwright-report/
blob-report/
allure-results/

# Sistema operativo (Windows junto a .DS_Store)
Thumbs.db
desktop.ini

# Artefacto generado por la evidencia del gobernador (huérfano, nunca versionado;
# ver deuda P3 post-auditoría v2.65). No es un fixture: ningún código lo referencia.
/governor.json

# Corridas de evidencia de AUTO-22 (`auto_evidence_run.py --out-root`). Artefactos GENERADOS con
# la huella de su material: se reproducen con el comando, no se versionan.
/evidence_runs/
