# Inventario pending-delete — R-12 A5 (2026-08-21)

> **Padre:** [`README.md`](./README.md) · plan [`plan-r12-auditoria-ux-2026-08-21.md`](../plan-r12-auditoria-ux-2026-08-21.md).
> **Regla:** **PROHIBIDO PURGE.** Este doc es read-only: writers → readers → runtime → storage. El borrado exige fase V2 propia + E8 completo.

AsOf: 2026-08-21 · HEAD de referencia al redactar: `f7a86cc`.

---

## Matriz

| Ítem                           | Storage / nombre                                           | Writers actuales                                                                                                                               | Readers runtime                                                                                                                                                                    | Dependencia migración              | Purge                                                             |
| ------------------------------ | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ----------------------------------------------------------------- |
| `readLegacyPendingOrders`      | `localStorage["bolsa-trading-ui"]` → `state.pendingOrders` | **Ningún writer vivo** del array legacy. El store `bolsa-trading-ui` ya no persiste pending orders (API es fuente).                            | [`use-pending-orders.ts`](../../../apps/web/src/features/trading/use-pending-orders.ts) L16–27 (lectura) + L42–68 (**migrador one-shot**: POST API + `removeItem`)                 | Usuarios con blob antiguo          | NO. Quitar el migrador pierde órdenes locales no subidas          |
| `chartDataStrip`               | campo opcional `WorkspaceDocument`                         | **0 asignaciones** en TS actual. Persist del workspace puede **reescribir** el campo si ya venía en el blob                                    | [`workspace-store-core.ts`](../../../apps/web/src/stores/workspace-store-core.ts) L748 merge a `chartToolbarGlobal`                                                                | Workspaces persistidos pre-toolbar | NO hasta confirmar 0 blobs                                        |
| `chartNewTabSeed`              | campo opcional workspace                                   | **0 writers**. Merge en [`chart-list-snapshot.ts`](../../../apps/web/src/lib/chart-list-snapshot.ts) L484 (conserva el valor al fusionar docs) | Tipo en [`chart-defaults.ts`](../../../packages/shared/src/chart-defaults.ts) L247                                                                                                 | Workspaces                         | NO                                                                |
| `newChartConfigSource`         | campo opcional `preferences`                               | **0 writers**                                                                                                                                  | Tipo [`chart-defaults.ts`](../../../packages/shared/src/chart-defaults.ts) L244                                                                                                    | Workspaces                         | NO (tipo + campo)                                                 |
| `readLegacyTimeframeFavorites` | `localStorage["bolsa-chart-timeframe-favorites"]`          | **0 writers** de esa clave                                                                                                                     | [`workspace-store-core.ts`](../../../apps/web/src/stores/workspace-store-core.ts) L648–663, usado en `normalizeWorkspace` L746                                                     | Favoritos pre-toolbar              | NO. Lectura one-shot al normalizar                                |
| `presetRuleGroups`             | n/a (API shared)                                           | Función **viva** en [`strategy-presets.ts`](../../../packages/shared/src/strategy-presets.ts) L102                                             | Callers: `hybrid-strategy.ts`, `research-platform.ts`, `strategy-gate-series.ts`; re-export [`strategy-rules.ts`](../../../packages/shared/src/strategy-rules.ts) L68 + `types.ts` | Ninguna de storage                 | **NO es código muerto.** Re-export no se “purga”; es API canónica |

---

## Conclusión

- Hay **readers/migradores runtime** reales en pending-orders y timeframe favorites.
- **No hay writers** de las claves/campos legacy en el árbol actual (salvo persist genérico de workspace que reexpide campos presentes).
- `presetRuleGroups` estaba mal catalogado como riesgo de borrado: **tiene consumidores de producción**.
- Siguiente (V2, decisión): (1) métrica de blobs en clientes, (2) flag de “migración completa”, (3) test de ausencia, (4) entonces E8.

Criterio E8 **no se cumple** hoy para ninguno de los ítems de riesgo alto excepto el malentendido de `presetRuleGroups` (que no debe borrarse).
