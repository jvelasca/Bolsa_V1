# Engineering Index — índice maestro de docs

> **AsOf:** 2026-08-03  
> **Propósito:** un **padre único** para la documentación de ingeniería (respuesta a auditoría externa A0 H1).  
> **Regla:** todo doc nuevo de ingeniería declara exactamente **un padre** (este índice o un hijo directo). No añadir más raíces en paralelo.  
> **Repo:** https://github.com/jvelasca/Bolsa_V1

---

## 0. Cómo leer (cuatro públicos)

| Público                    | Empieza aquí                                                                                        | No necesita          |
| -------------------------- | --------------------------------------------------------------------------------------------------- | -------------------- |
| **Usuario**                | [HELP.md](../HELP.md) · Ayuda (?) en app                                                            | ADRs / RFCs          |
| **Desarrollador**          | [ONBOARDING.md](../ONBOARDING.md) · [DEV_STARTUP.md](../DEV_STARTUP.md) · este índice §1–2          | RFCs completos       |
| **Arquitecto**             | [ARCHITECTURE.md](../ARCHITECTURE.md) · [adr/](../adr/) · [rfc/](../rfc/) · §3                      | Notebooks de campaña |
| **Investigador / auditor** | [audit-pack-post-audits-2026-08-03.md](./audit-pack-post-audits-2026-08-03.md) · freeze · lifecycle | Detalle UI de charts |

Índice general de todos los docs: [README.md](../README.md) (catálogo). **Este Engineering Index es el mapa de navegación**, no duplica el catálogo.

---

## 1. Árbol canónico (un padre)

```text
Engineering Index  (este doc)
├── Architecture
│   ├── ARCHITECTURE.md
│   ├── CURRENT_SYSTEM.md ← **SoT corto 2026-09-04** V2.0 PAPER AUTO tip `v2.0-beta` → `e05fc6b0` · [relevo tag V2.0](./traspaso-relevo-tag-v2-0-beta-2026-09-04.md) · V1.99 `v1.99-beta` → `bf57899b` · [spec-v199](./spec-v199-position-management-certification-2026-09-04.md)
│   ├── PROJECT_PREMISES.md
│   ├── PROJECT_PREMISES.md
│   ├── PROJECT_PREMISES.md
│   ├── adr/*  (decisiones)  — incl. ADR-001 (Prisma) · ADR-003 (Python backend) · ADR-025 (fuente verdad modelo, M4) · **ADR-026 (custodia Opción B, obligación pendiente, R-10 F4a)** · **ADR-027 (auth multi-user JWT híbrido Opción C, R12-AUTH, Aceptado)** · **ADR-031 (tesis ≠ plan ≠ permiso / TradePlan v0)** · **ADR-032 (Operational Core v1.9 · F1–F4 modelo CERRADO)** · **ADR-033 (Operational Authority v1.10 · Position persistida)** · **ADR-034 (Operational Integrity v1.11 · OI-1…OE-1 CERRADOS)** · **ADR-035 (Operational Reliability v1.12 · OR-1/3/4/5/6 CERRADOS · OR-2 PARTIAL · V1.13 DEX)**
│   ├── rfc/*  (constitución)
│   └── bounded-contexts-2026-08-03.md
├── Research
│   ├── research-lifecycle.md
│   ├── backtesting-dia-d-premises-*.md
│   ├── improvement-roadmap-*.md
│   └── belief-coach-brief-draft-*.md  (futuro)
├── Product / Ops
│   ├── HELP.md  (sync Ayuda)
│   ├── stage-audit-*.md
│   ├── post-audit-decision-freeze-*.md
│   ├── dual-universes / mandato / reconciliación ADRs
│   ├── demo-operating-modes-brief-*.md   (MANUAL/SEMI/AUTO)
│   ├── semi-demo-book-impl-slice1-*.md   (GO SEMI)
│   ├── trading-operativa-panel-2026-08-04.md  (Operativa · IO · modos en barra)
│   ├── estudio-supervision-model-2026-08-06.md  (ADR-024 · Supervisión ON · 3 cadencias)
│   ├── estudio-process-status-ui-2026-08-06.md  (iconos · Actualizar/Redescubrir · OPERATIVA)
│   ├── session-handoff-2026-08-06-estudio-process-ui.md
│   ├── visualizados-list-ux-2026-08-06.md
│   ├── session-handoff-2026-08-06-visualizados-list-ux.md
│   ├── dev-continuation-plan-2026-08-09.md   ← continuación (estado + próximos pasos)
│   ├── audit-resume-premises-2026-08-09.md   ← premisas nuevo hilo auditoría
│   ├── general-audit-plan-2026-08-10.md       ← auditoría general + plan por módulos
│   ├── plan-r8-prevencion-riesgo-2026-08-20.md ← R-8 plan de prevención de riesgo + corrección (2026-08-20) — **R-8 COMPLETA en `main`** (R-8A `edf2d0c` · R-8B.1 `ac147fe` · R-8B.2 `abf3dc2` · R-8C `3ad48aa` · R-8D `dbd1ee5` · R-8B.3 `158b3db`,`756e3a1`,`ce1d5ca`,`e6dde15`,`fb95b27`) + **CONTRACT-STALE RESUELTO** (`aecbb28`, D5 verde)
│   ├── plan-r9-refactor-hardening-2026-08-20.md ← R-9 hardening financiero (2026-08-20) — **R-9 COMPLETA en `main`** (F1 `fa070ec` · F2 `31954dd` · F3 `26f5ca1` · F4 `b384f31` · F5 `ef4c136` · F6 `e5d8926` · F7 `5d59671` · F8 `5ea336f`) · cierre R-9 `5c557fa` · traspaso `traspaso-relevo-cierre-r9-f1-f8-apertura-f9-2026-08-20.md` · **F9/V2 diferida a decisión futura**
│   ├── plan-r10-v1-2-1-correcciones-auditoria-2026-08-20.md ← R-10 / v1.2.1 (2026-08-20) correcciones P1/P2 de la auditoría externa post-v1.2.0 — **COMPLETA, CERRADA en `main`** (F1 `a1501e6` · F2a `b4dcc72` · F2b `86c315a` · F3 `b79e5dd` · F4a `49e2731` + `5c304e6` · F4b `e12a125`, 2026-08-21 · F5 docs+CHANGELOG `[1.2.1]` 2026-08-21) · 6 correcciones · decisiones D1–D7 + F1-C + F3-sim + ADR-026 · orden F1→F2a→F2b→F3→F4a→F4b→F5 ✅ · ADR `../adr/026-custodia-obligacion-pendiente.md` · cierre/traspaso activo `traspaso-relevo-cierre-r10-v1-2-1-apertura-siguiente-2026-08-21.md` (previo `traspaso-relevo-cierre-r10-f3-f4a-apertura-f4b-2026-08-21.md`) · **R-10/v1.2.1 CERRADA (F5 `2093296` + tag `v1.2.1`)**
│   ├── plan-r11-hardening-auditoria-v1-2-1-2026-08-21.md ← R-11 hardening post-v1.2.1 (2026-08-21) — **COMPLETA, CERRADA en `main`** (C1 `c3327c1` custodia multi-periodo · C2 `17a1107` idempotency_key end-to-end · C3 `cda26e9` Decimal trade · C4 `157bb45` contract:check EXIT 0 · C5 `6762614` mypy==0 gate CI · C6 `custody_charge_source = DEFAULT_PORTFOLIO` en [`ADR 026`](../adr/026-custodia-obligacion-pendiente.md) docs-only · D1 `870fb21` limpieza E8 métodos custodia muertos · D2 cierre documental con `db95709`) · batería global: mypy gate `344` EXIT 0 · mypy application `95` EXIT 0 · ruff 0 · pytest app+market `388` · pytest api-python `84` · **R-11/hardening CERRADA (C1–C6 + D1 + D2)** · traspaso **HISTÓRICO** `traspaso-relevo-cierre-r11-c1-c6-d1-d2-siguiente-2026-08-21.md` · **release `v1.3.0` tag `b778292`**
│   ├── plan-unificacion-research-radar-2026-08-21.md ← **Unificación Research→Radar — DRAFT / APARCADO** · F1–F3 ✅ doc CERRADAS · código F4′–F6′ condicional Track B · `PROJECT_STATE.md` §2b
│   ├── plan-split-backtests-page-2026-08-22.md ← **Split god-page Track B — CERRADO** · B0 plan · **B1–B12 ✅** (`3f9bd7e` shell 321 LOC) · origen informe read-only post-R-13
│   ├── plan-r13-consolidacion-beta-2026-08-22.md ← **R-13 (2026-08-22) — CERRADA** consolidación BETA. A0–A3 HECHAS · tag `v1.6.0-beta` = `c3964fc`. Track B BLOQUEADO. Traspaso cierre `traspaso-relevo-cierre-r13-consolidacion-beta-siguiente-2026-08-22.md`
│   ├── audit-pack-estado-global-2026-08-25-v180.md ← **PACK auditoría vivo** post-tag v1.8.0-beta · spine 159 · BETA-D + A3-wire · supersedes 24e
│   ├── roadmap-v181-operational-consolidation-2026-08-25.md ← **FASE CERRADA** v1.8.1 (C1–C6) · tag `v1.8.1-beta` · no thin nuevos
│   ├── audit-pack-estado-global-2026-08-25-v181.md ← pack consolidación v1.8.1 · tag `v1.8.1-beta` · **audit ext RATIFICADA**
│   ├── audit-ext-v181-triage-2026-08-25.md ← **TRIAGE** auditoría externa post-tag v1.8.1 · consolidación CERRADA · no otra limpieza
│   ├── adr-032-audit-gap-2026-08-25.md ← gap ADR-032 vs FASE 1–4 auditor · docs-only
│   ├── roadmap-v19-operational-core-2026-08-25.md ← **FASE CERRADA** v1.9 · F1–F4 + ExitPermission + INFRA · next = v1.10
│   ├── traspaso-relevo-audit-ext-v181-cierre-apertura-v19-2026-08-25.md ← **HISTÓRICO** · v1.8.1 cerrada · apertura diseño v1.9
│   ├── audit-ext-v19-ops-discontinuity-triage-2026-08-25.md ← **TRIAGE** auditoría discontinuidad decisión→posición · RATIFICADO · no consola
│   ├── adr-032-ops-authority-gap-2026-08-25.md ← gap ADR-032 modelo vs autoridad v1.10 · docs-only
│   ├── roadmap-v110-operational-authority-2026-08-25.md ← **FASE CERRADA** v1.10 · H1→P4 · tag `v1.10-beta` → `047ddb6`
│   ├── traspaso-relevo-tag-v1-10-beta-2026-08-25.md ← **HISTÓRICO** tag v1.10-beta
│   ├── roadmap-v111-operational-integrity-2026-08-26.md ← **FASE CERRADA** v1.11 · OI-1…OE-1 · tag `v1.11-beta`
│   ├── audit-pack-estado-global-2026-08-26-v111.md ← pack Operational Integrity · tag `v1.11-beta` → `76d0f951`
│   ├── traspaso-relevo-tag-v1-11-beta-2026-08-26.md ← **HISTÓRICO** tag v1.11-beta → auditoría
│   ├── audit-ext-v111-operational-reliability-triage-2026-08-26.md ← **TRIAGE** auditoría 1 post-v1.11 · RATIFICADO · v1.12 · OR-2 PARTIAL post-audit
│   ├── roadmap-v112-operational-reliability-2026-08-26.md ← **FASE CERRADA** v1.12 · D0+OR-1…OR-6 · OR-2 PARTIAL · tag `v1.12-beta`
│   ├── audit-pack-estado-global-2026-08-26-v112.md ← pack Operational Reliability · tag `v1.12-beta` → `369b5d1`
│   ├── traspaso-relevo-tag-v1-12-beta-2026-08-26.md ← **HISTÓRICO** tag v1.12-beta → auditoría recibida
│   ├── audit-ext-v112-durable-execution-triage-2026-08-26.md ← **TRIAGE** auditoría post-v1.12 · RATIFICADO · V1.13
│   ├── roadmap-v113-durable-execution-2026-08-26.md ← **FASE CERRADA** v1.13 · D0+DEX-1…DEX-5 · pack v113 · tag `v1.13-beta` pendiente owner
│   ├── audit-pack-estado-global-2026-08-26-v113.md ← pack Durable Execution · spine 483 · tag `v1.13-beta` pendiente
│   ├── traspaso-relevo-tag-v1-13-beta-2026-08-26.md ← **RELEVO** tag v1.13-beta → auditoría (owner publica)
│   ├── plan-dex1-pg-submit-intents-2026-08-26.md ← **DEX-1 CERRADO** · PG submit_intents
│   ├── traspaso-relevo-dex1-pg-submit-intents-2026-08-26.md ← **HISTÓRICO** DEX-1 → DEX-2
│   ├── plan-dex2-crash-restart-cross-pid-2026-08-26.md ← **DEX-2 CERRADO** · cert cross-PID
│   ├── traspaso-relevo-dex2-crash-restart-cross-pid-2026-08-26.md ← **HISTÓRICO** DEX-2 → DEX-3
│   ├── plan-dex3-operational-incident-2026-08-26.md ← **DEX-3 CERRADO** · OperationalIncident resolve/clear
│   ├── traspaso-relevo-dex3-operational-incident-2026-08-26.md ← **HISTÓRICO** DEX-3 → DEX-4
│   ├── plan-dex4-confirm-orchestrator-2026-08-26.md ← **DEX-4 CERRADO** · Confirm = orquestador
│   ├── traspaso-relevo-dex4-confirm-orchestrator-2026-08-26.md ← **HISTÓRICO** DEX-4 → DEX-5
│   ├── plan-dex5-operational-invariants-2026-08-26.md ← **DEX-5 CERRADO** · invariantes / property suite
│   ├── traspaso-relevo-dex5-operational-invariants-2026-08-26.md ← **HISTÓRICO** DEX-5 → pack v113
│   ├── traspaso-relevo-audit-ext-v112-apertura-v113-2026-08-26.md ← **HISTÓRICO** D0 v1.13 → chat DEX-1
│   ├── plan-or1-e2e-idempotency-2026-08-26.md ← **OR-1 CERRADO** · retry Confirm paper · spine 372
│   ├── traspaso-relevo-or1-e2e-idempotency-2026-08-26.md ← **HISTÓRICO** OR-1 → chat OR-2
│   ├── plan-or2-crash-restart-2026-08-26.md ← **OR-2 PARTIAL** · lógico CERRADO · físico → DEX-1 · spine 382
│   ├── traspaso-relevo-or2-crash-restart-2026-08-26.md ← **HISTÓRICO** OR-2 → chat OR-3 · nota PARTIAL
│   ├── plan-or3-order-state-machine-2026-08-26.md ← **OR-3 CERRADO** · PaperOrder state machine · spine 387
│   ├── traspaso-relevo-or3-order-state-machine-2026-08-26.md ← **HISTÓRICO** OR-3 → chat OR-4
│   ├── plan-or4-recon-opening-veto-2026-08-26.md ← **OR-4 CERRADO** · recon → opening veto · spine 403
│   ├── traspaso-relevo-or4-recon-opening-veto-2026-08-26.md ← **HISTÓRICO** OR-4 → chat OR-5
│   ├── plan-or5-broker-execution-scenario-suite-2026-08-26.md ← **OR-5 CERRADO** · suite A–L · spine 418
│   ├── traspaso-relevo-or5-broker-execution-scenario-suite-2026-08-26.md ← **HISTÓRICO** OR-5 → chat OR-6
│   ├── plan-or6-semi-operational-certification-2026-08-26.md ← **OR-6 CERRADO** · readiness 4 estados + CTA venue
│   ├── traspaso-relevo-or6-semi-operational-certification-2026-08-26.md ← **HISTÓRICO** OR-6 → pack/tag v1.12-beta
│   ├── traspaso-relevo-audit-ext-v111-cierre-apertura-v112-2026-08-26.md ← **HISTÓRICO** D0 v1.12 → chat OR-1
│   ├── plan-h1-honesty-pending-2026-08-25.md ← **H1 CERRADO** pending ≠ stop
│   ├── plan-h2-invariantes-factories-2026-08-25.md ← **H2 CERRADO** invariantes factories
│   ├── plan-p1-position-durable-2026-08-25.md ← **P1 CERRADO** Position durable + wire fill
│   ├── plan-p2-riesgo-al-firmar-2026-08-25.md ← **P2 CERRADO** firma de riesgo
│   ├── plan-p3-cadena-salida-2026-08-25.md ← **P3 CERRADO** cadena ExitPlan → ExitPermission → SEMI
│   ├── traspaso-relevo-p3-cadena-salida-2026-08-25.md ← **HISTÓRICO** · P3 listo · apertura P4
│   ├── traspaso-relevo-p2-riesgo-al-firmar-2026-08-25.md ← **HISTÓRICO** · P2 listo · apertura P3
│   ├── traspaso-relevo-p1-position-durable-2026-08-25.md ← **HISTÓRICO** · P1 listo · apertura P2
│   ├── traspaso-relevo-h2-invariantes-factories-2026-08-25.md ← **HISTÓRICO** · H2 listo · apertura P1
│   ├── traspaso-relevo-h1-honesty-pending-2026-08-25.md ← **HISTÓRICO** · H1 listo · apertura H2
│   ├── traspaso-relevo-audit-ext-v19-ops-discontinuity-2026-08-25.md ← **HISTÓRICO** · D0 triage · apertura H1
│   ├── traspaso-relevo-tag-v1-8-1-beta-2026-08-25.md ← **HISTÓRICO** tag v1.8.1-beta (audit recibida)
│   ├── plan-ciclo-c1-hoy-honesty-help-2026-08-25.md ← **C1** Hoy nunca BUY sin TradePlan · HELP 2026-08-25
│   ├── traspaso-relevo-ciclo-c1-hoy-honesty-help-2026-08-25.md ← relevo C1
│   ├── traspaso-relevo-adr-032-operational-core-docs-2026-08-25.md ← **ADR-032** Proposed/Accepted docs-only v1.9 Operational Core · sin código
│   ├── plan-ciclo-c2-alembic-authority-2026-08-25.md ← **C2 CÓDIGO LISTO** Alembic-only · Prisma público fail-closed
│   ├── traspaso-relevo-ciclo-c2-alembic-authority-2026-08-25.md ← relevo C2 · E1 = C3
│   ├── traspaso-relevo-ciclo-c2-alembic-authority-2026-08-25.md ← relevo C2 · E1=C3
│   ├── plan-ciclo-c3-action-queue-2026-08-25.md ← **C3 CÓDIGO LISTO** ActionQueue + Hoy slice
│   ├── traspaso-relevo-ciclo-c3-action-queue-2026-08-25.md ← relevo C3 · E1 = C5
│   ├── plan-ciclo-c5-metrics-honesty-2026-08-25.md ← **C5 CÓDIGO LISTO** MFE source · expectancy sampleQuality
│   ├── traspaso-relevo-ciclo-c5-metrics-honesty-2026-08-25.md ← relevo C5 · E1 = C4
│   ├── plan-ciclo-c4-shape-canonical-2026-08-25.md ← **C4 CÓDIGO LISTO** TradePlan reader canónico · planSource · sin contract:gen
│   ├── traspaso-relevo-ciclo-c4-shape-canonical-2026-08-25.md ← relevo C4 · E1 = C6 coordinador
│   ├── plan-ciclo-c6-stamp-v181-2026-08-25.md ← **C6 CERRADO** stamp pack · tag `v1.8.1-beta`
│   ├── traspaso-relevo-ciclo-c6-stamp-v181-2026-08-25.md ← relevo C6 · E1 = auditoría (hecha) / v1.9 ADR-032
│   ├── traspaso-relevo-tag-v1-8-0-beta-2026-08-25.md ← **RELEVO** tag v1.8.0-beta → auditoría externa
│   ├── audit-pack-estado-global-2026-08-24e.md ← histórico post-tag v1.7.0-beta + CI
│   ├── deuda-thaw-estricto-runbook-2026-08-25.md ← **DEUDA ESTRICTO** tracking P1–P5 (60d/50/70/55 · MaxDD) · W2–W4 · snapshot `scripts/thaw_estricto_snapshot.mjs` · **no** Accept estricto aún
│   ├── plan-ciclo-a3-wire-auto-arm-ui-2026-08-25.md ← **CERRADO** Ciclo A3-wire · armado `ACTIVAR AUTO` obligatorio (honesty BETA-D)
│   ├── traspaso-relevo-ciclo-a3-wire-auto-arm-ui-2026-08-25.md ← histórico · A3-wire CERRADO · arm UI ≠ execute
│   ├── traspaso-relevo-cierre-post-e1-2026-08-25.md ← cierre chat post E1 (push · deuda · proxies) · histórico
│   ├── traspaso-relevo-thaw-beta-d-2026-08-25.md ← thaw BETA-D CERRADO · ADR-023 Accepted parcial · estricto deuda → runbook│   ├── traspaso-relevo-ciclo-8-2-bracket-thin-2026-08-25.md ← **RELEVO ACTIVO** · Ciclo 8.2 Bracket thin · CERRADO `73044a7` · growth thin 8.0–8.2 · park plena
│   ├── plan-ciclo-8-2-bracket-thin-2026-08-25.md ← plan 8.2 · D1–D8 OK · CERRADO en `73044a7`
│   ├── traspaso-relevo-ciclo-8-1-trail-thin-2026-08-25.md ← histórico · Ciclo 8.1 Trail thin · CERRADO `655832c`
│   ├── plan-ciclo-8-1-trail-thin-2026-08-25.md ← plan 8.1 · D1–D8 OK · CERRADO en `655832c`
│   ├── traspaso-relevo-ciclo-8-0-expectancy-thin-2026-08-25.md ← histórico · Ciclo 8.0 Expectancy thin · CERRADO `cf880eb`
│   ├── plan-ciclo-8-0-expectancy-thin-2026-08-25.md ← plan 8.0 · D1–D8 OK · CERRADO en `cf880eb`
│   ├── roadmap-ciclo-8-crecimiento-expectancy-trail-bracket-2026-08-25.md ← roadmap fase 8.x · **8.0–8.2 thin CERRADOS** · plena parked
│   ├── traspaso-relevo-ciclo-rx1-exits-full-auto-honesty-2026-08-25.md ← histórico · RX1 exits honesty · CERRADO `9289b53` · **no thaw** · **no** I4
│   ├── plan-ciclo-rx1-exits-full-auto-honesty-2026-08-25.md ← plan RX1 · D1–D8 OK · CERRADO en `9289b53`
│   ├── traspaso-relevo-cierre-integridad-i1-i3-2026-08-25.md ← histórico · cierre línea I1–I3 · residual exits → RX1
│   ├── traspaso-relevo-ciclo-i3-shadow-honesty-2026-08-25.md ← histórico · I3 Shadow honesty · CERRADO `26901aa` · **no thaw**
│   ├── plan-ciclo-i3-shadow-honesty-2026-08-25.md ← plan I3 · D1–D8 OK · CERRADO en `26901aa`
│   ├── traspaso-relevo-ciclo-i2-actionability-io-2026-08-25.md ← histórico · I2 Actionability/IO · CERRADO `e31840d` (stamp `cfc8042`)
│   ├── plan-ciclo-i2-actionability-io-2026-08-25.md ← plan I2 · D1–D8 OK · CERRADO en `e31840d`
│   ├── traspaso-relevo-ciclo-i1-executetrade-converge-2026-08-25.md ← histórico · I1 ExecuteTrade · CERRADO `2bd5cd8`
│   ├── plan-ciclo-i1-executetrade-converge-2026-08-25.md ← plan I1 · D1–D8 OK · CERRADO en `2bd5cd8`
│   ├── traspaso-relevo-ciclo-5-3-mfe-mae-thin-2026-08-25.md ← Ciclo 5.3 MFE/MAE · CERRADO `fd44a03` (tip `05e354c`) · línea 5.x cerrada
│   ├── plan-ciclo-5-3-mfe-mae-thin-2026-08-25.md ← plan 5.3 · D1–D8 OK · CERRADO en `fd44a03`
│   ├── traspaso-relevo-ciclo-5-2-exit-radar-thin-2026-08-25.md ← Ciclo 5.2 Exit Radar · CERRADO `e813aa3` (tip `13a69f9`)
│   ├── plan-ciclo-5-2-exit-radar-thin-2026-08-25.md ← plan 5.2 · D1–D8 OK · CERRADO en `e813aa3`
│   ├── traspaso-relevo-ciclo-5-1-protect-t1-thin-2026-08-25.md ← histórico · Ciclo 5.1 Protect/T1 · feat `12d05d2`
│   ├── plan-ciclo-5-1-protect-t1-thin-2026-08-25.md ← plan 5.1 · D1–D8 OK · CERRADO en `12d05d2`
│   ├── traspaso-relevo-ciclo-5-thesis-health-thin-2026-08-25.md ← histórico · Ciclo 5.0 Thesis Health · feat `a2f32bb`
│   ├── plan-ciclo-5-thesis-health-thin-2026-08-25.md ← plan 5.0 · D1–D8 OK · CERRADO en `a2f32bb`
│   ├── traspaso-relevo-ciclo-7-spine-honesty-2026-08-25.md ← histórico · Ciclo 7 spine honesty · feat `eef94ec`
│   ├── plan-ciclo-7-spine-honesty-2026-08-25.md ← plan 7 · D1–D8 OK · CERRADO en `eef94ec`
│   ├── traspaso-relevo-ciclo-6-attribution-journal-thin-2026-08-25.md ← histórico · Ciclo 6 Attribution thin · feat `7de91e5`
│   ├── plan-ciclo-6-attribution-journal-thin-2026-08-25.md ← plan Ciclo 6 · D1–D8 OK · CERRADO en `7de91e5`
│   ├── traspaso-relevo-ciclo-49-board-session-tradeplan-echo-2026-08-25.md ← histórico · Ciclo 4.9 Board echo · feat `e569003`
│   ├── plan-ciclo-49-board-session-tradeplan-echo-2026-08-25.md ← plan 4.9 mesa · D1–D8 OK · CERRADO en `e569003`
│   ├── traspaso-relevo-ciclo-48-wyckoff-surface-effort-cierre-2026-08-25.md ← histórico · Ciclo 4.8 surface+effort · feat `b381d06`
│   ├── plan-ciclo-48-wyckoff-surface-effort-cierre-2026-08-25.md ← plan 4.8 · D1–D8 OK · CERRADO en `b381d06`
│   ├── traspaso-relevo-ciclo-47-wyckoff-thesis-binding-2026-08-25.md ← histórico · Ciclo 4.7 thesis binding · feat `604fd90`
│   ├── traspaso-relevo-ciclo-46-wyckoff-sm-lookback-2026-08-25.md ← histórico · Ciclo 4.6 SM lookback · feat `fb6e801`
│   ├── traspaso-relevo-ciclo-45-wyckoff-lps-sm-2026-08-25.md ← histórico · Ciclo 4.5 LPS/SM thin · feat `baaa9b4`
│   ├── traspaso-relevo-ciclo-44-wyckoff-formal-2026-08-25.md ← histórico · Ciclo 4.4 Wyckoff formal · feat `7003ddf`
│   ├── traspaso-relevo-ciclo-43-armed-2026-08-25.md ← histórico · Ciclo 4.3 ARMED · `4eb99a2` en origin
│   ├── traspaso-relevo-ciclo-42-entrysetup-2026-08-25.md ← histórico · Ciclo 4.2 EntrySetup · `a7eeaee` en origin
│   ├── traspaso-relevo-ciclo-41-no-new-longs-2026-08-25.md ← histórico · Ciclo 4.1 NO_NEW_LONGS · `97f4862` en origin
│   ├── traspaso-relevo-ciclo-40-stop-entry-size-2026-08-25.md ← histórico · Ciclo 4.0 stop/entry_ready/size · `1cbd021` en origin
│   ├── plan-ciclo-47-wyckoff-thesis-binding-2026-08-25.md ← plan 4.7 thesis binding · CERRADO en `604fd90`
│   ├── plan-ciclo-46-wyckoff-sm-multi-sesion-2026-08-25.md ← plan 4.6 SM lookback · CERRADO en `fb6e801`
│   ├── plan-ciclo-45-wyckoff-lps-sm-2026-08-25.md ← plan 4.5 LPS/SM Wyckoff · CERRADO en `baaa9b4`
│   ├── plan-ciclo-44-wyckoff-formal-2026-08-25.md ← plan 4.4 CERRADO en `7003ddf`
│   ├── plan-ciclo-43-armed-entrysetup-2026-08-25.md ← plan 4.3 CERRADO en `4eb99a2`
│   ├── plan-ciclo-42-entrysetup-2026-08-25.md ← plan 4.2 CERRADO en `a7eeaee`
│   ├── plan-ciclo-41-no-new-longs-entrysetup-2026-08-25.md ← plan 4.1 CERRADO en origin
│   ├── traspaso-relevo-adr-031-tradeplan-hoy-cierre-apertura-siguiente-2026-08-24.md ← histórico · ADR-031 Ciclos 0–3 · Hoy strip
│   ├── traspaso-relevo-ruff-i001-ci-stamp-24e-2026-08-24.md ← relevo histórico · F9-A3 CI · Ruff I001 · pack 24e · cerrado por ADR-031
│   ├── traspaso-relevo-ops-alembic-010-f9a3-monitor-purge-2026-08-24.md ← relevo histórico · Alembic `010` · F9-A3 CI · Purge V2 MONITOR · cerrado por 24e
│   ├── traspaso-relevo-order-proposal-journal-cierre-f1-f3-2026-08-24.md ← relevo histórico · Ciclo 1 F1–F3 + F9-A CERRADO · HEAD `8a1e64d`
│   ├── audit-pack-estado-global-2026-08-24d.md ← histórico pre-Ruff CI unblock · supersedido por 24e
│   ├── audit-pack-estado-global-2026-08-24b.md ← histórico post-spine + mesa U0–U5 · HEAD `04e441e`
│   ├── audit-pack-estado-global-2026-08-24.md ← histórico R-1→R-13 + Track B (pre-spine/mesa) · HEAD `47a3b58`
│   ├── audit-pack-estado-global-2026-08-22.md ← histórico (pre Track B)
│   ├── traspaso-relevo-fase0-f01-asis-apertura-f02-2026-08-24.md ← relevo histórico Fase 0 · F0.1 AS-IS cerrado · F0.2 TO-BE
│   ├── fase0-decision-spine-roadmap-2026-08-24.md ← **esquema maestro Fase 0** (roadmap F0.1→F0.2→F0.3→Fit→Daily vista) · verificación del orquestador · docs-only
│   ├── fase0-decision-spine-tobe-2026-08-24.md ← **F0.2 TO-BE Decision Spine** (arquitectura objetivo; tres colas → DecisionPackage → fill) · docs-only · verificado VERDE vs AS-IS
│   ├── fase0-decision-spine-mapping-2026-08-24.md ← **F0.3 Mapping** (carta conservar/adaptar/crear/no-op por ítem del AS-IS) · docs-only · Fit único create · verificado VERDE vs AS-IS
│   ├── fase0-decision-spine-descarga-2026-08-24.md ← **F0.4 Descargue de decisión** (gates+autorización; D1 risk cesta SEMI y AUTO, sin override) · docs-only · verificado VERDE · **D2 CERRADA con código** (`f7b1f6c`) · **D3 CONFIRMADA (a: Lab/Radar fuera del spine)**
│   ├── traspaso-relevo-fase0-cierre-docs-apertura-codigo-2026-08-24.md ← relevo histórico · F0.5 (Fit) abierta · F0.5a métrica = decisión propietario · F0.6 pendiente
│   ├── traspaso-relevo-ux-mesa-u5-cierre-apertura-ciclo-2026-08-24.md ← relevo histórico · UX mesa U0–U5 `04e441e` CERRADA
│   ├── traspaso-relevo-ux-mesa-u0-u4-cierre-apertura-u5-2026-08-24.md ← relevo histórico · U0–U4 cerró apertura U5 · **cerrado por** `traspaso-relevo-ux-mesa-u5-cierre-apertura-ciclo-2026-08-24.md` (U5 en `04e441e`)
│   ├── traspaso-relevo-probar-spine-cierre-apertura-h5-2026-08-24.md ← relevo histórico · Prove+H5 CERRADOS · apertura UX mesa · **cerrado por** arco U0–U5
│   ├── traspaso-relevo-f0-6-cierre-apertura-siguiente-2026-08-24.md ← relevo histórico · **F0.6 Decision Board COMPLETA** (backend `8df8a65` + UI `672e88f`) · F0.6 CERRADA · siguiente fase por abrir
│   ├── traspaso-relevo-f0-6b-cierre-apertura-ui-2026-08-24.md ← relevo histórico · **F0.6b Decision Board v1 (backend) CERRADA** (`8df8a65`) · abrió UI web (posteriormente CERRADA `672e88f`)
│   ├── traspaso-relevo-f0-5b-cierre-apertura-f0-6-2026-08-24.md ← relevo histórico · **F0.5b PortfolioFit v1 CERRADA** (`3670a09`) · F0.6 (Daily vista) abierta
│   ├── fase0-decision-spine-implementacion-plan-2026-08-24.md ← **PLAN CÓDIGO F0.5/F0.6** (Fit + Daily vista) · **F0.5 CERRADA** · **F0.6 Decision Board COMPLETA** (backend `8df8a65` + UI `672e88f`)
│   ├── fase0-decision-spine-asis-2026-08-24.md ← **F0.1 AS-IS Decision Spine** (inventario file:line, cero código) · HEAD `f69a7b0`
│   ├── decision-spine-cadena-2026-08-24.md ← **mapa file:line Runtime→Package→Gate→confirm/router→fill** + matriz DS-01..11 / Golden · suite `pnpm test:decision-spine`
│   ├── traspaso-relevo-track-b-b12-cierre-split-siguiente-2026-08-24.md ← relevo histórico post B12 · Track B split CERRADO
│   ├── traspaso-relevo-track-b-b11-apertura-b12-2026-08-23.md ← relevo histórico post B11 · apertura B12 thin shell (código B12 `3f9bd7e`)
│   ├── traspaso-relevo-track-b-b10-apertura-b11-2026-08-23.md ← relevo histórico post B10 · apertura B11 JSX tab `jobs` (código B11 `50649a3`)
│   ├── traspaso-relevo-track-b-b8-apertura-b9-2026-08-23.md ← relevo histórico post B8 · apertura B9 Lab handlers (código B9 `7e2d24f` sin stamp docs propio)
│   ├── traspaso-relevo-track-b-b7-apertura-b8-2026-08-23.md ← relevo histórico post B7 · apertura B8 Asistente
│   ├── traspaso-relevo-track-b-b6-apertura-b7-2026-08-23.md ← relevo histórico post B6 · apertura B7 Lista AUTO
│   ├── traspaso-relevo-track-b-b5-apertura-b6-2026-08-23.md ← relevo histórico post B5 · apertura B6
│   ├── traspaso-relevo-track-b-b4-apertura-b5-2026-08-23.md ← relevo histórico post B4 · apertura B5
│   ├── traspaso-relevo-track-b-b1-b3-apertura-b4-2026-08-23.md ← relevo histórico post B1–B3 · apertura B4
│   ├── traspaso-relevo-track-b-f4-f6-apertura-backtests-b1-2026-08-23.md ← relevo histórico post F4′–F6′ · apertura B1
│   ├── traspaso-relevo-cierre-r13-consolidacion-beta-siguiente-2026-08-22.md ← relevo histórico R-13
│   ├── traspaso-relevo-r13-apertura-2026-08-22.md ← relevo histórico apertura R-13
│   ├── plan-r12-auditoria-ux-2026-08-21.md ← **R-12 CERRADA**. Histórico. Purge V2 = monitor (E8 N).
│   ├── plan-r12-auth-d4-jwt-multiuser-2026-08-22.md ← R-12 D4 **CERRADO**. JWT-only. §1 snapshot pre-JWT es histórico.
│   ├── plan-r12-pending-delete-v2-purge-2026-08-22.md ← R-12 purge V2 (draft). E8 sigue N; métricas/flags antes de borrar migradores.
│   ├── estado-verificado-auditoria-vs-main-2026-08-21.md ← **ANCLA anti-alucinación** — GitHub `origin/main` es la verdad; tag `v1.3.0` → `b778292`. La auditoría de la mañana evaluó `75e8c23`; la de la tarde ~`49ecbcd` (sin Relevo UNO/DOS). Partida R-12 `f7a86cc`.
│   ├── plan-refactor-refuerzo-post-v1-3-0-2026-08-21.md ← **SUPERSEDIDO por R-12**. Relevo UNO (`f7a4ab0`) + DOS (`f7a86cc`) ejecutados; Fase 6 higiene absorbida en R-12 A5/A6.
│   ├── estudio-flujo-semi-vs-tops-2026-08-21.md ← R-12 Track B **APROBADO** (2026-08-21). Mesa 5 puertas.
│   ├── plan-r12-track-c-frontend-2026-08-21.md ← R-12 Track C (fases FE). **C1–C5 en `origin/main`** (`5bc51ff`…`0eb8976` · stamp **`d281b8c`**).
│   ├── traspaso-m1-reproducibilidad-backend-2026-08-10.md  ← M1 reproducibilidad backend (uv.lock)
│   ├── traspaso-m2-versiones-frontend-2026-08-10.md        ← M2 versiones frontend (@types react) CERRADO 08-10
│   ├── traspaso-m3-dominio-2026-08-10.md                   ← M3 capa de dominio (py/domain + application) · entrada original (punto de entrada del hilo)
│   ├── traspaso-m3-dominio-cierre-2026-08-11.md            ← M3 CIERRE real: 4 lotes (fix ADR-024 `ce0cdab` · código muerto 5 Protocols `f13b09d` · docstrings 33 módulos `03472dc` · coherencia MIN_SCAN_BARS+KnowledgeStage `2c41b41`) · HEAD `2c41b41` · pytest domain+application 222 passed · deuda remanente: timeframes y mypy
│   ├── traspaso-m4-infraestructura-datos-2026-08-10.md     ← M4 infraestructura/modelo datos (Prisma vs SQLAlchemy + Alembic + repos) CERRADO 08-10 · ADR-025
│   ├── traspaso-m6-ai-analytics-2026-08-10.md              ← M6 AI/analytics (py/ai + py/analytics) CERRADO 08-10
│   ├── traspaso-m5-frontend-2026-08-10.md                  ← M5 Frontend web por features (apps/web): CERRADO-08-10 como feature-slicing Diseño B agotado (ver §7 nota de cierre) / entrada
│   ├── traspaso-m5-f4-8-coach-lab-2026-08-10.md            ← M5 hilo F4.8 siguiente: Coach + Lab (feature-slicing backtests-page)
│   ├── traspaso-m5-frente-coach-cierre-2026-08-10.md      ← M5 cierre frente: Lab extraído (paso 9) · Coach extraído (paso 10) · frentes alternativos evaluados
│   ├── traspaso-m5-frente-trading-dia-d-cierre-2026-08-10.md ← M5 frente trading-dia-d: B.1+B.2+B.3 extraídos (DiaDTradesPanel, DiaDPendingTradeBanner, DiaDSessionReportPanel) · frente CERRADO
│   ├── traspaso-m5-frente-backtest-explore-cierre-2026-08-10.md ← M5 frente backtest-explore (área Coach/TOP): E.1+E.2+E.3+E.4+E.5 extraídos (BH, Header, BatteryTable, AtOutlook, StarsGrid) · frente CERRADO · M5 en pausa
│   ├── traspaso-m5-continuacion-orquestacion-2026-08-10.md      ← M5 continuación: FASE 1 orquestación list-values-panel (1.395) / instruments-page (1.222) · punto de entrada del siguiente hilo
│   ├── traspaso-m5-frente-list-values-instruments-cierre-2026-08-10.md ← M5 frentes list-values/instruments: FASE 1 + I.1+I.2 extraídos (ListSearchBox, ListSelectionToolbar) · list-values 1.395→1.242 · instruments CERRADO como ya feature-sliced · M5 en pausa
│   ├── traspaso-m5-frente-create-account-wizard-cierre-2026-08-10.md ← M5 frente create-account-wizard: C.1–C.5 extraídos (IdentityStep, CapitalStep, CommissionsStep, TaxStep, ReviewStep) · 791→632 · profile excluido por alto acoplamiento · M5 en pausa
│   ├── traspaso-m5-frente-optimize-panel-entrada-2026-08-10.md ← M5 frente backtest-optimize-panel (2.168): ENTRADA con FASE 1 hecha (7 islas data-only bajo riesgo) · primera ola sugerida C-OPT.1–C-OPT.4
│   ├── traspaso-m5-frente-optimize-panel-cierre-2026-08-10.md ← M5 frente backtest-optimize-panel: CIERRE PARCIAL bajo+medio riesgo (C-OPT.4 cardheader + C-OPT.1 summarystrip/emptytip + C-OPT.2 WF/Edge/CPCV cards + C-OPT.5 seedbanner) · 2168→1880
│   ├── traspaso-higiene-formato-legacy-entrada-2026-08-10.md ← M0/§6.2 higiene de formato Prettier legacy por lotes aislados: ENTRADA · CRLF no es deuda (git normaliza LF) · real = 643 archivos desincronizados · lote 1 (components/ui+layout) `d39bbbb` · lote 2 (backtests/optimize) `0ceeb5b` · lote 3 (backtests/explore) `7c174c7` · lote 4 (backtests/result) `d96123d` · lote 5 (backtests/wizard) `9fa403a` · lote 6 (backtests/library+strategy-matrix) `68c9dac` · lote 7 (backtests/core-r) `f241872` · lote 8 (backtests/dia-d) `0a96220` · lote 9 (backtests/assistant) `1cb6ee7` · lote 10 (backtests/optimize restantes) `9853e79` · lote 11 (backtests/strategy-matrix restantes) `ee381d7` · lote 12 (backtests/list-auto+mass-compare) `9975cd3` · lote 13 (backtests/finalists+top) `f011918` · lote 14 (backtests/coach+lab) `6f8c668` · lote 15 (backtests/hub) `6c32b06` · lote 16 (backtests/chart) `a42587e` · lote 17 (backtests/misc motores) `da2f7d9` · lote 18 (cierre misc: library/estudio/ibex) `5bec6ed` · **`features/backtests` COMPLETO** · siguiente: resto `apps/web/src`
│   ├── traspaso-higiene-formato-legacy-salida-2026-08-11.md ← M0/§6.2 higiene Prettier legacy: SALIDA/RELEVO · protocolo 8 pasos + EOL check · **COMPLETO: `apps/web/src` (fuera de `features/backtests`, ya COMPLETO) 512 files con diff real en 36 commits · HEAD `5b47f60` (línea 1-31: backtests + accounts/workspace/config+platform/research/settings/instruments/stores/lib/screeners; líneas 32-36: trading 4 sub-lotes + auxiliares+root)** · `features/backtests` COMPLETO (lotes 2-18) · **`trading` 32-35 (CIERRE) · auxiliares+root 36 (CIERRE resto `apps/web/src`) · prettier --check amplio = 0 files desincronizados** · siguiente: sin pendientes (iniciar nuevo dominio o deuda si chat saturado) · ver §7.6.i
│   ├── relevo-higiene-formato-lotes-24-27-2026-08-11.md ← M0/§6.2 relevo del hilo (lotes 19-27): estado verificado HEAD `e498c68` · protocolo 8 pasos + encodificación ASCII + aviso auto-review · dominios pendientes (screeners/charts/trading/auxiliares) · 250 files pendientes · docs a actualizar (3)
│   ├── relevo-higiene-formato-cierre-2026-08-11.md ← M0/§6.2 CIERRE del hilo de higiene Prettier: `apps/web/src` (fuera de `features/backtests`, ya COMPLETO) COMPLETO · 512 files / 36 commits · HEAD `6112fb2` · 0 files por `prettier --check` amplio · sin pendientes · ancla de cierre para futuros frentes
│   ├── chart-top1-indicator-switch-*.md
│   └── operativa-test-plan-*.md
├── Audit (entrada externa)
│   ├── audit-pack-post-audits-*.md     ← START externos
│   ├── audit1-response-*.md
│   ├── audit2-response-*.md
│   └── audit-ext-round2-triage-*.md    ← round 2 (A0 / N4 / deep)
└── Historical  ← **NO son HEAD vivo.** Fuente viva: GitHub `origin/main` + `PROJECT_STATE.md` + `backlog-trabajo-2026-08-20.md` §0 + `plan-r12-auditoria-ux-2026-08-21.md`.
    ├── archive/  ← relevos ya etiquetados `-obsoleto` (no borrar; no son entrada de chat)
    ├── traspaso-*.md / traspaso-relevo-*.md (relevos cerrados; el SHA incrustado es histórico)
    ├── session-handoff-*.md
    ├── backups/
    └── research/observations/*
```

**Anti-patrón:** un doc con tres “padres” (p. ej. enlazado como raíz desde README + HELP + lifecycle sin declarar jerarquía). Enlazar **sí**; ser raíz **no**.

**Protocolo recurrente (norma permanente, 2026-08-11):** al **cerrar cualquier hilo de chat** (fin de módulo/fase/frente o saturación de contexto), el agente DEBE preparar el siguiente: crear/actualizar su `traspaso-*` con estado + decisiones + deuda residual, añadir su entrada aquí, y **entregar al usuario en el chat el texto exacto para pegar en el siguiente hilo**. Este índice es el punto de enrutamiento de esas continuaciones.

---

## 2. Producto vs docs (no confundir)

| Capa                  | Qué es                       | Riesgo si diverge                             |
| --------------------- | ---------------------------- | --------------------------------------------- |
| **Producto (código)** | FastAPI + React + motor BT   | Fuente de verdad de comportamiento            |
| **Docs**              | Premisas, ADRs, freeze, HELP | Explican y **congelan política**; no ejecutan |

Si código y ADR divergen: **ADR o freeze gana en política**; el código se alinea o se abre enmienda. No “arreglar” en silencio.

---

## 3. CORE — taxonomía única (A0 H5)

| Código     | Nombre canónico        | Dominio      | Depende de                      | No puede depender de                |
| ---------- | ---------------------- | ------------ | ------------------------------- | ----------------------------------- |
| **CORE-P** | Profile / Policy       | Trading      | InvestorProfile, TradingPolicy  | Coach LLM, Belief                   |
| **CORE-R** | Recommendation monitor | Trading ops  | Finalistas, BD `core_r_*`       | Research Belief, Lab re-opt en vivo |
| **CORE-A** | Assistant / Coach soft | Research UX  | Ranking determinista, narración | Reordenar TOP, Belief (freeze)      |
| **CORE-B** | Behaviour / Lab board  | Research Lab | Jobs hold-out/WF                | DEMO ledger, paper_auto             |

Crecer en vertical bajo **CORE**, no inventar CORE-X sin fila aquí + issue.

---

## 4. Dependencias entre bounded contexts

Ver [bounded-contexts-2026-08-03.md](./bounded-contexts-2026-08-03.md).

**Regla de oro (A0 conclusión):** toda dependencia **nueva** entre módulos se justifica en PR (una frase) o se rechaza.

---

## 5. Auditorías externas

1.  Entrada: [audit-pack-post-audits-2026-08-03.md](./audit-pack-post-audits-2026-08-03.md)
2.  Round 2 triage: [audit-ext-round2-triage-2026-08-03.md](./audit-ext-round2-triage-2026-08-03.md)
3.  Freeze: [post-audit-decision-freeze-2026-08-03.md](./post-audit-decision-freeze-2026-08-03.md)
4.  **Round 3 (pausa motor Estudio):** [audit-brief-estudio-motor-operativo-2026-08-04.md](./audit-brief-estudio-motor-operativo-2026-08-04.md)
5.  **Round 3 triage (ratificado O3-C · D1–Canales cerrados):** [audit-ext-round3-triage-estudio-motor-2026-08-04.md](./audit-ext-round3-triage-estudio-motor-2026-08-04.md) §11 · [ADR-022](../adr/022-estudio-daily-opinion-motor.md)
6.  Diseño interno: [estudio-daily-opinion-alarms-design-2026-08-04.md](./estudio-daily-opinion-alarms-design-2026-08-04.md)
7.  **Asesor UI:** [asesor-ui-2026-08-04.md](./asesor-ui-2026-08-04.md) (ex-Research · tab Opiniones)
8.  **Pack cierre Estudio/Asesor/Canales:** [audit-pack-estudio-asesor-canales-2026-08-04.md](./audit-pack-estudio-asesor-canales-2026-08-04.md)
9.  **Thaw AUTO BETA-D:** [camino-d-auto-thaw-checklist-2026-08-04.md](./camino-d-auto-thaw-checklist-2026-08-04.md) · adaptado [`thaw-beta-adapted-remeasure-2026-08-25.md`](./thaw-beta-adapted-remeasure-2026-08-25.md) · estricto FAIL [`thaw-p1-p5-measurement-2026-08-25.md`](./thaw-p1-p5-measurement-2026-08-25.md) · **deuda estricto** [`deuda-thaw-estricto-runbook-2026-08-25.md`](./deuda-thaw-estricto-runbook-2026-08-25.md) (tracking P1–P5 · W2–W4 · snapshot `scripts/thaw_estricto_snapshot.mjs`)
10. **Triage institucional pre-AUTO (Aud 1+2):** [audit-ext-institutional-pre-auto-triage-2026-08-04.md](./audit-ext-institutional-pre-auto-triage-2026-08-04.md)
11. **Estudio operativa AUTO + gráfico (ronda auditorías abierta, 2026-08-28):** [estudio-operativa-auto-y-grafico-2026-08-28.md](./estudio-operativa-auto-y-grafico-2026-08-28.md) · arranque [arranque-auditor-estudio-operativa-auto-grafico-2026-08-28.md](./arranque-auditor-estudio-operativa-auto-grafico-2026-08-28.md) · padre [analisis-vs-apps-top-operative-flow-2026-08-28.md](./analisis-vs-apps-top-operative-flow-2026-08-28.md) · §8 **Aplazado** (N4 pendiente) · respuestas [Deep](./respuesta-auditor-Deep-operativa-auto-grafico-2026-08-28.md) · [A0](./respuesta-auditor-A0-position-operating-model-2026-08-28.md) · roadmap [path-to-10](./roadmap-v127-path-to-10-2026-08-28.md) · **sin drag / AUTO ampliado** hasta `diseno-operativa-auto-grafico-ACORDADO-*`
    11b. **V1.27 Position Operating Model:** [traspaso-relevo-v1-27-position-operating-model-2026-08-28.md](./traspaso-relevo-v1-27-position-operating-model-2026-08-28.md) · tag certificado `v1.26-beta` [relevo tag](./traspaso-relevo-tag-v1-26-beta-2026-08-28.md)
    11c. **V1.28 Daily cockpit (B-α/G2):** [traspaso-relevo-v1-28-daily-cockpit-2026-08-28.md](./traspaso-relevo-v1-28-daily-cockpit-2026-08-28.md) · badges Listas + toasts DISPARADA/T1
    11d. **V1.29 Exit and profit:** [traspaso-relevo-v1-29-exit-and-profit-2026-08-28.md](./traspaso-relevo-v1-29-exit-and-profit-2026-08-28.md) · UI ExitPolicy + trail no empeora · **V1.30 Portfolio intelligence** [traspaso-relevo-v1-30-portfolio-intelligence-2026-08-28.md](./traspaso-relevo-v1-30-portfolio-intelligence-2026-08-28.md) · **V1.31 UX 10/10** [traspaso-relevo-v1-31-ux-10-2026-08-30.md](./traspaso-relevo-v1-31-ux-10-2026-08-30.md) (palette + densidad) · **V1.31.1 tema** [traspaso-relevo-v1-31-1-tema-claro-2026-08-30.md](./traspaso-relevo-v1-31-1-tema-claro-2026-08-30.md) · **V1.31.2 residual** [traspaso-relevo-v1-31-2-ux-residual-2026-08-30.md](./traspaso-relevo-v1-31-2-ux-residual-2026-08-30.md) (layouts · flash · KPI Protección) · **V1.32 SEMI paper maduro** [traspaso-relevo-v1-32-semi-paper-maduro-2026-08-30.md](./traspaso-relevo-v1-32-semi-paper-maduro-2026-08-30.md) · **V1.33 AUTO A-β** [traspaso-relevo-v1-33-auto-a-beta-2026-08-30.md](./traspaso-relevo-v1-33-auto-a-beta-2026-08-30.md) (paridad TradePlan; A-δ Estudio; no drag / no thaw)
12. **Risk Engine OR-RE v0:** [risk-engine-or-re-2026-08-04.md](./risk-engine-or-re-2026-08-04.md)
13. **Fase 0 Decision Spine — F0.1 AS-IS:** [fase0-decision-spine-asis-2026-08-24.md](./fase0-decision-spine-asis-2026-08-24.md) — inventario file:line; cero código
    13b. **Decision Spine cadena (probar, no reescribir):** [decision-spine-cadena-2026-08-24.md](./decision-spine-cadena-2026-08-24.md) · SoT corto [CURRENT_SYSTEM.md](../CURRENT_SYSTEM.md) · `pnpm test:decision-spine`
14. **OR-lite + Repro+ + Obs/CI:** [or-lite-repro-obs-2026-08-04.md](./or-lite-repro-obs-2026-08-04.md)
15. **Prep A2–A5 + ADR-023 BETA-D:** [camino-d-a2-a5-prep-2026-08-04.md](./camino-d-a2-a5-prep-2026-08-04.md) · [ADR-023](../adr/023-camino-d-thaw.md) **Accepted BETA-D** · estricto deuda [`deuda-thaw-estricto-runbook-2026-08-25.md`](./deuda-thaw-estricto-runbook-2026-08-25.md)
16. **Pack auditoría prep AUTO:** [audit-pack-pre-auto-a0-a5-2026-08-04.md](./audit-pack-pre-auto-a0-a5-2026-08-04.md) — SEMI OK · execute AUTO **no**
17. **Resumen operativo diario (R1–R4):** [daily-ops-report-brief-2026-08-04.md](./daily-ops-report-brief-2026-08-04.md) — Diario · HTML email · PDF opt-in
18. **Auditoría consolidada interna+externas + plan hardening (2026-08-11):** [audit-consolidado-internas-externas-2026-08-11.md](./audit-consolidado-internas-externas-2026-08-11.md) — 4 fuentes cruzadas (interna · externa 1 · externa 2 · externa 3) · mapa P0/P1/P2 · plan F1–F5 · decisiones D0–D5 pactadas · checkpoint git `audit-checkpoint-2026-08-11` · NO tocar código
19. **Pack estado global post-tag v1.7.0-beta (2026-08-24e):** [audit-pack-estado-global-2026-08-24e.md](./audit-pack-estado-global-2026-08-24e.md) — HEAD vivo **`386a959`** · tag **`v1.7.0-beta` → `e3b943a`** · Ciclo 1 OP/Journal **F1–F3 CERRADO** · F9-A3 CI **`f8c7e3f`** · Ruff I001 **`a3dcc3f`** · pytest CI **`386a959`** (569 pass offline) · Alembic `010` en `bolsa_v1` · `pnpm test:decision-spine` **53** · histórico: [24d](./audit-pack-estado-global-2026-08-24d.md) · [24c](./audit-pack-estado-global-2026-08-24c.md) · [24b](./audit-pack-estado-global-2026-08-24b.md) · [2026-08-24](./audit-pack-estado-global-2026-08-24.md) · [2026-08-22](./audit-pack-estado-global-2026-08-22.md) · [2026-08-20](./audit-pack-estado-global-2026-08-20.md)
    >     └── **Plan F1 (integridad financiera):** [plan-f1-integridad-financiera-2026-08-11.md](./plan-f1-integridad-financiera-2026-08-11.md) — micro-cambios M1–M5 (`with_for_update`, `deduct_cash`, `ExecuteTrade`, idempotencia, invariantes) · batería+riesgos+orden commits · pendiente de aprobación
    >         └── **F1 — traspaso de hilo:** [traspaso-f1-integridad-financiera-2026-08-11.md](./traspaso-f1-integridad-financiera-2026-08-11.md) — documento de trabajo F1 (integridad financiera) **CERRADO 2026-08-11** ✅ · rama `stage/f1-integridad-financiera-2026-08-11` creada desde `7edb3d1` · M1 `3d093cb` (with_for_update) · M2 `113e27a` (deduct_cash allow_partial, Opción B) · M3 `5128cf4` (ExecuteTrade saldo real) · M4 `986399d` (idempotencia+contrato estricto, migración Prisma `20260811010000_trade_idempotency`) · M5 `3c59dc8` (suite invariantes contables) · **verificación final**: web 707 ✓ · infra M2+M5 8+8 ✓ · api M4 3 ✓ · ruff 0 nuevos · mypy 0 nuevos (71 pre-existentes base) · **cero regresiones** · commits M1→M5 · ver §9 del traspaso
    >         └── **F2 — traspaso de hilo:** [traspaso-f2-backtest-next-open-2026-08-11.md](./traspaso-f2-backtest-next-open-2026-08-11.md) — documento de trabajo F2 (Rigor científico del backtest, fill `next_open`) **COMPLETADO 2026-08-11** · A `backtest.py` `execution_model` next_open · B grids H0 SMA/RSI/MACD + `optimize.py` · C `vectorbt_sma`/`optuna_sma` shift+open · D fingerprint OHLCV completo + bump manifest 1.1 / engine 0.4.0 · E `test_no_lookahead.py` · F `scripts/research/recalc_trials_next_open.py` · pytest analytics 323✓ + application 222✓ + api offline 9✓ = **554✓ · 0 fallos** · **PR #30 MERGED** (fast-forward en `stage/f1-*`, 6 commits C1–C6, rama `stage/f2-*` eliminada) · **recálculo de trials ejecutado 20/20 (`next_open`, engine 0.4.0)** · fix `distinct` SQLAlchemy 2.0 (`a3573ae`) · deuda: `--mark-legacy` no-op (sin columna, → F3b) · ver §9
    >         └── **F3b — traspaso de hilo:** [traspaso-f3b-alembic-data-epoch-2026-08-11.md](./traspaso-f3b-alembic-data-epoch-2026-08-11.md) — documento de trabajo F3b (Alembic autoridad BD + columna `data_epoch`) **COMMITEADO + PR #31 ABIERTO** · A `env.py` real · B alembic.ini `prepend_sys_path`/`path_separator` · C baseline `001` tolerante a TimescaleDB ausente · D migración `002_research_data_epoch` (DDL `data_epoch` en `backtest_runs`/`research_trials`) · E `database/migrations.ensure_migrated()` (upgrade head idempotente) · F llamada en lifespan (P1.2, fuera del path) · G columna en `tables.py` · H `--mark-legacy` deja de ser no-op (`_mark_legacy` legacy/next_open) · I `test_f3b_alembic_data_epoch.py` · pytest infra 46✓ + app 222✓ + analytics 323✓ + api offline 11✓ = **602✓ · 0 fallos** · `alembic_version=002` aplicado en local · **PR #31 MERGED 2026-08-11** (merge commit `014a207` en `stage/f1-*`) · deuda: portar DDL Prisma→Alembic (→F3a/F4), `account_repository.ensure_migrated` por-request (→F3a) · ver §9
    >         └── **F5a — traspaso de hilo:** [traspaso-f5a-contratos-fe-be-2026-08-11.md](./traspaso-f5a-contratos-fe-be-2026-08-11.md) — documento de trabajo F5a (Contratos FE/BE: OpenAPI fuente de verdad + drift gate, hallazgo P1.5) **COMMITEADO + PR #32 ABIERTO (6 commits C1–C6, HEAD `dde9c32`)** · rama `stage/f5a-contratos-fe-be-2026-08-11` desde `stage/f1-*` (tras merge PR #31) · A `apps/api-python/scripts/dump_openapi.py` (offline, sin servir/BD) · B `apps/web/api/openapi.json` versionado (**180 paths / 367 schemas**, fuente de verdad) · C `apps/web/src/api/schema.d.ts` generado (`openapi-typescript@7.13.0`) · D `apps/web/scripts/sync-contract.mjs` (`contract:gen` + `contract:check` gate de spec) · E scripts en `apps/web/package.json` + devDep · F `apps/web/src/api/contract-check.ts` gate de tipos (claves FE ⊆ contrato, sentinelas BacktestRunDto/PortfolioSummaryDto/InvestmentAccountDto) · **G (corrección C5–C6)** `.prettierignore` (excluye artefactos generados) + LF en dump + norm. CRLF en `--check` → contrato reproducible byte-a-byte, tree limpio tras `contract:gen` · batería: ruff✓ mypy✓ pytest api-python **27✓** · web typecheck✓ lint✓ test **707✓** · contract:check✓ (verificado falla ante drift) · **drift medido P1.5**: `manifest` FE ⊆ BE roto, normalizaciones number↔integer / ?↔null · deuda: sustituir DTOs manuales fidelidad campo-a-campo + `openapi-fetch` como cliente (no en esta fase) · ver §9
    >             └── **F5a-fidelidad — traspaso de fase:** [traspaso-f5a-fidelidad-gate-2026-08-12.md](./traspaso-f5a-fidelidad-gate-2026-08-12.md) — **COMPLETADO + MERGEADO 2026-08-12** · rama `stage/f5a-fidelidad-gate-2026-08-12` desde base `d892b3f` (tip F5b-drift) · **gate `contract-check.ts` BIDIRECCIONAL** (nuevo tipo `CoversContract<BE,FE>` = `Exclude<keyof BE, keyof FE>`) cubriendo FE⊆contrato Y contrato⊆FE · **sentinelas 5→11** (G1–G11: +`InstrumentDto`/`OhlcvBarDto`/`PortfolioDto`/`PositionDto`/`InvestmentPortfolioDto`/`AccountSummaryDto`) · si el BE emite un campo nuevo, el typecheck rompe (antes no se detectaba) · **cero cambios de DTOs shared** (todos ya con igualdad de claves) · solo `contract-check.ts` modificado · probe de drift contrato→FE verificado positivo (G9 rompe con `driftProbe`) y restaurado · batería: contract:check **VERDE** ✓ reproducible · web typecheck✓ · lint 0 errores · shared typecheck✓ · test **714✓** (141 f) · **PR #38 MERGED** (merge commit `bc51980` en `stage/f1-*`) · base viva `stage/f1-*` ahora `bc51980` · deuda: `openapi-fetch` completo (fase propia), reconciliar ~87+ DTOs restantes / consolidar tipos web-only → P2.6, fidelidad de tipos del valor (`manifest`/`number`↔`integer`) fuera de claves · ver §8
    >                 └── **F5a §6 openapi-fetch — traspaso de fase:** [traspaso-f5a-openapi-fetch-2026-08-12.md](./traspaso-f5a-openapi-fetch-2026-08-12.md) — **COMPLETADO + MERGEADO 2026-08-12** · rama `stage/f5a-openapi-fetch-2026-08-12` desde base `d25d71f` (PR #51) · **`openapi-fetch@^0.17.0` como cliente HTTP completo** de `apps/web/src/lib/api.ts` (2083→1904 líneas): `createClient<paths>` + un middleware (`Authorization`/`X-Account-Id`/`Content-Type` respetando cabeceras ya presentes; `TypeError` de red → `ApiError` 0) + helper `call<T>` que desenvuelve `{data,error,response}` replicando `request<T>` (401 específico/`formatApiErrorDetail`/204→undefined) + `apiBody<never>` (cast benigno por optionality del requestBody, wire idéntico) · **D5** superficie `api` idéntica, cero call sites, cero cambios de wire (`openapi.json`/`schema.d.ts` sin diff) · path params snake_case del contrato via `params.path` (`{instrument_id}`/`{account_id}`/`{run_id}`) · `depositCash`/`withdrawCash` preservan `X-Account-Id` por-request · `updateWorkspaceKeepalive`/FormData/blob conservan `fetch` crudo · batería: contract:check **VERDE** ✓ reproducible · web typecheck✓ · lint 0 (2 warnings pre-existentes) · test **714✓** (141 f) · shared typecheck✓ · commits `a302f22`/`cfebdcb` · CI battery/quality/scan **VERDES** · **PR #52 MERGED** (merge commit `cd6bf35` en `stage/f1-*`) · base viva ahora `cd6bf35` · deuda: reconciliar ~87+ DTOs vs contrato + fidelidad de tipos de valor (`manifest`/`number`↔`integer`/`null`↔`undefined`) → **P2.6** · ver §5-§6 del traspaso
    >             └── **Fixes operativos/CI (2026-08-12):** [`traspaso-f1-integridad-financiera-2026-08-11.md`](./traspaso-f1-integridad-financiera-2026-08-11.md) (deuda de la línea base) — **3 fix-MERGEs de arranque y CI** que surgieron al operar (terminal 13) y desbloquearon los merges #38/#39/#40 · **PR #39** (fix scheduler Windows): `scheduler_worker.py` forzaba `asyncio.run` sobre el `ProactorEventLoop` de Python 3.12 en win32 → `psycopg.InterfaceError` (psycopg no soporta Proactor). Fix: `asyncio.Runner(loop_factory=lambda: loop)` con `asyncio.SelectorEventLoop()` cuando `sys.platform == "win32"`. Rama `stage/f3a-fix-scheduler-selector-loop-win`, commit `1c05ffc`, **PR #39 MERGED** (merge commit `4deb025`) · **PR #40** (fix CI shared): `quality` fallaba en TODOS los PRs con `vitest: not found` (ENOENT) porque `packages/shared/package.json` declara `"test": "vitest run"` pero **no tenía `vitest` en devDependencies** (en local pasaba por hoisting de `apps/web`; en CI se instala por-paquete). Fix: `"vitest": "^3.2.3"` en devDependencies de shared + lockfile. Rama `stage/f5c-fix-shared-vitest-ci`, commit `15e002d`, **PR #40 MERGED** (merge commit `07aa2fb`) · orden de merge real: `d892b3f → 07aa2fb (PR #40) → 4deb025 (PR #39) → bc51980 (PR #38)` · battery/quality/scan **VERDES** tras el fix #40 · deuda de operativa: el `run-dev.mjs` debe reiniciarse una vez en la base `bc51980` para que el scheduler use el worker corregido (sin --reload) · **nota validada 2026-08-12**: el usuario reinició el dev (pid nuevo) y el terminal ya NO muestra el traceback; worker corregido procesa datos reales · ver §9
    >             └── **F-IND-1 — Causality Layer (2026-08-19):** rama `stage/f1-integridad-financiera-2026-08-11` (`79fa155`, merge commit) — sub-ola de integridad por **causalidad de indicadores** pactada en `PROJECT_STATE.md` · **metadatos causal/confirmationLag/visualizationOffset/nonCausalOutputKeys** en `indicator-universe.ts` (IND-FR `causal:false, confirmationLag:2`; IND-ICH familia causal pero salida `chikou` no causal) · **guardia estática** `_NON_CAUSAL_OUTPUT_LINES` en `rules_engine.py`: `ich:chikou`/`fr` devuelven `None` en el feature set de backtest/research (el chart NO pasa por aquí) · `validate_strategy_definition` rechaza features no causales · tests anti-lookahead pytest (`test_causality_layer.py`) + metadata vitest (`indicator-causality.test.ts`) · **estado real del doc**: PROJECT_STATE §3 refleja F-IND-1 ✅ MERGED (79fa155) · F-IND-2 (batería) entrante → abajo
    >                 └── **F-IND-2 — batería de causalidad en CI (2026-08-19):** commit `09fb06b` (pend. push/merge en `stage/f1-*`) · `packages/py/analytics/tests/test_causality_battery_ind_2.py` (34 tests) — **`feature_at_t` con/sin barra futura idéntico para todos los indicadores** · 31 indicadores causales parametrizados DEBEN ser prefix-estables (`compute_spec` sobre 220 barras vs 210 + 10 futuras; igualdad para todo `t<cut`) · 2 canarios no causales (chikou/fractals) DEBEN romper la propiedad + guard de cobertura de `definition_id` · batería: ruff (config CI root) ✓ · mypy strict file ✓ · pytest analytics **362✓** 0 fallos · CI `python-ci.yml` lo recoge automáticamente → **paso de traspaso en `PROJECT_STATE.md` §6, próxima F-FIN-1**
    >                   └── **F-FIN-1 — default de cartera fail-closed (2026-08-19):** commit `f595761` (pend. push/merge en `stage/f1-*`) · **feedback de ADR-008 multi-cuenta**: elimina `get_or_create_default_portfolio()` que resolvía SIEMPRE la cartera por **nombre global** ("Cartera principal") — una bomba latente que, si un call-site omitiera el scope, podía tocar la cartera por defecto de otra cuenta · exploración previa confirmó que el flujo API/worker viva NO lo alcanza (cada cuenta usa su cartera vía `AccountScope.legacy_portfolio_id`) · fix **fail-closed**: `_resolve_portfolio` exige `legacy_portfolio_id: str` y lanza `ValueError` si el id no existe (nunca resuelve un default global); `get_summary`/`list_transactions`/`execute_trade` requieren id de scope · test anti-regresión `test_fin1_no_global_default_portfolio_by_name` · batería: ruff ✓ · mypy full-tree 243 ✓ · pytest infra **49✓** + application **224✓** + api **29✓** → **paso de traspaso en `PROJECT_STATE.md` §6, próxima F-FIN-2**
    >                     └── **F-FIN-2 — ejercicio fiscal en `GetTaxReport` (2026-08-19):** commit `9a35405` (pend. merge en `stage/f1-*`) · **fix de integridad fiscal**: el explorador desveló que «filtrar transacciones por año en SQL» habría ROMPIDO el cost basis FIFO/avg (las ventas de un año usan lots de compras de años anteriores, carry-in) · solución: `fiscal_year_range(year,start_month)` en dominio (rango `[inicio,fin)` UTC, respeta `fiscal_year_start_month`) · `list_transactions` acepta `limit=None`+`executed_before=fiscal_end` (preserva carry-in, excluye años futuros, elimina el truncado de `limit=10000` que cortaba las compras antiguas) · `list_for_account`/`total_fees_for_account` filtran por rango (fees del año) · `fees_paid_total` en dominio suma solo fees del ejercicio (antes sumaba todas) · test `test_tax_report_fiscal.py` (5: rango natural/desfasado, fees del año, FIFO cross-year, realized por año fiscal) · batería: ruff ✓ · mypy full-tree 243 ✓ · pytest domain **14✓** + application **224✓** + infra **49✓** + api **29✓** + integration tax **1✓** └── **F-SEG-1 — fail-closed production + comparaciones constantes (2026-08-19):** commit `4b7a984` (pend. merge en `stage/f1-*`) · **feedback de seguridad (audit externa 1, find 2026-08-19)**: production podía arrancar sin `APP_PASSWORD` o con `APP_AUTH_SECRET` vacío/`bolsa-dev-secret` (solo aviso `degraded` en `/health`) · fix **fail-closed**: `config.py` `compose_database_url` `@model_validator` bloquea el arranque si `ENVIRONMENT`∈{`prod`,`production`} y falta `APP_PASSWORD`, o `APP_AUTH_SECRET` vacío/`"bolsa-dev-secret"` — **condicionado estrictamente a prod** para no romper dev/tests/CI (anti-regresión `test_dev_sin_password_sigue_ok_no_rompe`) · **anti timing-attack**: `tokens.py:17` y `routes/auth.py:42` de `==` → `secrets.compare_digest` · auth JWT sigue diferida (D4) · test `test_config.py` +5 (prod sin password, secreto vacío, dev-secret, secreto real OK, dev sin password ok) · batería: ruff ✓ · mypy full-tree 243 ✓ · pytest config/auth **14✓** + api **29✓** + infra **49✓** + application **224✓** + startup **5✓** → **paso de traspaso en `PROJECT_STATE.md` §6, próxima F-SEG-2**
    >                       └── **F-SEG-2 — auditoría git + rotación de logs + redacción de secretos (2026-08-19):** commit `dcb8a37` (pend. merge en `stage/f1-*`) · **bloque de seguridad (audit externa 1, find 2026-08-19)**: repo público con historial no auditado + ~155,8 MB de logs sin rotación + riesgo de filtración de secretos por `repr`/logs · auditó el **historial completo (417 commits)** con git de solo lectura: sin secretos críticos (solo valores dev `bolsa:bolsa_dev`/`bolsa-dev-secret` ya eliminados en HEAD, pero visibles en la historia antigua — purga con filter-repo/BFG **NO ejecutada** por protocolo; activar GitHub secret scanning nativo + confirmar `gitleaks.yml`) · **rotación**: `pruneStampedLogs()` en `scripts/lib/logger.mjs` conserva las 10 sesiones `dev/*.log` más recientes (`DEV_LOG_KEEP` ajustable), invocado en `run-dev.mjs` (el grueso eran 151,4 MB de `logs/dev/dev-*.log`, uno por cada `pnpm dev`) · **redacción**: `__repr__` de `config.py` redacta `app_password`/`app_auth_secret`/`db_password` + credenciales de DSN (`_redact_dsn_credentials`); `logging_redact.py` +3 patrones (`.app_password`/`.app_auth_secret`/`.db_password`) · +5 tests negativos (test_config ×3: repr no expone password/secret/credenciales DB + error validación no filtra secreto; test_q2_hygiene ×2: pares `key=valor` no filtran valores + LogRecord no contiene secretos reales) · batería: ruff 0 ✓ · node --check OK (logger/run-dev) · pytest config **15✓** + hygiene **3✓** + infra **57✓** → **paso de traspaso en `PROJECT_STATE.md` §6, próxima F-SEG-3**
    >                         └── **F-SEG-3 — CORS mínimo privilegio + `X-Forwarded-For`/TrustedHost (2026-08-19):** commit `e628ae3` (pend. merge en `stage/f1-*`) · **bloque de seguridad (audit externa 1)**: `allow_methods=["*"]`/`allow_headers=["*"]` + rate-limit que ignoraba `X-Forwarded-For` (IP real del peer, sin TrustedHost) · **CORS**: `main.py` pasa a listas explícitas (`GET/POST/PUT/PATCH/DELETE/OPTIONS`; `Content-Type/Authorization/X-Account-Id/Accept`) fijadas por el contrato real del FE (`apps/web/src/lib/api.ts`); se mantienen `allow_credentials`/`allow_origins`/`allow_origin_regex` dev-only · **rate-limit anti-spoofing**: `get_client_ip()` en `middleware/rate_limit.py` — usa **la primera IP de `X-Forwarded-For` solo si el peer inmediato está en `TRUSTED_PROXIES`** (setting nueva `trusted_proxies` en `config.py`, default vacío → dev/local usa `client.host`; anti-regeneración de contador); peer de confianza sin header → cae a `client.host`; fallback `RedisStore`→`MemoryStore` intacto · **10 tests**: `test_cors.py` (nuevo, ×5: methods/headers explícitos, preflight OK del origen permitido, origen no permitido sin allow-origin) + `test_rate_limit.py` (+5: ignora XFF sin trusted, usa primera XFF con trusted, coma-separados, sin XFF cae a client.host, trims whitespace) · batería: ruff 0 ✓ · mypy full-tree 0 ✓ · pytest CORS/rate-limit/config **38✓** + API offline **47✓** → **paso de traspaso en `PROJECT_STATE.md` §6, próxima F-HLTH-1**
    >                           └── **F-HLTH-1 — mojibake en `workspace-store-core.ts` (2026-08-19):** commit `2400a4b` (pend. merge en `stage/f1-*`) · **hallazgo de auditoría interna propia (halzg)**: el archivo `apps/web/src/stores/workspace-store-core.ts` (1.137 líneas) contenía mojibake UTF-8→Latin-1 — 2 strings UI de label (`"GrÃ¡fico"` → `"Gráfico"`, default de pestaña) + ~28 JSDoc/comentarios corruptos (`Ãº→ú`, `Ã­→í`, `Ã©→é`, `Ã±→ñ`, `Ã¡→á`, `Ã³→ó`; `â€”→—`, `â†’→→`, `â€¦→…`) · **fix directo en el hilo** (sin subagente: 1 archivo, corrección de text-encoding, resta 28+/28−, solo comentarios y 2 strings de label; lógica de código intacta según diff) · batería web: typecheck `tsc -b --noEmit` 0 ✓ · lint eslint `src/` 0 errores (2 warnings preexistentes ajenos en `backtests-page.tsx`) ✓ · build vite producción OK ✓ → **paso de traspaso en `PROJECT_STATE.md` §6, próxima F-DEBT-1**
    >                             └── **CIERRE sub-ola hardening F-IND-2→F-HLTH-1 (2026-08-19):** [`traspaso-ola-hardening-cierre-2026-08-19.md`](./traspaso-ola-hardening-cierre-2026-08-19.md) — **MERGEADO a `main`** (`eb31a7d..94aee47`, fast-forward de toda la ola F1→F5a + sub-ola) · documenta por fase commit/batería del cierre de sub-ola, acciones pendientes acumuladas (GitHub secret scanning, `TRUSTED_PROXIES` prod, limpieza `logs/dev` ~150MB, purga historial dev, no recalcular backtests chikou) · **próxima fase F-DEBT-1 = P1.9 API thin (alcance exclusivo)**; `mypy ~450` ya cerrado (P1.6 `6a89f6c`); P2.6 → F-DEBT-2 (deuda futura) · texto de traspaso para el nuevo hilo en el §4 de ese doc y en `PROJECT_STATE.md` §6
    >                                 └── **F-DEBT-1 P1.9 — cierre de hilo (2026-08-19):** [`traspaso-p1.9-api-thin-2026-08-19.md`](./traspaso-p1.9-api-thin-2026-08-19.md) — **F-DEBT-1 = P1.9 API thin CERRADO** en `stage/f1-integridad-financiera-2026-08-11` (alcance exclusivo SOLO P1.9) · **10 slices COMMITEADOS**: `21d3dd0` (_iso/SignalEventV1) · `19cd433` (scans/trackers → schemas.scans) · `8d63ea7` (blob-sync → use-cases account_blob_state) · `4297528` (instrument_daily_opinions schemas+DI) · `836348c` (research 8 `_to_*_dto`) · `8406d55` (15 DTOs ai_governance → schemas/ai_governance) · `6a1c57f` (DI `get_cognitive_repository`) · `2331ad9` (getters propose/confirm + `_EdgeReportAdapter` en dependencies.py) · `939e477` (use-case `EnsureAccountInvestorProfile` + `DeclaredProfileInput`; create_account delega) · `cbfac26` (A4-safe `response_model` list_decision_sessions `DecisionSessionSummaryDto`; contract gen) · batería: ruff 0 · mypy 244 files 0 · pytest api 56✓ / 164✓+flake pre-existente · `contract:check` OK (D5 cero wire) · **deuda residual P1.9 = A4/B2 `response_model` shape-abierto (13 ai_governance + 3 accounts) — DIFERIDO por decisión usuario (cambia el contrato OpenAPI, fuera de D5)** · texto de traspaso en el §4 de ese doc y en `PROJECT_STATE.md` §6 · después: F-WORKER-1 (cerrado abajo) · F-DEBT-2
    >                                 └── **F-WORKER-1 — cierre (2026-08-19):** [`traspaso-worker1-auto-sync-bp-2026-08-19.md`](./traspaso-worker1-auto-sync-bp-2026-08-19.md) — **F-WORKER-1 CERRADO** (documental, riesgo Bajo) · warning heredado auto-sync ticker `BP/.L` (Yahoo 404) **evaluado y cerrado** retomando el subagente previo (transcript `41c57061`, 12/08) · **causa raíz = dato BD corrupto** (`yahoo_symbol='BP/.L'`, slash literal; ticker válido `BP.L`), NO bug de mapping de código (el `yahoo_client` emite `chart/{yahoo_symbol}` fiel a lo almacenado) · **fix de comportamiento YA MERGED** (PR #43, `4a1dc69`): 404 → `YahooSymbolNotFoundError` permanente (`retryable=False`), sin retry (`fail_item(retry=result.retryable)`), WARNING→INFO en `auto_sync_worker` — verificado en el código actual (`yahoo_client.py:228-233` · `sync_instrument.py:171-186` · `sync_scheduler.py:166-179` · `auto_sync_worker.py:50-61`) · **acción residual manual (NO código)**: corregir el registro en BD a `BP.L` si se quiere dato real · cierre documental sin batería (0 cambios de código) · texto de traspaso en el §5 de ese doc y en `PROJECT_STATE.md` §6 · siguiente: F-DEBT-2
    >                                 └── **SINCRONIZACIÓN `stage → main` (2026-08-19):** — verificación `PROJECT_STATE.md` §4 punto 20 / §6 / §7 (sección maestro) · **fast-forward `7d41ca0..45fc724`** de 14 commits (P1.9 API thin completo 10 slices, cierre F-WORKER-1, fix fiscal P2.1, README P2.3) **MERGEADO y pusheado** (`origin/main = origin/stage = 45fc724`) · **CI `main` VERDE** vía `gh run watch` (Python CI, Frontend CI, Fase 2 scientific, Optimize lab, Gitleaks) · **motivo**: la Auditoría 2 no veía los cierres porque estaban en `stage` (14 commits por delante de `main`); la sincronización alinea `main` con el estado real para la próxima lectura · aviso no-bloqueante Node.js 20 deprecado en actions (→ **rama R-5** del plan de refactor) y 2 warnings React en `backtests-page.tsx:1228,1231` · **plan de refactor 2026-08-19 aprobado por fases**: R-1 ops/seguridad documental · R-2 F-DEBT-2/P2.6 tipos web-only en `packages/shared` (gate D5) · R-3 fidelidad de valor + reconciliación DTOs wire (`regen_full`) · R-4 A4/B2 response_model (`regen_full`) · R-5 CI Node 20 · **regla**: subagente aislado por fase + batería + aprobación por commit + relevo documentado al cerrar chat
    >                                 └── **R-1 — cierre deuda operativa/seguridad (2026-08-19):** [`ops-r1-seguridad-operaciones-2026-08-19.md`](./ops-r1-seguridad-operaciones-2026-08-19.md) — **R-1 VERIFICADO y documentado** · **Naturaleza**: 100% operativa/manual, **0 cambios de código** · **código ya hecho**: `gitleaks.yml` activo (CI verde) + setting `TRUSTED_PROXIES` en `config.py:28` · **checklist manual pendiente (fuera de repo)**: activar GitHub **secret scanning** nativo en UI · definir **`TRUSTED_PROXIES` prod** con IPs del proxy de borde (**bloqueado por valores reales del usuario**) · limpiar `logs/dev` locales (gitignore ya cubre `logs/**`) · corregir `BP/.L→BP.L` en BD local si se quiere dato real (F-WORKER-1) · opcional: purga de valores dev en historial público (filter-repo/BFG, decisión explícita) · **sin batería de código** (no aplica) · **próxima fase pactada: R-2 = F-DEBT-2/P2.6** tipos web-only en `packages/shared`
    >                                 └── **R-2 — F-DEBT-2/P2.6 consolidar tipos web-only en `packages/shared` (2026-08-19):** [`traspaso-r2-fdebt2-tipos-shared-2026-08-19.md`](./traspaso-r2-fdebt2-tipos-shared-2026-08-19.md) — **R-2 COMPLETADO y MERGED a `main` vía PR #54** (fast-forward `4271ccf..7e6cb24`, rama `stage/r2-fdebt2-tipos-shared-2026-08-19` sincronizada a main, árbol limpio, CI PR verde battery/quality/scan) · **verificado**: los candidatos explícitos `RecommendationV1`/`execution-policies`/`tax-report`/`RunManifest` ya vivían en shared (el único web-only real era la familia CORE-R) · **3 commits**: `2f5e458` (familia CORE-R → `packages/shared/src/core-r-judgment.ts` nuevo + espejo `core-r-api.ts` enriquecido con `\| string` seguridad D5; web re-exporta sin tocar call-sites) · `97c47a7` (cola Supervised-F3 → `supervised-f3-api.ts` payload tipado vía `SupervisedQueueOriginDto`/`SupervisedProposePayloadDto`/`SupervisedEnqueueMetaDto`; **elimina el cast `as unknown as`** de `supervised-f3-sync.ts`) · `7e6cb24` (dedupe unions `MandateActor`/`MandateReason` → `MandateActorDto`/`MandateReasonDto`) · **gate D5**: `contract:check` VERDE por commit, `openapi.json`/`schema.d.ts` sin cambios (re-declaración de forma) · **batería**: shared build/typecheck/lint/test 15 ✓ · web typecheck/lint ✓ · web test **714✓** · build ✓ · **punto abierto**: dominio `MandateTenure`/`MandateTradeLink` del web conserva shape localStorage (NO el `*Dto` wire), no consolidado por D5 → deuda futura si se quiere único hogar · **próxima: R-3** fidelidad de valor + reconciliación ~87 DTOs wire (requiere `regen_full`, decisión explícita)
    >                                 └── **R-3 — fidelidad de valor de tipos (2026-08-19):** [`traspaso-r3-fidelidad-valor-tipos-2026-08-19.md`](./traspaso-r3-fidelidad-valor-tipos-2026-08-19.md) — **R-3 COMPLETADO en `main`** (commit directo `df269f0`, sin PR, árbol limpio) · **hallazgo clave: NO requirió `regen_full`** — `contract:check` VERDE y el artefacto generado (`schema.d.ts`) ya reflejaba la verdad del wire; el gap estaba en los tipos shared (prometían no-nulo) · auditoría: 143 DTOs mapeados, deep en ~40 dinero/valor · **solo 5 DTOs (14 campos) con gaps reales**, todos FE-only (añadir `\| null`/`?`): `BacktestRunDto`/`Detail` (`timeframe`/`dataVersion`/`commissionBps`/`slippageBps`/`manifest`/`strategyDefinitionId`/`equityCurve`, sentinela G1), `InstrumentRecordDto.profileFetchedAt`, `ScanRunResultDto` (`strategyDefinitionId`/`listId`), `ScanUniverseDto` (`listId`/`instrumentIds`), `IndexSubscribeJobDto` (`result`/`error`/`completedAt`) · gate D5: `contract:check` VERDE, cero wire · **batería**: shared typecheck/lint 0 · shared test 15 ✓ · web typecheck/lint 0 errores · web test **714✓** · **nota**: churn formato precommit (`prettier` comillas dobles sobre legacy comillas simples) preexistente en R-2, opcional `.prettierrc` en R-5 · **próxima: R-4** (A4/B2 `response_model`, requiere `regen_full`) · **R-5** (CI Node.js 20 + optional `.prettierrc`)
    >                                 └── **R-4 — A4/B2 `response_model` shape-abierto (2026-08-19):** [`traspaso-r4-response-model-shape-abierto-2026-08-19.md`](./traspaso-r4-response-model-shape-abierto-2026-08-19.md) — **R-4 COMPLETADO en `main`** (2 commits directos `38b31d1` fix call-sites R-3 + `01b3093` R-4 response_model, sin PR, árbol limpio) · **hallazgo clave: NO requirió `regen_full`** — auditoría: 13 endpoints JSON comparten envelope fijo `{data: <open>}` → reutilizar `AiEffectivenessResponseDto` (wrapper `data: dict[str,Any]`, componente ya existente) es **wire-safe** (tipar solo la clave `data` no dropea claves internas) · **13 categoría A** (12 AI en `ai_governance.py` + `daily-ops-report` en `accounts.py`), **0 B**, **2 C** excluidos (`daily-ops-report.pdf` Response + `DELETE /accounts/{id}` 204) · `contract:gen`: 13 paths pasan de objeto anónimo `additionalProperties` a `$ref` del componente existente (**cero componentes nuevos**) · **fix adicional R-3** (`38b31d1`): al regenerar, `tsc` completo destapó 2 assignaciones latentes de R-3 (cache `tsc -b`): `equityCurve ?? undefined` (`backtest-deep-coach.ts:1197`) + `profileFetchedAt ?? null` (`instrument-db-tab.tsx:421`) · **batería**: ruff (full-tree) 0 · mypy 0 · pytest 16 ✓ · **D5 `contract:check` VERDE** · web typecheck/lint/build ✓ · web test **714 ✓** · deuda mínima: nombre semántico del wrapper (efectiveness) cosmético; inner `data` NO tipado por diseño · **próxima: R-5** = CI Node.js 20 deprecado (bump actions) + opcional `.prettierrc`
    >                                   └── **RELEVO para re-auditoría de la APP (2026-08-19):** [`relevo-para-reauditoria-2026-08-19.md`](./relevo-para-reauditoria-2026-08-19.md) — **texto de paso listo para el próximo hilo** (`local main = origin/main = f9767d8`, árbol limpio) · consolida R-2 (PR #54) + R-3 (`df269f0`+`a82068f`) + R-4 (`38b31d1`+`01b3093`+`f9767d8`) en `origin/main` · **conclusión trío**: reconciliación de wire (~87 DTOs) resuelta **sin ningún `regen_full`** · **próxima fase pactada: R-5** (CI Node.js 20 deprecado, bump actions + opcional `.prettierrc`; aviso CI adicional: 2 warnings React en `backtests-page.tsx:1228,1231`) · deuda operativa/futura anotada (`MandateTenure`/`MandateTradeLink` web-only; inner `data` AI NO tipado por diseño; alias wrapper cosmético; transferencias/dividendos P2.3; `BP.L` en BD F-WORKER-1) · freeze vigente: sin features · no reabrir Belief/H · no tocar gobernanza IA · **auth JWT diferida (D4)**
    >                                     └── **R-5 — CI/higiene deuda operativa (2026-08-20):** [`traspaso-r5-ci-higiene-2026-08-20.md`](./traspaso-r5-ci-higiene-2026-08-20.md) — **R-5 COMPLETADO en `main`** (4 commits directos `3b9c139` + `da8a10c` + `017b495` + `9cad4bb` docs, árbol limpio, `local main = origin/main = 9cbfcb0`, CI verde) · **cierra la deuda abierta/operativa** (alcance pactado de la re-auditoría 2026-08-20) · **hallazgo clave**: la deprecación de Node 20 en CI NO era nuestro `node-version` (ya 22 en casi todo) sino **las acciones corriendo internamente en Node 20** → bump `checkout@v4→v5`, `setup-node@v4→v5` (mantiene `cache:'pnpm'` frente al auto-cache de `packageManager`), `setup-python@v5→v6`, `pnpm/action-setup@v4→v5`, **`gitleaks-action@v2→v3`** (v2 dejaría de funcionar 2026-09-16) → **sin ninguna action en Node 20**, anotación eliminada, CI verde (Frontend/Optimize/Fase2/Gitleaks) · `optimize-lab.yml` Node `20→22` · 2 warnings `react-hooks/exhaustive-deps` en `backtests-page.tsx` suprimidos puntualmente con justificación (el autofix snapshotearía el ref y REGRESIONARÍA: refs null al montar) · **`.prettierrc` NO tocado** (obsoleto: hygiene M0/§6.2 ya convirtió a comillas dobles; añadir `singleQuote` revertiría 500+ archivos) · **hallazgo auditoría**: `.venv_backup_*/` + `.import_linter_cache/` en `.gitignore` (inflaban `format:check` ~446 files; borrado venv backup 661MB) · checklist operativo manual pendiente (secret scanning, `TRUSTED_PROXIES` prod, `BP/.L`→`BP.L`, logs dev) · **plan de refactor 2026-08-19 (R-1..R-5) COMPLETADO**
    >                                       └── **R-6 — Re-auditoría de superficie completa (2026-08-20):** [`traspaso-r6-reauditoria-superficie-2026-08-20.md`](./traspaso-r6-reauditoria-superficie-2026-08-20.md) — **R-6 COMPLETADO en `main`** (3 commits de fix `a64f4d0` F-1 + `da8aee5` F-4 + `8731827` F-2 + docs `9aa20b8`/`d0f90f1`, árbol limpio, CI verde) · **barrido transversal READ-ONLY** (web + api-python + shared/domain/analytics, 3 subagentes paralelos, priorización por riesgo dinero/verdad) tras elegir ampliar alcance de la re-auditoría a superficie completa · **corregidos los 4 Altos**: **F-1** `parseLocalizedNumber` en `apps/web/src/lib/format.ts` (parser es-ES tolerante miles `.`/decimal `,`) aplicado en `order-dialog.tsx` + `account-detail-panel.tsx` (corrige `1.500`→1.5, error ×1000) · **F-4** eliminada lógica fiscal muerta/divergente de `packages/shared/src/tax-report.ts` (262 líneas, 0 importadores; conserva DTO types) · **F-2** `TAX_PRESETS.US.dividendWithholdingPct` 15→30 (alineado al domain 30) · **F-3** (sync workspace solo-aditivo) DOCUMENTADO como deuda con trade-off intencional, NO tocado (fix de propagación de borrados = cambio del modelo sync = collides con freeze) · deuda nueva inventariada (Alto×4/Medio×13/Bajo×5) en el traspaso · batería verde (web 716 tests, shared 17), prettier limpio · **no-deuda confirmada**: domain purity 0 violaciones, fidelidad tipos shared↔schemas sin gaps nuevos post R-3/R-4 (contrato sigue VERDE)
    >                                         └── **R-7 — Lógica de dinero real en `packages/py/{application,infrastructure}` (2026-08-20):** [`traspaso-r7-dinero-application-infrastructure-2026-08-20.md`](./traspaso-r7-dinero-application-infrastructure-2026-08-20.md) — **R-7 COMPLETADO en `main`** (deuda de R-7 completa: **Fase 1 PUSHEADA** `c957df1` + **Fase 2 PUSHEADA** `8c081ea` + **Fase 3 PUSHEADA** (L-M3/M-5) + **M-3 PUSHEADA** (T-M3) + **M-6 PUSHEADA** (T-M6) + **M-4/T-M5 PUSHEADA** (T-M5) + **M-7 PUSHEADA** (L-M5) + **B-1 PUSHEADA** (T-M7) + **B-3 PUSHEADA** (L-M4) + **B-4 PUSHEADA** (L-M6) + **B-5 PUSHEADA** (T-M9/T-M10) + **B-2 PUSHEADA** (T-M8 `7b3dd91`); solo queda M-4/T-M4 diferido), árbol limpio) · mayor agujero de cobertura heredado de R-6 (use-cases/repos/invariantes contables quedaron fuera del surface web+api+shared) · **auditoría read-only** (3 subagentes paralelos + verificación personal del coordinador) · **F1/FFIN todos INTACTOS** (with_for_update, deduct_cash en lock, idempotencia trade + constraint, F-FIN-1 fail-closed, transfer_cash atómico) · **deuda NUEVA: Alto×3 / Medio×7 / Bajo×5** · **Alto×3 + Medio×7 + Baja B-1 + Baja B-3 + Baja B-4 + Baja B-5 de R-7 COMPLETADOS** · **F1 `c957df1`**: fix doble cargo custodia en GET (mutex `claim_custody_charge` por cuenta|periodo) + release de claim AUTO en fill fallido + `_redis_client -> Any|None` · **F2 `8c081ea`**: idempotencia Deposit/Withdraw (`idempotency_key` + guard `find_cash_movement_by_reference`) · **F3 (L-M3/M-5, opción A)**: migración `004_ledger_reference_unique` — `UNIQUE` parcial `(account_id, reference_type, reference_id, type)` (por-cuenta + `type` para no romper trade+fee; preflight que FALLA si hay duplicados) · **M-1 (T-M1, Opción B)**: fallback mark-to-cost al último precio transaccional en `get_summary` (helper `_latest_transaction_prices`, `DISTINCT ON`, sin N+1) para posiciones sin close D1 — deja de fabricar pérdida fantasma y conserva `total_equity` · **M-2 (T-M2)**: invariante de reconciliación cash↔ledger — nuevo `sum_cash_amounts(account_id)` (Σ `amount` del ledger del account; scoped por `account_id`; semántica de signo seed deposit+/withdrawal−/buy-sell∓notional/fee−; Σ==cash) + tests-postcondición `test_m2_ledger_cash_reconciliation.py` (6, Postgres real) + `xfail` documental escotilla B-3 (`add_cash`/`deduct_cash`/`transfer_cash` mutan cash sin ledger; M-2 acotado, B-3 aparte) · **M-3 (T-M3, puente iv)**: divergencia cost-basis — nuevo `open_positions_with_fee_basis(transactions, method, prices, live)` (deriva posiciones abiertas con fee capitalizada con la MISMA máquina FIFO/avg que realized; refactor `_fifo_realized`/`_average_realized` → `(lines,residual)` sin duplicar) + `GetTaxReport` sincroniza su cara unrealized (antes `pos.quantity*pos.avg_cost` fee-excluida contradecía a su propia cara realized); storage/`avg_cost` y FE de posiciones NO cambian; gap report-vs-almacén se **loguea** (`logger.warning`, corrección coordinador: el subagente usó una global mutable muerta) · **M-6 (T-M6, margen real)**: `_account_summary_from_portfolio` computa `margin_used=Σ(mv con precio)/leverage` (guard `>0`, fail-closed→0.0), `free_margin=total_equity−margin_used`, `margin_level_pct=equity/margin_used*100` (None si 0); formula decidida por usuario (`Σ market_value/leverage`); DTO/endpoints intactos (sin contract:gen); posiciones con `market_value=None` no cuentan (fidelidad M-1); FE `dashboard-page.tsx:163` ve `free_margin` real (valor esperado, NO tocar FE) · **M-4/T-M5 (Opción B acotada, `6a1759c`)**: `total_fees_for_account` excluye `reference_type="custody"` → `GetTaxReport` deja de sobrescribir `fees_paid_total` con custodia (fix del override accounts.py:750-751 en el origen; trade-fees `"transaction"` intactos; `map_ledger_fees_to_transactions` sin impacto); tests `test_m4_total_fees_excludes_custody.py` (2, Postgres real) · batería hasta M-4: ruff 0 (config CI raíz) · mypy infra/domain 0 (`accounts.py` sin errores nuevos, 6 pre-existentes) · pytest infra Postgres real 74+1xfail · domain 21 · application 241 · api-python test_accounts 5 · **M-7 (L-M5, `f598e2d`)**: re-cobro de custodia (dedup time-only del mutex) **ya cubierto por L-M3/M-5** — UNIQUE `(account_id, reference_type, reference_id, type)` rechaza con IntegrityError una 2ª fila del mismo account+periodo; `deduct_cash`+`append_custody_fee` comparten AsyncSession y el `rollback` de `get_db_session` revierte el cash → no queda cash descontado sin fila; **Alto×3 + Medio×7 + Baja B-1 + Baja B-3 + Baja B-4 + Baja B-5 de R-7 COMPLETADOS** · **B-1 (T-M7, high-water-mark, `4f43aeb`)**: max drawdown naive (vs depósito inicial, estateless) → **HWM** en `EquityMarkBook.update` (`account_drawdown.py`): trackea `peakEquity` (running equity peak, persistido en `equityMarks`) + `maxDrawdownPct` (running-max del drawdown desde el pico), reseteado a 0 solo cuando `equity>=peak` (nuevo máximo) → corrige el **under-bloqueo tras recuperación** del gate `HardMaxDrawdown` (tras 100k→90k→98k reporta 10.0, no 2.0); gating preservado (sin `initial_deposit`/≤0 → `max_pct=None`, gate SKIPPED); shape DTO intacto (consumidores wire-compatibles); batería: ruff 0 · mypy Success · application 245 (4 tests nuevos) · **SIGUIENTES por decisión (deuda Baja): B-5 (FIFO/PnL whole-account) · B-2 (T-M8, `total_unrealized_gain` silencia posiciones sin precio) · M-4/T-M4 (custodia fuera de GET → job dedicado, DIFERIDO por freeze) · **B-3 (L-M4) CERRADA (`7cffaa7`)**: `transfer_cash` dead code (0 callers) y sin ledger → ELIMINADO (decisión usuario "eliminar"); un futuro caller ya no puede existir → hazard dinero/verdad cerrado de raíz; `add_cash`/`deduct_cash` se conservan (trazados); xfail M-2 reconvertido a escotilla residual `add_cash`/`deduct_cash** · **B-4 (L-M6) CERRADA (`3712e63`)**: la 3ª llamada `append_fee` NO era "no-atómica" en sentido DB (las 3 llamadas de `ExecuteTrade.execute` comparten la MISMA `AsyncSession` y se commitean juntas al final del request; rollback ante excepción). El haz real de dinero era el **doble trade+fee sin `idempotency_key`** en los 2 caminos internos (`execution_router.py:611` AUTO paper_auto, `confirm_recommendation.py:78` confirm intent) — el guard `find_transaction_by_idempotency` nunca se activaba y un repost/retry escribía un `new_id()` fresco → trade+fee 2×. **Opción B (decisión usuario)**: reenvían `idempotency_key` estable — AUTO usa `make_auto_execute_idempotency_key` (día×política×kind, alineando el dedup Redis/TTL con el guard DB persistente `transactions` UNIQUE), confirm usa `decision_id or session_id`; `idem_key`/`claimed` movidos a nivel de función (evita un `NameError` latente del `except ValueError` con el cognitive gate desactivado); sin falsos positivos (la clave identifica la ejecución lógica, no el payload); API `POST /portfolio/trade` ya soporta `idempotency_key` (sin `contract:gen`); sin migración/repos/API/FE. Tests `test_execute_trade_idempotency.py` (4). Batería: ruff 0 · mypy confirm_recommendation Success · app 249 (245+4) · api-python accounts 5. **B-5 (T-M9/T-M10) CERRADA (`76abe0f`):** guard `quantity==0` en FIFO buy (`tax_report.py:100` pasó de `(total+fee)/quantity` sin guard a apendizar lote solo si `quantity>1e-9`; ZeroDivisionError latente, write-path `execute_trade` ya rechaza quantity≤0) + `logger.warning` en los skips silenciosos de `fetch_core_r_pnl_extra_rows` (fail-open PnL CORE-R observable sin cambiar la semántica instrumento→cuenta, intencional y espejada en `core-r-scheduler-tick.ts:40`). Tests `test_fifo_buy_quantity_zero_no_divide_by_zero_ni_lote`. Batería: ruff 0 · mypy domain Success · domain FIFO 13 (nuevo incl.) · app 249. SIGUIENTE: **B-2 (T-M8)** (unrealized silencia sin precio), luego audit. Ver §4m del traspaso R-7. **B-2 (T-M8) CERRADA (`7b3dd91`, opción A fail-closed):** `build_tax_report` (`tax_report.py:368-372`) `total_unrealized_gain` pasaba a 0 las posiciones sin precio (`sum(... or 0.0)`); ahora si alguna posición abierta tiene `unrealized_gain None` → el total es `None` (FE `tax-report-page.tsx:332-345` lo muestra "—", no un 0 infra-representado). Consumidores solo API `account_mappers.py:219` + FE tax-report (jobs CORE-R NO consumen el gain). Coherente con M-1; shape `float|None` intacto (sin contract:gen). Tests `test_tax_report_open_positions.py` (+3). Batería: ruff 0 · mypy domain/src Success · domain 25 (3 nuevos) · app 249. **R-7 deuda COMPLETA** (solo M-4/T-M4 diferido). **PACK de estado global consolidado R-1→R-7 (listo para auditoría externa): [`audit-pack-estado-global-2026-08-20.md`](./audit-pack-estado-global-2026-08-20.md)**. SIGUIENTE: GUARDAR + auditorías externas del estado global. Ver §4n del traspaso R-7.
    >             └── **CIERRE de ola — refactorización/integridad (2026-08-12):** [traspaso-cierre-refactorizacion-2026-08-12.md](./traspaso-cierre-refactorizacion-2026-08-12.md) — doc de **CIERRE de hilo** consolidando la ola F1→F5a-fidelidad + fixes operativos/CI (#38/#39/#40/#41) MERGEADOS en `stage/f1-integridad-financiera-2026-08-11` a `25564a2` · tablas de fases terminadas + **deuda para «terminar toda la refactorización»** priorizada (F5a §6 fidelidad restante / openapi-fetch · P2.6 duplicación TS↔Py · P1.9 API thin · P1.3 auth full D4 · mypy por fases) · nota: warning auto-sync de ticker `BP/.L` que Yahoo resuelve 404 (datos externos, subagente evaluando → **F-WORKER-1 CERRADO**) · **texto de paso listo en §6 del doc**
    >         └── **F3a — traspaso de hilo:** [traspaso-f3a-procesos-db-2026-08-11.md](./traspaso-f3a-procesos-db-2026-08-11.md) — documento de trabajo F3a (Arquitectura de procesos y DB) **COMPLETADO 2026-08-11** · rama `stage/f3a-procesos-db-2026-08-11` desde `stage/f1-*` (tras merge PR #32) · **P0.4/D3** `scheduler_worker.py` (proceso dedicado; workers fuera del `lifespan`) + `main.py` limpio + `pyproject` script + `run-dev.mjs` + tests · **P1.2** `run_account_data_migration` idempotente una-vez (lifespan + scheduler); retirada `ensure_migrated` del path de petición · **P0.5/D2** `003_prisma_schema_baseline` (takeover: schema completo 53 tablas + 8 enums, no-op en BD Prisma / fresh-build sin Prisma) + guards idempotencia en `002` + generador `dump_alembic_prisma_baseline.py --check` (reproducible) · **fix borrado de cuentas FK** en `delete_simulated_account` · batería: ruff 0 en fase (25→13→7 heredados → F4) · mypy✓ nuevos · pytest infra **48✓** + api **30✓** + domain/market/application/analytics/ai **676✓** · `--check`✓ · deuda: P1.6 mypy gate, ruff gates infra restantes (7), P1.9 API thin → F4 · ver §9
    >         └── **F4 — traspaso de hilo:** [traspaso-f4-arquitectura-python-2026-08-11.md](./traspaso-f4-arquitectura-python-2026-08-11.md) — documento de trabajo F4 (Arquitectura Python) **COMPLETADO 2026-08-12** · rama `stage/f4-arquitectura-python-2026-08-11` desde `stage/f1-*` (tras merge PR #33) · **P0.6** ciclo `analytics↔market` ROTO: `MarketEvent`/`MarketEventCalendar`/`EventBlackoutContext`/`build_market_event`/`event_decay_weight` → `bolsa_domain/entities/market_event.py` y `prefer_summary_excerpt` → `bolsa_domain/value_objects/excerpt.py`; `analytics` y `market` quedan como pares solo con `domain` (0 imports cruzados src↔src; re-export de compatibilidad en `bolsa_analytics.cognitive`) + 2 fixes mypy preexistentes en `news_snapshot.py` · **7 ruff gates CERRADOS** (I001 ×6 + B007): `ruff check packages/py apps/api-python` → **0 errores** · **P1.6** mypy gate POR FASES (files_only): paso CI bloqueante `Mypy — gate scoped F4` sobre 13 ficheros (exit 0); paso global sigue `continue-on-error` (~451 preexistentes) · batería: ruff 0 · mypy gate 13 files ✓ · pytest market+analytics **431✓** + application **222✓** + infra+domain **57✓** + api-python **30✓** (bolsa_v1) + ai+analytics **337✓** · deuda: P1.9 API thin (hilo propio), mypy resto del árbol por fases, D4 auth → F5b · ver §9
    >         └── **F5b — traspaso de hilo:** [traspaso-f5b-backend-seguridad-2026-08-12.md](./traspaso-f5b-backend-seguridad-2026-08-12.md) — documento de trabajo F5b (Backend/Seguridad) **COMPLETADO 2026-08-12** · rama `stage/f5b-backend-seguridad-2026-08-12` desde `stage/f1-*` (tras merge PR #34/F4) · **P1.8** rate-limit **distribuido** entre workers: `rate_limit.py` con `RedisStore` (INCR+PEXPIRE en Lua) + `MemoryStore` fallback + circuit-breaker; prefijos `SENSITIVE_PREFIXES` deterministas (específico→genérico) y `/api/instruments/{id}/fundamentals` por segmento · **P2.3** `upsert_bars` en **bulk INSERT...ON CONFLICT DO UPDATE** (una sentencia multi-fila; antes loop N+1), validado contra BD real en rollback · **P2.5** `/health` sin filtrar internos: Redis sin `url_host`/`{exc}`, auth sin `APP_PASSWORD`/`OR-S1`, xtb sin URL real, smtp sin `port`/`hasUser`/`missing`, worker_arq sin key interna, DB mensaje genérico; + `test_health_redacts_internal_details` · **P2.7** `Deposit/Withdraw.amount` → `gt=0`+`allow_inf_nan=False` (TradeRequestDto ya estricto F1/M4) · **CI (defecto preexistente)** `quality` Pytest sin Postgres corría `test_health/auth/ai_authoring` (lifespan→DB → `Connection refused`): se excluyen (ig. `test_lists`/`test_workspaces`) → **quality UNBLOCK**; mypy gate → `F4+F5b` (16 files, exit 0) · batería: ruff 0 · mypy gate 16 ✓ · pytest offline **451✓** + api 27✓ (bolsa_v1) + application 222✓ + infra+domain 57✓ + ai+analytics+digest **340✓** · deuda: P1.9 API thin (hilo propio), P1.3 auth full D4 diferido, P2.1 god-components frontend, F5a §6 fidelidad DTOs/openapi-fetch, mypy preexistente por fases · ver §9
    >         └── **F5c — traspaso de hilo:** [traspaso-f5c-frontend-cleanup-2026-08-12.md](./traspaso-f5c-frontend-cleanup-2026-08-12.md) — documento de trabajo F5c (Frontend clean-up de la parte frontend de F5) **COMPLETADO 2026-08-12** · rama `stage/f5c-frontend-cleanup-2026-08-12` desde `stage/f1-*` (tras merge PR #35/F5b) · **P2.8** formato: nuevo `apps/web/src/lib/format.ts` (punto único local "es-ES", helpers preservan semántica exacta) + `format.test.ts` (7) · migrados **29 call sites/helpers locales** `toLocaleString("es-ES")` (dispersos en ~30 ficheros) · **P2.8 timers**: 11 llamadas bare → `window.setTimeout/setInterval` + handles `number` (antes `ReturnType<typeof setTimeout>`) · **P2.6** `packages/shared`: script `test` (vitest) + `vitest.config.ts` (alias a src) + `policy-gate.test.ts` (8, paridad TS↔Py RFC-008) + pasos CI `Frontend` "Typecheck shared"/"Test shared" (el único test era huérfano) · batería: shared test **10✓** typecheck✓ lint✓ + web typecheck✓ lint 0 test **714✓** (141 f) · commits `e72650d`/`d908ac2`/`d9ae632` · **P2.1 god-components DIFERIDO a hilo propio** (decisión usuario; mapa listo) · residue: `as unknown as` (8) bridges intencionales→fidelidad F5a §6; duplicación TS↔Py restante (ai-indicator-series/execution-policies/position-policies) exige acuerdo de diseño · ver §8
    >             └── **P2.1 — plan/cierre:** [plan-p2.1-god-components-2026-08-12.md](./plan-p2.1-god-components-2026-08-12.md) — plan + **CIERRE P2.1 (god-components) COMPLETADO 2026-08-12** · rama `stage/p2.1-god-components-2026-08-12` desde base `bce6066` (tip F5c) · **workspace-store** 3982→89 líneas: `workspace-store-core.ts` (helpers+tipo+interfaz) + 7 slices `workspace-slice-{layout,charts,indicators,drawings,toolbar,list,account}.ts` compuestos en **UN solo** `create<WorkspaceState>()(persist(...))` (persist `bolsa-workspace-meta`, partialize/merge byte-idénticos; **no** stores paralelos) · **backtests-page** 5129→4509 líneas: `runListBatch/runCoachBattery/startOptimizeFromExplore/settleFullCycle` → `lib/backtest-orchestration.ts` (729 líneas) `createBacktestOrchestration(ctx)` contexto tipado, re-instanciación en render · **cero cambios de lógica** (D5) · batería: web typecheck✓ lint 0 errores test **714✓** (141 f) build✓ + shared **10✓** · commits `18a8878`/`0fcdc8b` · residue: `as unknown as` (8)→F5a §6; duplicación TS↔Py restante (P2.6)→acuerdo de diseño; deuda previa P1.9/P1.3/F5a §6/mypy · ver §7-§8
    >             └── **P2.8 — plan/cierre:** [plan-p2.8-as-unknown-as-2026-08-12.md](./plan-p2.8-as-unknown-as-2026-08-12.md) — plan + **CIERRE P2.8 (sopa «as unknown as») COMPLETADO 2026-08-12** · rama `stage/p2.8-as-unknown-as-2026-08-12` desde base `30a0804` (tip P2.1) · **7 bridges de serialización TS↔Py eliminados** vía DTOs concretos en `packages/shared` (A1: fidelidad FE 1:1 con wire, sin tocar BE/OpenAPI; decisión usuario): `core-r-api.ts` (CoreRQueueItemDto/CoreRReportDto/CoreRReportsMapDto/CoreRSchedulerPrefsDto) · `supervised-f3-api.ts` (SupervisedF3QueueItemDto) · nuevos `dia-d-evidence-api.ts` + `coach-facts-api.ts` · **`api.ts`** tipado con DTOs concretos (syncAccountCoreR/syncAccountSupervisedF3/persistDiaDSessionEvidence/analyzeBacktestCoach, retirado `Record`) · **2 casts residuales confinados** en helpers nombrados (`toF3ItemDto`/`toCoachFactsRecord`, blobs abiertos) · **`contract-check.ts`** + sentinelas G4/G5 · `contract:check` **ROJO por drift F5b heredado** (`exclusiveMinimum` Deposit/Withdraw, `gt=0`; decisión `cd_exclude`) · batería: shared typecheck✓ build✓ test **10✓** + web typecheck✓ lint **0 errores** test **714✓** (141 f) build✓ · commits (3) · residue: drift contrato F5b (regen+commit), P2.6 duplicación TS↔Py / consolidación tipos web-only, F5a §6 openapi-fetch · ver §8
    >                 └── **F5b-drift — traspaso de hilo:** [traspaso-f5b-drift-contrato-2026-08-12.md](./traspaso-f5b-drift-contrato-2026-08-12.md) — **COMPLETADO + MERGEADO 2026-08-12** · rama `stage/f5b-drift-contrato-2026-08-12` desde base `01cb72a` (tip P2.8) · **regen del `openapi.json`** contra FastAPI vivo (`contract:gen`, `PYTHONIOENCODING=utf-8`) para desbloquear `contract:check` (drift F5b heredado: `Deposit/Withdraw.amount → gt=0` faltaba `exclusiveMinimum: 0.0`) · diff mínimo = **2 inserciones** (`exclusiveMinimum: 0.0` en Deposit/Withdraw) · `schema.d.ts` **sin cambio** (cero impacto tipos FE) · batería: contract:check **VERDE** (antes ROJO) · web typecheck✓ lint 0 errores test **714✓** (141 f) ✓ · commits `9c3f643`+`aabc3f1` · **PR #37 MERGED** (fast-forward `01cb72a → aabc3f1`, mergeCommit `aabc3f1`) · base viva `stage/f1-*` ahora `aabc3f1` · tree limpio · deuda sin resolver: P2.6 duplicación TS↔Py/consolidación tipos web-only, F5a §6 fidelidad+openapi-fetch, P1.9 API thin, P1.3 auth full (D4), mypy por fases · ver §7
    >                 └── **P1.6 — plan/cierre:** [plan-p1.6-mypy-fases-2026-08-12.md](./plan-p1.6-mypy-fases-2026-08-12.md) + **CIERRE P1.6 (mypy por fases) COMPLETADO 2026-08-12** · objetivo: gate mypy **BLOQUEANTE** full-tree (antes `continue-on-error: true`) · ~448 preexistentes → **0 errores en 243 files** (domain/market/infrastructure/apps-api-python) · estrategia: 4 worktrees aislados (`wt-mypy-{domain,market,infrastructure,api-python}`) + 4 subagentes (evita colisión checkout compartido) · pattern fixes: `type-arg`/`arg-type` (guards `X | None`)/`no-untyped-def`/`Mapped` SQLAlchemy 2.0 · **PRs #45–#48 MERGED** (#48 → base `441c590`) · gate CI final `python-ci.yml` bloqueante full-tree (PR **#49**) + retirada de gate scoped F4+F5b · batería: ruff **0** · mypy **0/243** · pytest **451✓** · cierre ola: #42 docs + #43 fix auto-sync + #49 gate (#43 rebase p/ desbloquear quality I001) · base final `6a89f6c` · **0 PRs abiertos** · deuda: P1.9 API thin, P1.3 auth D4, F5a §6 openapi-fetch/DTOs, P2.6 web types · ver §9/§7
20. **Auditoría externa post-tag v1.8.1-beta (2026-08-25):** [triage](./audit-ext-v181-triage-2026-08-25.md) · [roadmap v1.9](./roadmap-v19-operational-core-2026-08-25.md) · **F1–F4 + ExitPermission + INFRA CERRADOS** (modelo). Tag `v1.9-beta`.
21. **Auditoría discontinuidad operativa (2026-08-25):** [triage](./audit-ext-v19-ops-discontinuity-triage-2026-08-25.md) · [roadmap v1.10](./roadmap-v110-operational-authority-2026-08-25.md) · [ADR-033](../adr/033-operational-authority-position-persistence.md) · **H1→P4 CERRADOS**. Tag `v1.10-beta` → `047ddb6`.
22. **Operational Integrity v1.11 (2026-08-26):** [roadmap](./roadmap-v111-operational-integrity-2026-08-26.md) · [ADR-034](../adr/034-operational-integrity-continuity.md) · [pack](./audit-pack-estado-global-2026-08-26-v111.md) · [relevo tag](./traspaso-relevo-tag-v1-11-beta-2026-08-26.md) · **OI-1…OE-1 CERRADOS**. Tag `v1.11-beta` → `76d0f951`.
23. **Operational Reliability v1.12 (2026-08-26):** [triage](./audit-ext-v111-operational-reliability-triage-2026-08-26.md) · [roadmap](./roadmap-v112-operational-reliability-2026-08-26.md) · [ADR-035](../adr/035-operational-reliability.md) · [pack](./audit-pack-estado-global-2026-08-26-v112.md) · [relevo tag](./traspaso-relevo-tag-v1-12-beta-2026-08-26.md) · **D0 + OR-1/3/4/5/6 CERRADOS · OR-2 PARTIAL**. Tag `v1.12-beta` → `369b5d1`. Partida `v1.11-beta` → `76d0f951`. Spine **433**.
24. **Durable Execution v1.13 (2026-08-26):** [triage](./audit-ext-v112-durable-execution-triage-2026-08-26.md) · [roadmap](./roadmap-v113-durable-execution-2026-08-26.md) · [pack](./audit-pack-estado-global-2026-08-26-v113.md) · [relevo tag](./traspaso-relevo-tag-v1-13-beta-2026-08-26.md) · [plan DEX-1](./plan-dex1-pg-submit-intents-2026-08-26.md) · [plan DEX-2](./plan-dex2-crash-restart-cross-pid-2026-08-26.md) · [plan DEX-3](./plan-dex3-operational-incident-2026-08-26.md) · [plan DEX-4](./plan-dex4-confirm-orchestrator-2026-08-26.md) · [plan DEX-5](./plan-dex5-operational-invariants-2026-08-26.md) · **D0 + DEX-1…DEX-5 CERRADOS · pack v113 stampado**. Tag `v1.13-beta` pendiente owner. ADR-035 §8. Spine **483**. Partida `v1.12-beta` → `369b5d1`.
25. **Paper Desk Event Continuity v1.48 (2026-09-01):** [spec-v148](./spec-v148-paper-desk-event-continuity-2026-09-01.md) · [relevo](./traspaso-relevo-v1-48-paper-desk-event-continuity-2026-09-01.md) · [relevo tag](./traspaso-relevo-tag-v1-48-beta-2026-09-01.md) · [arranque auditor](./arranque-auditor-v1-48-beta-2026-09-01.md) · [ADR-043](../adr/043-position-automation.md) · **events[]/eventId · ExecutionSnapshot · Golden Session + CAOS**. Tag `v1.48-beta` → `d5852e8d` (CI GREEN). EntryTick stub.
26. **Paper Desk Entry AUTO v1.49 (2026-09-01):** [spec-v149](./spec-v149-paper-desk-entry-auto-2026-09-01.md) · [relevo](./traspaso-relevo-v1-49-paper-desk-entry-auto-2026-09-01.md) · [relevo tag](./traspaso-relevo-tag-v1-49-beta-2026-09-01.md) · [respuesta auditor](./respuesta-auditor-v149-entry-auto-2026-09-01.md) · **EstudioPaperDeskEntry · GP-DESK-03 cableado**. Tag `v1.49-beta` → `c8975c9d` (CI GREEN). Auditoría externa: PASS integración; AUTO no cerrado.
27. **Entry Decision Integrity v1.50 (2026-09-01):** [spec-v150](./spec-v150-entry-decision-integrity-2026-09-01.md) · [plan](./plan-v150-entry-decision-integrity-2026-09-01.md) · [relevo](./traspaso-relevo-v1-50-entry-decision-integrity-2026-09-01.md) · [relevo tag](./traspaso-relevo-tag-v1-50-beta-2026-09-01.md) · [arranque auditor](./arranque-auditor-v1-50-beta-2026-09-01.md) · **CandidateSnapshot · GP-DESK-04/05/06**. Tag `v1.50-beta` → `96623755` (CI GREEN · auditoría externa PASS). **No** LIVE.
28. **Entry → Fill → Position v1.51 (2026-09-01):** [spec-v151](./spec-v151-entry-fill-position-2026-09-01.md) · [close-out auditable](./spec-v151-operativo-auditable-2026-09-01.md) · [plan](./plan-v151-entry-fill-position-2026-09-01.md) · [relevo](./traspaso-relevo-v1-51-entry-fill-position-2026-09-01.md) · [relevo tag](./traspaso-relevo-tag-v1-51-beta-2026-09-01.md) · [respuesta auditor](./respuesta-auditor-v151-entry-fill-position-2026-09-01.md) · **PersistPositionFromFill · GP-DESK-07/08/05b · identidades distintas**. Tag `v1.51-beta` → `5eb8e6de` (CI GREEN · auditoría PASS 9,1). **No** LIVE.
29. **Position Lifecycle v1.52 (2026-09-01):** [spec-v152](./spec-v152-position-lifecycle-2026-09-01.md) · [plan](./plan-v152-position-lifecycle-2026-09-01.md) · [relevo](./traspaso-relevo-v1-52-position-lifecycle-2026-09-01.md) · [relevo tag](./traspaso-relevo-tag-v1-52-beta-2026-09-01.md) · **Lab execute DENY · TargetLeg · revision decisionId/policyId · GP-EXIT/CRASH**. Tag `v1.52-beta` → `9725e9e7` (CI GREEN). **No** LIVE.
30. **Golden Session v1.53 (2026-09-01):** [spec-v153](./spec-v153-golden-session-2026-09-01.md) · [plan](./plan-v153-golden-session-2026-09-01.md) · [relevo](./traspaso-relevo-v1-53-golden-session-2026-09-01.md) · [relevo tag](./traspaso-relevo-tag-v1-53-beta-2026-09-01.md) · **Estudio 09:00 → protect → T1 → TRAIL×2 → exit → PaperDailyReport**. Tag `v1.53-beta` → `9725e9e7` (CI GREEN). **No** LIVE.
31. **Operating Desk v1.54 (2026-09-01):** [spec-v154](./spec-v154-operating-desk-2026-09-01.md) · [plan](./plan-v154-operating-desk-2026-09-01.md) · [relevo](./traspaso-relevo-v1-54-operating-desk-2026-09-01.md) · [relevo tag](./traspaso-relevo-tag-v1-54-beta-2026-09-01.md) · [arranque auditor](./arranque-auditor-v1-54-beta-2026-09-01.md) · **autoDesk → Daily Desk inbox · GP-DESK-UI-01..09**. Tag `v1.54-beta` → `e057a8cc` (CI GREEN). Previo **`v1.53-beta` → `9725e9e7`**. **No** LIVE.
32. **Operational Hardening v1.55 (2026-09-01):** [spec-v155](./spec-v155-operational-hardening-2026-09-01.md) · [plan](./plan-v155-operational-hardening-2026-09-01.md) · [relevo](./traspaso-relevo-v1-55-operational-hardening-2026-09-01.md) · [relevo tag](./traspaso-relevo-tag-v1-55-beta-2026-09-01.md) · [arranque auditor](./arranque-auditor-v1-55-beta-2026-09-01.md) · [respuesta auditor](./respuesta-auditor-v155-operational-hardening-2026-09-01.md) · **CERRADO**. Tag `v1.55-beta` → `c23091d9` (CI GREEN · audit PASS 9,3). Previo **`v1.54-beta` → `e057a8cc`**. **No** LIVE.
33. **Cierre V1.55 → siguiente (2026-09-01):** [cierre-apertura](./traspaso-relevo-cierre-v155-apertura-siguiente-2026-09-01.md) · arco Paper Desk V1.48→V1.55 **cerrado** · fork §5. **V1.56** y **V1.57** tienen spec propia (ítems 34–35).
34. **Hardening Residuals v1.56 (2026-09-01):** [spec-v156](./spec-v156-hardening-residuals-2026-09-01.md) · [plan](./plan-v156-hardening-residuals-2026-09-01.md) · [relevo tag](./traspaso-relevo-tag-v1-56-beta-2026-09-01.md) · **CERRADO**. Tag `v1.56-beta`. GP-SESSION-07e · GP-SESSION-10r · GP-E2E-01..02. **No** LIVE.
35. **Operational Truth v1.57 (2026-09-01):** [spec-v157](./spec-v157-operational-truth-2026-09-01.md) · [plan](./plan-v157-operational-truth-2026-09-01.md) · [relevo](./traspaso-relevo-v1-57-operational-truth-2026-09-01.md) · [arranque auditor](./arranque-auditor-v1-57-operational-truth-2026-09-01.md) · **CERRADO** tag `v1.57-beta`. GP-V157-01..03 · INV-01..10. Partida `v1.56-beta` → `5c598a62`. **No** LIVE.
36. **Adversarial Execution v1.58 (2026-09-01):** [spec-v158](./spec-v158-adversarial-execution-2026-09-01.md) · [plan](./plan-v158-adversarial-execution-2026-09-01.md) · [relevo](./traspaso-relevo-v1-58-adversarial-execution-2026-09-01.md) · [arranque auditor](./arranque-auditor-v1-58-adversarial-execution-2026-09-01.md) · **CERRADO** tag `v1.58-beta`. GP-GOLDEN-DAY-ADV-01 · GP-V158-STOP-CLOSED · P0b network skip. **No** LIVE.
37. **E2E Integrated v1.59 (2026-09-02):** [spec-v159](./spec-v159-e2e-integrated-2026-09-02.md) · [plan](./plan-v159-e2e-integrated-2026-09-02.md) · [relevo](./traspaso-relevo-v1-59-e2e-integrated-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-59-beta-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-59-e2e-integrated-2026-09-02.md) · **CERRADO** tag `v1.59-beta` → `b5c5c6ab`. GP-V159-01..07 · pytest+ASGI+PG · fix `opening_gate_seed`. Partida `v1.58-beta` → `4c42f1fc`. **No** LIVE.
38. **UX Mercado v1.60 (2026-09-02):** [spec-v160](./spec-v160-ux-mercado-2026-09-02.md) · [plan](./plan-v160-ux-mercado-2026-09-02.md) · [relevo](./traspaso-relevo-v1-60-ux-mercado-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-60-beta-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-60-ux-mercado-2026-09-02.md) · **CERRADO** tag `v1.60-beta` → `7ac8ad9b`. GP-V160-01..04 tarjeta estrella POV en DECISIÓN. Partida `v1.59-beta` → `b5c5c6ab`. **No** LIVE.
39. **LISTA→GRÁFICO→ACCIÓN v1.70 (2026-09-02):** [spec-v170](./spec-v170-lista-grafico-accion-2026-09-02.md) · [plan](./plan-v170-lista-grafico-accion-2026-09-02.md) · [relevo](./traspaso-relevo-v1-70-lista-grafico-accion-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-70-lista-grafico-accion-2026-09-02.md) · **CERRADO con reservas de certificación** `960383d2`. Playwright integrado **opt-in** — **no** CI GREEN. **No** LIVE.
40. **Identity & Certification v1.71 (2026-09-02):** [spec-v171](./spec-v171-identity-certification-2026-09-02.md) · [plan](./plan-v171-identity-certification-2026-09-02.md) · [relevo](./traspaso-relevo-v1-71-identity-certification-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-71-identity-certification-2026-09-02.md) · **CERRADO** `b70849bd` (código + tests locales). GP-V171-01..06. Partida V1.70 `960383d2`. **No** LIVE · **no** stamp CI GREEN.
41. **Decision Explainability TOP v1.72 (2026-09-02):** [spec-v172](./spec-v172-decision-explainability-top-2026-09-02.md) · [plan](./plan-v172-decision-explainability-top-2026-09-02.md) · [relevo](./traspaso-relevo-v1-72-decision-explainability-top-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-72-decision-explainability-top-2026-09-02.md) · **CERRADO** (código + tests locales). GP-V172-01..05. Partida V1.71 `b70849bd`. **No** LIVE · **no** stamp CI GREEN.
42. **Multi-instrument integrity v1.73 (2026-09-02):** [spec-v173](./spec-v173-multi-instrument-integrity-2026-09-02.md) · [plan](./plan-v173-multi-instrument-integrity-2026-09-02.md) · [relevo](./traspaso-relevo-v1-73-multi-instrument-integrity-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-73-multi-instrument-integrity-2026-09-02.md) · **CERRADO** (E2E mock locales). GP-V173-01..04. Partida V1.72. **No** LIVE · **no** stamp CI GREEN.
43. **Paper Autonomous Day v1.74 (2026-09-02):** [spec-v174](./spec-v174-paper-autonomous-day-2026-09-02.md) · [plan](./plan-v174-paper-autonomous-day-2026-09-02.md) · [relevo](./traspaso-relevo-v1-74-paper-autonomous-day-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-74-paper-autonomous-day-2026-09-02.md) · **CERRADO** (E2E mock locales). GP-V174-01..08. Partida V1.73. **No** LIVE · **no** dryRun=false browser · **no** stamp CI GREEN.
44. **Chaos & stale → no-execute v1.75 (2026-09-02):** [spec-v175](./spec-v175-chaos-stale-no-execute-2026-09-02.md) · [plan](./plan-v175-chaos-stale-no-execute-2026-09-02.md) · [relevo](./traspaso-relevo-v1-75-chaos-stale-no-execute-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-75-chaos-stale-no-execute-2026-09-02.md) · **CERRADO** `b5b114ff` (E2E mock + pytest locales). GP-V175-01..07. Partida V1.74 `67f922bf`. **No** LIVE · **no** dryRun=false browser · **no** stamp CI GREEN.
45. **Certification Hardening v1.76 (2026-09-02):** [spec-v176](./spec-v176-certification-hardening-2026-09-02.md) · [plan](./plan-v176-certification-hardening-2026-09-02.md) · [relevo](./traspaso-relevo-v1-76-certification-hardening-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-76-certification-hardening-2026-09-02.md) · **CERRADO** `bf6ba462` (E2E mock locales). GP-V175-01/03/04 endurecidos · GP-V176-01 causalidad. Partida V1.75 `b5b114ff`. **No** LIVE · **no** dryRun=false browser · **no** stamp CI GREEN.
46. **Session Reliability v1.77 (2026-09-02):** [spec-v177](./spec-v177-session-reliability-2026-09-02.md) · [plan](./plan-v177-session-reliability-2026-09-02.md) · [relevo](./traspaso-relevo-v1-77-session-reliability-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-77-session-reliability-2026-09-02.md) · **CERRADO** `1f25d351` (E2E mock locales). GP-V177-01..07 · A→B→C→A→refresh→stale→recovery→UNKNOWN→recon→clean. Partida V1.76 `bf6ba462`. **No** LIVE · **no** dryRun=false browser · **no** stamp CI GREEN.
47. **Session Golden MERCADO→EXIT v1.78 (2026-09-02):** [spec-v178](./spec-v178-session-golden-mercado-exit-2026-09-02.md) · [plan](./plan-v178-session-golden-mercado-exit-2026-09-02.md) · [relevo](./traspaso-relevo-v1-78-session-golden-mercado-exit-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-78-session-golden-mercado-exit-2026-09-02.md) · **CERRADO** `e1dcfba8` (E2E mock locales). GP-V178-01..08 · candidato→ENTRY dryRun→stale/recovery→POSITION→T1→TRAIL→recon→EXIT. Partida V1.77 `1f25d351`. **No** LIVE · **no** dryRun=false browser · **no** stamp CI GREEN.
48. **Stateful Position Lifecycle v1.79 (2026-09-02):** [spec-v179](./spec-v179-stateful-position-lifecycle-2026-09-02.md) · [plan](./plan-v179-stateful-position-lifecycle-2026-09-02.md) · [relevo](./traspaso-relevo-v1-79-stateful-position-lifecycle-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-79-stateful-position-lifecycle-2026-09-02.md) · **CERRADO** `8228d1c3` (E2E mock locales). GP-V179-01 · AAPL candidato→CLOSED (T1_EXECUTED · deny stale ausente · Journal). Partida V1.78 `e1dcfba8`. **No** LIVE · **no** dryRun=false browser · **no** stamp CI GREEN.
49. **CI GREEN Tip Honesty v1.80 (2026-09-02):** [spec-v180](./spec-v180-ci-green-tip-honesty-2026-09-02.md) · [plan](./plan-v180-ci-green-tip-honesty-2026-09-02.md) · [relevo](./traspaso-relevo-v1-80-ci-green-tip-honesty-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-80-ci-green-tip-honesty-2026-09-02.md) · **CERRADO** · **stamp CI GREEN** [`v1.80-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.80-beta) → [`7bd6ed81`](https://github.com/jvelasca/Bolsa_V1/commit/7bd6ed81) · [run 33644966298](https://github.com/jvelasca/Bolsa_V1/actions/runs/33644966298). `release-tag-ci` `playwright-mock`: `gp-e2e|gp-v173|…|gp-v179`. Partida V1.79 `8228d1c3`. **No** LIVE · **no** Playwright en frontend-ci · **no** T2 · **no** fixtures split.
50. **T2 POV Stages v1.81 (2026-09-02):** [spec-v181](./spec-v181-t2-pov-stages-2026-09-02.md) · [plan](./plan-v181-t2-pov-stages-2026-09-02.md) · [relevo](./traspaso-relevo-v1-81-t2-pov-stages-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-81-t2-pov-stages-2026-09-02.md) · **CERRADO** · **stamp CI GREEN** [`v1.81-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.81-beta) → [`4fcfc9bb`](https://github.com/jvelasca/Bolsa_V1/commit/4fcfc9bb) · [run 33648642728](https://github.com/jvelasca/Bolsa_V1/actions/runs/33648642728). GP-V181-01 · stages `t2_ready`\|`t2_executed` · AAPL · MONITOR/Mantener · 0 COMPRAR · filtro `+gp-v181`. Partida V1.80 `7bd6ed81`. **No** LIVE · **no** desk CTA redesign · next = fixtures split V1.82.
51. **Fixtures Split v1.82 (2026-09-02):** [spec-v182](./spec-v182-fixtures-split-2026-09-02.md) · [plan](./plan-v182-fixtures-split-2026-09-02.md) · [relevo](./traspaso-relevo-v1-82-fixtures-split-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-82-beta-2026-09-02.md) · [arranque auditor slice](./arranque-auditor-v1-82-fixtures-split-2026-09-02.md) · [arranque auditor externo](./arranque-auditor-v1-82-beta-2026-09-02.md) · [respuesta auditor](./respuesta-auditor-v182-operational-financial-truth-2026-09-02.md) · **CERRADO** · **stamp CI GREEN** [`v1.82-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.82-beta) → [`d0ccf235`](https://github.com/jvelasca/Bolsa_V1/commit/d0ccf235) · [run 33651647262](https://github.com/jvelasca/Bolsa_V1/actions/runs/33651647262) · docs `f543fab5`. Higiene `fixtures.ts` → `helpers/e2e-mock-*` + barrel · misma semántica · filtro CI intacto · 33 passed. Partida V1.81 `4fcfc9bb`. Auditoría 9,75/10 · P1→V1.83. **No** LIVE · **no** bump · **no** Playwright en frontend-ci.
52. **Lifecycle Snapshot Truth v1.83 (2026-09-02):** [spec-v183](./spec-v183-lifecycle-snapshot-truth-2026-09-02.md) · [plan](./plan-v183-lifecycle-snapshot-truth-2026-09-02.md) · [relevo](./traspaso-relevo-v1-83-lifecycle-snapshot-truth-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-83-beta-2026-09-02.md) · [arranque auditor slice](./arranque-auditor-v1-83-lifecycle-snapshot-truth-2026-09-02.md) · [arranque auditor externo](./arranque-auditor-v1-83-beta-2026-09-02.md) · [respuesta auditor](./respuesta-auditor-v183-lifecycle-snapshot-truth-2026-09-02.md) · **CERRADO** · **PASS auditor 9,85/10** · **stamp CI GREEN** [`v1.83-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.83-beta) → [`dc596ee5`](https://github.com/jvelasca/Bolsa_V1/commit/dc596ee5) · [run 33657045026](https://github.com/jvelasca/Bolsa_V1/actions/runs/33657045026) · docs `67ab2e75`. `LifecycleSnapshot` SoT · lineage EXIT/CLOSED · invariantes qty/PnL/R · equity única · GP-V183-01/02 · filtro `+gp-v183` · 35 passed. Partida V1.82 `d0ccf235`. **No** LIVE · next = event-driven mock V1.84.
53. **Lifecycle Event-Driven Mock v1.84 (2026-09-02):** [spec-v184](./spec-v184-lifecycle-event-driven-mock-2026-09-02.md) · [plan](./plan-v184-lifecycle-event-driven-mock-2026-09-02.md) · [relevo](./traspaso-relevo-v1-84-lifecycle-event-driven-mock-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-84-beta-2026-09-02.md) · [arranque auditor externo](./arranque-auditor-v1-84-beta-2026-09-02.md) · [respuesta auditor](./respuesta-auditor-v184-lifecycle-event-driven-mock-2026-09-02.md) · **CERRADO** · **PASS auditor 9,5/10** · **stamp CI GREEN** [`v1.84-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.84-beta) → [`504aa19d`](https://github.com/jvelasca/Bolsa_V1/commit/504aa19d) · [run 33659480690](https://github.com/jvelasca/Bolsa_V1/actions/runs/33659480690) · docs `d47168b7`. POST emit→log append-only→GET reduce · wire `events` ⊆ log · GP-V184-01/02 · filtro `+gp-v184` · 37 passed. Partida V1.83 `dc596ee5` PASS. P1→V1.85 (FSM · time · identity). **No** LIVE · **no** FastAPI event store · **no** integrated obligatorio.
54. **Lifecycle Integrity & Financial Event Model v1.85 (2026-09-02):** [spec-v185](./spec-v185-lifecycle-integrity-financial-event-model-2026-09-02.md) · [plan](./plan-v185-lifecycle-integrity-financial-event-model-2026-09-02.md) · [relevo](./traspaso-relevo-v1-85-lifecycle-integrity-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-85-beta-2026-09-02.md) · [arranque auditor externo](./arranque-auditor-v1-85-beta-2026-09-02.md) · [respuesta auditor](./respuesta-auditor-v185-lifecycle-integrity-2026-09-02.md) · **CERRADO** · **PASS modelo mock 9,25/10** · **NO beta estable** · **stamp CI GREEN** [`v1.85-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.85-beta) → [`665242a3`](https://github.com/jvelasca/Bolsa_V1/commit/665242a3) · [run 33663836923](https://github.com/jvelasca/Bolsa_V1/actions/runs/33663836923). VALIDATE FSM→APPEND→REDUCE→SNAPSHOT+accounting · Vitest FSM · GP-V185 · filtro `+gp-v185` · **40 passed**. Partida V1.84 PASS 9,5/10. P1→V1.86 (ENTRY · eventId conflict · identity · payload · trail · PG). **No** LIVE.
55. **Lifecycle Event Store v1.86 (2026-09-02):** [spec-v186](./spec-v186-lifecycle-event-store-2026-09-02.md) · [plan](./plan-v186-lifecycle-event-store-2026-09-02.md) · [relevo](./traspaso-relevo-v1-86-lifecycle-event-store-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-86-beta-2026-09-02.md) · [arranque auditor externo](./arranque-auditor-v1-86-beta-2026-09-02.md) · [respuesta auditor](./respuesta-auditor-v186-lifecycle-event-store-2026-09-02.md) · **CERRADO** · **stamp CI GREEN** [`v1.86-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.86-beta) → [`baaa7034`](https://github.com/jvelasca/Bolsa_V1/commit/baaa7034) · [run 33686297402](https://github.com/jvelasca/Bolsa_V1/actions/runs/33686297402). ENTRY accounting · P1-01…05 · PG event store · FastAPI · GP-V186 · `lifecycle-pg`. Partida V1.85 PASS modelo. Auditoría **9,0/10 arquitectura · 7,5/10 explotable · NO beta estable** (P0 auth HTTP · P1 sequence/Alembic/DTO). **No** LIVE.
56. **Lifecycle Operational Integration v1.87 (2026-09-02):** [spec-v187](./spec-v187-lifecycle-operational-certification-2026-09-02.md) · [plan](./plan-v187-lifecycle-operational-certification-2026-09-02.md) · [relevo](./traspaso-relevo-v1-87-lifecycle-operational-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-87-beta-2026-09-02.md) · [arranque auditor externo](./arranque-auditor-v1-87-beta-2026-09-02.md) · [respuesta auditor](./respuesta-auditor-v187-lifecycle-operational-2026-09-02.md) · **CERRADO** · **PASS operacional 9,2/10** · **stamp CI GREEN** [`v1.87-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.87-beta) → [`646b97ac`](https://github.com/jvelasca/Bolsa_V1/commit/646b97ac) · [run 33689747400](https://github.com/jvelasca/Bolsa_V1/actions/runs/33689747400). JWT ownership · sequence lock · Alembic from-zero · forbid DTO · Decimal. Partida `v1.86-beta` → `baaa7034`. P0/P1 V1.86 cerrados. **No** LIVE.
57. **Lifecycle Integrated Golden v1.88 (2026-09-02):** [spec-v188](./spec-v188-lifecycle-integrated-golden-2026-09-02.md) · [plan](./plan-v188-lifecycle-integrated-golden-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-88-beta-2026-09-02.md) · [arranque auditor](./arranque-auditor-v1-88-beta-2026-09-02.md) · [respuesta auditor](./respuesta-auditor-v188-lifecycle-integrated-golden-2026-09-02.md) · **CERRADA (CI GREEN)** · tip `v1.88-beta` → `33685242` · [run 33691233738](https://github.com/jvelasca/Bolsa_V1/actions/runs/33691233738) · **PASS sidecar 8,3/10 · NO beta PAPER**. Pendiente V1.89 desk truth.
58. **PAPER Desk Truth v1.89 (2026-09-03):** [spec-v189](./spec-v189-paper-desk-truth-2026-09-02.md) · [plan](./plan-v189-paper-desk-truth-2026-09-02.md) · [relevo](./traspaso-relevo-v1-89-paper-desk-truth-2026-09-02.md) · [relevo tag](./traspaso-relevo-tag-v1-89-beta-2026-09-03.md) · [arranque auditor](./arranque-auditor-v1-89-beta-2026-09-03.md) · [respuesta auditor](./respuesta-auditor-v189-paper-desk-truth-2026-09-03.md) · **CERRADA (CI GREEN)** · tip `v1.89-beta` → `58806be1` · [run 33718828984](https://github.com/jvelasca/Bolsa_V1/actions/runs/33718828984). Confirm→lifecycle · recon HTTP fail-closed · mesa snapshot · **PASS arquitectónico · NO beta PAPER · P1=3**.
59. **Lifecycle Reliability v1.90 (2026-09-03):** [spec-v190](./spec-v190-lifecycle-reliability-2026-09-03.md) · [plan](./plan-v190-lifecycle-reliability-2026-09-03.md) · [relevo](./traspaso-relevo-v1-90-lifecycle-reliability-2026-09-03.md) · [relevo tag](./traspaso-relevo-tag-v1-90-beta-2026-09-03.md) · [arranque auditor](./arranque-auditor-v1-90-lifecycle-reliability-2026-09-03.md) · **CERRADA (CI GREEN)** — tip `v1.90-beta` → `0c2e3af7` · [run 33726147414](https://github.com/jvelasca/Bolsa_V1/actions/runs/33726147414). Golden Confirm real · outbox · idempotencia · AUTO sidecar · SHORT ratchet · Mesa labels · OpenAPI. Partida `v1.89-beta` → `58806be1`. **No** LIVE · **no** bump · `PAPER_D_EXECUTE` off.
60. **Operational Atomicity v1.91 (2026-09-03):** [spec-v191](./spec-v191-operational-atomicity-2026-09-03.md) · [plan](./plan-v191-operational-atomicity-2026-09-03.md) · [relevo](./traspaso-relevo-v1-91-operational-atomicity-2026-09-03.md) · [relevo tag](./traspaso-relevo-tag-v1-91-beta-2026-09-03.md) · [arranque auditor](./arranque-auditor-v1-91-operational-atomicity-2026-09-03.md) · [respuesta auditor V1.90](./respuesta-auditor-v190-lifecycle-reliability-2026-09-03.md) · [respuesta auditor V1.91](./respuesta-auditor-v191-operational-atomicity-2026-09-03.md) · **CERRADA (CI GREEN)** — tip `v1.91-beta` → `4644fef9` · [run 33748255004](https://github.com/jvelasca/Bolsa_V1/actions/runs/33748255004). PositionState+Outbox misma TX · LifecycleOutboxWorker · Golden Confirm HTTP OPEN→T1→EXIT · requeue · Mesa `lifecycleStage` · append DTO tipado. Partida `v1.90-beta` → `0c2e3af7`. **No** LIVE · **no** bump · `PAPER_D_EXECUTE` off · auditoría tip **PASS arquitectónico · P1=2** (worker cert + orden).
61. **Lifecycle Concurrency & Worker Certification v1.92 (2026-09-03):** [spec-v192](./spec-v192-lifecycle-concurrency-worker-cert-2026-09-03.md) · [plan](./plan-v192-lifecycle-concurrency-worker-cert-2026-09-03.md) · [relevo](./traspaso-relevo-v1-92-lifecycle-concurrency-worker-cert-2026-09-03.md) · [relevo tag](./traspaso-relevo-tag-v1-92-beta-2026-09-03.md) · [arranque auditor](./arranque-auditor-v1-92-lifecycle-concurrency-worker-cert-2026-09-03.md) · [respuesta auditor V1.91](./respuesta-auditor-v191-operational-atomicity-2026-09-03.md) · [respuesta auditor V1.92](./respuesta-auditor-v192-lifecycle-concurrency-worker-cert-2026-09-03.md) · **CERRADA (CI GREEN)** — tip `v1.92-beta` → `752918ef` · [run 33754485267](https://github.com/jvelasca/Bolsa_V1/actions/runs/33754485267) · **PASS tip audit · P0=0 · P1=0**. FIFO claim por `position_id` · worker real PG · stale reclaim · métricas outbox Consola. Partida `v1.91-beta` → `4644fef9`. **No** LIVE · **no** bump · `PAPER_D_EXECUTE` off.
62. **Operational Failure Injection v1.93 (2026-09-03):** [spec-v193](./spec-v193-operational-failure-injection-2026-09-03.md) · [plan](./plan-v193-operational-failure-injection-2026-09-03.md) · [relevo](./traspaso-relevo-v1-93-operational-failure-injection-2026-09-03.md) · [relevo tag](./traspaso-relevo-tag-v1-93-beta-2026-09-03.md) · [arranque auditor](./arranque-auditor-v1-93-operational-failure-injection-2026-09-03.md) · [respuesta auditor V1.92](./respuesta-auditor-v192-lifecycle-concurrency-worker-cert-2026-09-03.md) · [respuesta auditor V1.93](./respuesta-auditor-v193-operational-failure-injection-2026-09-03.md) · **CERRADA (CI GREEN)** — tip `v1.93-beta` → `7168de3a` · [run 33759914125](https://github.com/jvelasca/Bolsa_V1/actions/runs/33759914125) · **PASS fuerte · P0=0 · P1=0 · P2=5**. Worker TX split · crash/idempotencia · reconnect · recon PositionState↔Lifecycle · JWT stats · SLA. Partida `v1.92-beta` → `752918ef`. **No** LIVE · **no** bump · **no** `queue_sequence` · `PAPER_D_EXECUTE` off.
63. **Financial Integrity & Reconciliation v1.94 (2026-09-03):** [spec-v194](./spec-v194-financial-integrity-2026-09-03.md) · [plan](./plan-v194-financial-integrity-2026-09-03.md) · [relevo](./traspaso-relevo-v1-94-financial-integrity-2026-09-03.md) · [relevo tag](./traspaso-relevo-tag-v1-94-beta-2026-09-03.md) · [arranque auditor](./arranque-auditor-v1-94-financial-integrity-2026-09-03.md) · [respuesta auditor V1.94](./respuesta-auditor-v194-financial-integrity-2026-09-03.md) · tip `v1.94-beta` → `363984d2` · **NO beta estable** (CI rojo · P1 fail-closed). Partida `v1.93-beta` → `7168de3a`. **No** LIVE · **no** bump · `PAPER_D_EXECUTE` off.
64. **Beta Certification v1.95 (2026-09-03):** [spec-v195](./spec-v195-beta-certification-2026-09-03.md) · [plan](./plan-v195-beta-certification-2026-09-03.md) · [relevo](./traspaso-relevo-v1-95-beta-certification-2026-09-03.md) · [relevo tag](./traspaso-relevo-tag-v1-95-beta-2026-09-03.md) · [arranque auditor](./arranque-auditor-v1-95-beta-certification-2026-09-03.md) · [respuesta auditor V1.94](./respuesta-auditor-v194-financial-integrity-2026-09-03.md) · [respuesta auditor V1.95](./respuesta-auditor-v195-beta-certification-2026-09-03.md) · tip [`v1.95-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.95-beta) → `6f262293` · Release-tag CI **GREEN** ([run 33804374800](https://github.com/jvelasca/Bolsa_V1/actions/runs/33804374800)) — lag/dead DENY · fill OPEN+T1+T2+EXIT (código) · Golden HTTP OPEN→T1→EXIT. Auditoría tip **PASS con 1 P1 cobertura T2**. **No** LIVE · **no** bump · **no** unificar ledger · **no** auto-heal · `PAPER_D_EXECUTE` off.
65. **Final Beta Certification / T2 v1.96 (2026-09-03):** [spec-v196](./spec-v196-final-beta-certification-2026-09-03.md) · [plan](./plan-v196-final-beta-certification-2026-09-03.md) · [relevo](./traspaso-relevo-v1-96-final-beta-certification-2026-09-03.md) · [relevo tag](./traspaso-relevo-tag-v1-96-beta-2026-09-03.md) · [arranque auditor](./arranque-auditor-v1-96-final-beta-certification-2026-09-03.md) · [respuesta auditor V1.95](./respuesta-auditor-v195-beta-certification-2026-09-03.md) · [respuesta auditor V1.96](./respuesta-auditor-v196-final-beta-certification-2026-09-03.md) · **CERRADA (CI GREEN · PASS auditor)** — tip [`v1.96-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.96-beta) → `30479e97` · [run 33808076820](https://github.com/jvelasca/Bolsa_V1/actions/runs/33808076820). SEMI T2 + Golden HTTP OPEN→T1→T2→EXIT. lifecycle-pg **25 passed**. BETA PAPER técnicamente certificada (P0=0 P1=0). **No** LIVE · **no** bump · **no** unificar ledger · **no** auto-heal · `PAPER_D_EXECUTE` off.
66. **T2 Transactional Atomicity v1.97 (2026-09-03):** [spec-v197](./spec-v197-t2-transactional-atomicity-2026-09-03.md) · [plan](./plan-v197-t2-transactional-atomicity-2026-09-03.md) · [relevo](./traspaso-relevo-v1-97-t2-transactional-atomicity-2026-09-03.md) · [relevo tag](./traspaso-relevo-tag-v1-97-beta-2026-09-03.md) · [arranque auditor](./arranque-auditor-v1-97-t2-transactional-atomicity-2026-09-03.md) · [respuesta auditor V1.96](./respuesta-auditor-v196-final-beta-certification-2026-09-03.md) · **CERRADA (CI GREEN)** — tip [`v1.97-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.97-beta) → `2e9d4675` · [run 33812286022](https://github.com/jvelasca/Bolsa_V1/actions/runs/33812286022). Par atómico T2 + crash/replay. Partida `v1.96-beta` → `30479e97`. **No** LIVE · **no** bump · **no** Alembic nuevo · E2E integrado opt-in · `PAPER_D_EXECUTE` off.
67. **Trail + T2 Coexistence v1.98 (2026-09-04):** [spec-v198](./spec-v198-trail-t2-coexistence-2026-09-04.md) · [plan](./plan-v198-trail-t2-coexistence-2026-09-04.md) · [relevo](./traspaso-relevo-v1-98-trail-t2-coexistence-2026-09-04.md) · [relevo tag](./traspaso-relevo-tag-v1-98-beta-2026-09-04.md) · [arranque auditor](./arranque-auditor-v1-98-trail-t2-coexistence-2026-09-04.md) · **CERRADA (CI GREEN)** — tip [`v1.98-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.98-beta) → `7b5b1052` · [run 33844531875](https://github.com/jvelasca/Bolsa_V1/actions/runs/33844531875). FSM trail⇄T2 · `last_fill_price` · `stop_worsens` dominio · sync TS SHORT. Partida `v1.97-beta` → `2e9d4675`. **No** LIVE · **no** bump · **no** Alembic · SEMI protect→TRAIL residual · `PAPER_D_EXECUTE` off.
68. **Position Management Certification v1.99 (2026-09-04):** [spec-v199](./spec-v199-position-management-certification-2026-09-04.md) · [plan](./plan-v199-position-management-certification-2026-09-04.md) · [relevo](./traspaso-relevo-v1-99-position-management-certification-2026-09-04.md) · [relevo tag](./traspaso-relevo-tag-v1-99-beta-2026-09-04.md) · [arranque auditor](./arranque-auditor-v1-99-position-management-certification-2026-09-04.md) · tip [`v1.99-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v1.99-beta) → [`bf57899b`](https://github.com/jvelasca/Bolsa_V1/commit/bf57899b) · Release-tag CI **GREEN** ([run 33847460567](https://github.com/jvelasca/Bolsa_V1/actions/runs/33847460567)). Solo certificación (G1–G8) · lineage ≠ log · `initialRisk` inmutable · **no** features FSM · **no** LIVE · **no** bump · `PAPER_D_EXECUTE` off · PASS auditor pendiente.
69. **Post-V1.99 apertura plan cierre PAPER AUTO (2026-09-04):** [relevo planificador](./traspaso-relevo-post-v199-plan-cierre-paper-auto-2026-09-04.md) · [runbook DEMO execute](./runbook-demo-paper-d-execute-2026-09-04.md) — ENGINE FREEZE (implementación) → DEMO execute honesty → V2.0 AUTO Desk journey HUD en MERCADO · risk readout · SEMI→TRAIL sidecar. Auditoría V1.99 se mantiene · **no** reabrir FSM.
70. **V2.0 cierre PAPER AUTO (2026-09-04):** [relevo](./traspaso-relevo-v2-0-cierre-paper-auto-2026-09-04.md) · [relevo tag](./traspaso-relevo-tag-v2-0-beta-2026-09-04.md) · tip [`v2.0-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v2.0-beta) → [`e05fc6b0`](https://github.com/jvelasca/Bolsa_V1/commit/e05fc6b0) · Release-tag CI **GREEN** ([run 33849921728](https://github.com/jvelasca/Bolsa_V1/actions/runs/33849921728)) · journey HUD · DEMO runbook · SEMI→`TRAIL_APPLIED` · risk derivado · **no** bump · **no** LIVE · `TRANSITIONS` intacto · PASS auditor V1.99 pendiente · smoke journey con posición abierta + DEMO policy residual.
71. **V2.x Product UX (2026-09-04):** [relevo](./traspaso-relevo-v2-x-product-ux-2026-09-04.md) · [arranque auditor](./arranque-auditor-v2-x-product-ux-2026-09-04.md) — cabina Mercado · NEXT ACTION · Opportunity/Position Card · AUTO Desk · Hoy 4 cubos · Asesor · Journal · smoke Position+protect **OK** · tip cadena `b8192d3a` (V2.01–V2.09) · **CERRADO para auditoría externa** · ENGINE FREEZE · sin tip GitHub `v2.x-*`.
72. **V2.9 Visual and Operational Certification (2026-09-05):** [relevo](./traspaso-relevo-v2-9-visual-operational-certification-2026-09-05.md) · [arranque](./arranque-agente-v2-9-2026-09-05.md) — ARM chrome `autoActive` · `orphan_recovery_failed` · touch 44px · layout zoom · snapshots/contraste · **incluido en tip `v2.10-beta`** (sin `v2.9-beta`) · **no** LIVE.
73. **V2.10 Seed Ops (2026-09-05):** [relevo](./traspaso-relevo-v2-10-seed-ops-2026-09-05.md) · [arranque](./arranque-agente-v2-10-2026-09-05.md) · [runbook](./runbook-v2-10-seed-ops-cabin-smoke-2026-09-05.md) · [relevo tag](./traspaso-relevo-tag-v2-10-beta-2026-09-05.md) · [arranque post-tip](./arranque-agente-post-tip-v2-10-2026-09-05.md) — birth Confirm+`signedStop` estructural · Journal `mfeMae` · `ops_seed_cabin_smoke` · tip [`v2.10-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v2.10-beta) → `6495dd5f` · package `1.39.0-beta` · tip SHA CI [33980277268](https://github.com/jvelasca/Bolsa_V1/actions/runs/33980277268) `failure` · **certificado vía V2.10.1** · **no** LIVE.
74. **V2.10.1 CI GREEN hotfix (2026-09-05):** [relevo](./traspaso-relevo-v2-10-1-ci-green-2026-09-05.md) — Vitest copy + Hoy expand `no_operar` + journey `sr-only` stop/t1/t2 · SHA `7156169f` · Release-tag CI [run 33981998373](https://github.com/jvelasca/Bolsa_V1/actions/runs/33981998373) `conclusion=success` · **CERTIFICABLE**.
75. **V2.10 final certification pack (2026-09-05):** [audit-pack](./audit-pack-v2-10-final-certification-2026-09-05.md) — scorecard auditoría · matriz UI honestidad · P2 diferidos · listo auditoría externa · **no** V2.11.
76. **V2.10.1 tip provenance + PRODUCT FREEZE (2026-09-05):** [relevo tag](./traspaso-relevo-tag-v2-10-1-beta-2026-09-05.md) · [arranque post-tip](./arranque-agente-post-tip-v2-10-1-2026-09-05.md) — tip [`v2.10.1-beta`](https://github.com/jvelasca/Bolsa_V1/releases/tag/v2.10.1-beta) → [`a060af37`](https://github.com/jvelasca/Bolsa_V1/commit/a060af37) · package `1.39.1-beta` · hotfix `7156169f` · `v2.10-beta` inmutable `6495dd5f` · Release-tag CI [run 33983574346](https://github.com/jvelasca/Bolsa_V1/actions/runs/33983574346) `conclusion=success` · **CERTIFICABLE** · **PRODUCT FREEZE** · **no** V2.11 · **no** LIVE.
77. **Operational readiness post V2.10.1 (2026-09-05):** [audit pack](./audit-pack-v2-10-1-operational-readiness-2026-09-05.md) · [arranque](./arranque-agente-post-freeze-operational-2026-09-05.md) — uso real cabina · carga/resiliencia · observabilidad · seguridad ops · **docs + checklist** · **no** V2.11 · **PRODUCT FREEZE**.
78. **Preparación PAPER post V2.10.1 (2026-09-05):** [relevo](./traspaso-relevo-paper-prep-post-v2101-2026-09-05.md) · [runbook prep OFF](./runbook-paper-prep-post-v2101-2026-09-05.md) · [stamp PASS flags OFF 2026-09-06](./traspaso-relevo-stamp-paper-prep-post-v2101-2026-09-06.md) · [runbook DEMO](./runbook-demo-paper-d-execute-2026-09-04.md) — honesty Arm≠Execute · `PAPER_D_EXECUTE` default off · LIVE bloqueado · **docs only**.
79. **Triage P2 V2.10 diferidos (2026-09-05):** [triage](./triage-p2-v2-10-deferred-2026-09-05.md) — P2-01 pixel · P2-02 contrast smoke · P2-03 meta 9px · **no implementar** · P2-04 cerrado por tip.
80. **Cierre certificable V2.10.x (2026-09-06):** [relevo cierre](./traspaso-relevo-cierre-v2-10-x-2026-09-06.md) — tip `v2.10.1-beta` → `a060af37` / `1.39.1-beta` · hotfix `7156169f` · CI tag [33983574346](https://github.com/jvelasca/Bolsa_V1/actions/runs/33983574346) `success` · `v2.10-beta` inmutable `6495dd5f` · **V2.10.x CERRADA** · **no** retaguear · **no** `v2.10.2+` · siguiente = Nivel 4 operacional (LIVE bloqueado).
81. **Nivel 4 operacional PARTIAL (2026-09-06):** [stamp](./traspaso-relevo-stamp-nivel4-ops-partial-2026-09-06.md) · offline V1.75/V1.93/V1.97 PASS · chaos PG DEFER · ops readiness residuales B1·B3·C3 PASS · A6 wire PARTIAL · LIVE bloqueado.
82. **Diseño broker LIVE venue (2026-09-06):** [audit pack](./audit-pack-live-venue-thaw-design-2026-09-06.md) · [runbook gates](./runbook-live-venue-thaw-gates-2026-09-06.md) · [relevo](./traspaso-relevo-live-venue-design-2026-09-06.md) — DESIGN_ONLY · LIVE bloqueado · ≠ Accept estricto · ≠ flip venue.
83. **Camino Accept estricto post V2.10.1 (2026-09-06/07):** [audit pack](./audit-pack-thaw-estricto-path-post-v2101-2026-09-06.md) · [remeasure W+5](./traspaso-relevo-thaw-estricto-remeasure-2026-09-07.md) · [W+4](./traspaso-relevo-thaw-estricto-remeasure-2026-09-06.md) — P1=39 · 0/5 PASS · gates planos W+4→W+5 · W2–W4 vigentes · **NO** Accept · ≠ broker LIVE.
84. **Cierre sesión + arranque LIVE VIRTUAL (2026-09-06/07):** [cierre sesión](./traspaso-relevo-cierre-sesion-pista-a-2026-09-06.md) · [arranque](./arranque-agente-pista-a-estricto-2026-09-07.md) · pack LIVE [§0.1 premisas](./audit-pack-live-venue-thaw-design-2026-09-06.md) — LIVE **VIRTUAL** hasta APP 100% · pista A fondo · **W+5** **0/5** · **PRODUCT FREEZE** · **NO** Accept · **NO** flips.
85. **Diseño + UI Confirm pasarela LIVE VIRTUAL híbrido (2026-09-07):** [design UI](./design-live-virtual-order-gateway-ui-2026-09-07.md) · [relevo](./traspaso-relevo-design-live-virtual-ui-2026-09-07.md) · [probe histórico](./traspaso-relevo-pista-a-probe-no-w5-2026-09-07.md) · [W+5](./traspaso-relevo-thaw-estricto-remeasure-2026-09-07.md) — telegrama + por qué en Confirm · banner SIMULADO · E2E mock **PASS** · **≠** thaw · **≠** Accept · pista A **0/5**.
86. **Tip V2.11 LIVE VIRTUAL Confirm (2026-09-07):** [relevo tag](./traspaso-relevo-tag-v2-11-beta-2026-09-07.md) · [audit pack](./audit-pack-v2-11-live-virtual-confirm-2026-09-07.md) · [arranque auditor](./arranque-auditor-v2-11-live-virtual-2026-09-07.md) · [post-tip](./arranque-agente-post-tip-v2-11-2026-09-07.md) — package `1.40.0-beta` · UI honesty · **≠** Accept LIVE · **≠** estricto.
87. **LIVE Execution Core post-V2.11 (2026-09-07):** [roadmap](./roadmap-live-execution-core-2026-09-07.md) · [honesty bridge externo](./honesty-pack-xtb-bridge-external-2026-09-07.md) — OR-6 fail-closed · OE-1↔LR-1 · `LIVE_EXECUTION_UNLOCKED` sandbox · XL-3 dominio LiveOrder · tríada Producto/Estrategia/Broker · **≠** thaw.
88. **XL-3 wire persist-only en Confirm (2026-09-07):** [relevo agente](./traspaso-relevo-agente-xl3-wire-2026-09-07.md) · [roadmap](./roadmap-live-execution-core-2026-09-07.md) — `LiveOrderStore` + `LiveOrderCoordinator` persisten/exponen `result["liveOrder"]` (submitted→SUBMITTED · unknown→UNKNOWN); PAPER/sandbox/executed no rastro. Rama `live-honesty-post-tip-2026-09-07` · HEAD `6c780ecc`. UNKNOWN-recovery/sled PARKED (V2.12) · **≠** thaw · **≠** unlock.
89. **V2.12 durable core + V2.13 formal tip (2026-09-07):** [traspaso V2.12](./traspaso-relevo-tag-v2-12-beta-2026-09-07.md) · [xl3 durable core](./traspaso-relevo-xl3-durable-core-2026-09-07.md) · [roadmap](./roadmap-live-execution-core-2026-09-07.md) — `live_orders` PG + migraciones `020`/`021` (`NUMERIC(18,6)` + CHECK `filled+remaining=quantity`) · claim/lease/SKIP LOCKED · broker-query real (H6) + reconcile máquina H7 · cancel honesto H5 · formal tip **`da5c4b2a`** (`1.42.0-beta`, 5 commits sobre V2.12) · Release-tag CI **NO** certificable sin evidencia GitHub (`statuses`). **≠** LIVE READY.
90. **V2.13 auditoría externa + hallazgos (2026-09-07):** [audit-ext](./audit-ext-v2-13-execution-core-honesty-2026-09-07.md) · [arranque auditor](./arranque-auditor-v2-13-execution-core-honesty-2026-09-07.md) · [deuda ampliada](./deuda-anotada-audit-v2-13-ampliado-2026-09-07.md) · [veredicto `08cada82`] — sin P0 · 4 P1 (ExecutionEvent/idempotencia · full recon · Decimal boundary · cert PG+CI) · P2-01..P2-05 · ~9.3/10. **Fix N-1..N-4**.
91. **V2.14 Financial Execution & Full Reconciliation (2026-09-07):** [decree plan](./plan-v2-14-financial-execution-reconciliation-2026-09-07.md) · [D0 relevo](./traspaso-relevo-d0-v2-14-foundation-2026-09-07.md) — **decreto numeración** (V2.14=Financial Execution · V2.15 Shadow LIVE · V2.16 cert) resuelve colisión operator-journey/roadmap · faenas D0·B1·B2·E1·E2·C1 · D0 docs (sin código, esta faena). Rama `v2-13-1-rc-honesty-remediation` HEAD `08cada82`. **No** cert CI GREEN.
92. **B1 V2.14 Decimal boundary (2026-09-07):** [relevo B1](./traspaso-relevo-b1-v2-14-decimal-boundary-2026-09-07.md) — P1-03/P2-02/P2-03 parcial decretada: `XtbBridgeOrderState.filled/remaining`, `XtbBridgeAccountCash.cash`, `XtbBridgePosition.quantity`, `BrokerOrderQueryResult`, `XtbLiveOrderQueryAdapter`, `LiveOrderDrift` → `Decimal(6dp)`; se elimina `float` de la cadena broker→máquina/orders. Ruff/mypy 0 · pytest subsets verdes. Rama `v2-14-financial-execution`. Agregado Account/Position full-decimal del LR-1 EPS → E2.
93. **B2 V2.14 (2026-09-07): lease configurable env + cancel honesta:** [relevo B2](./traspaso-relevo-b2-v2-14-worker-lease-cancel-2026-09-07.md) — `LIVE_RECOVERY_CLAIM_STALE_SECONDS` regula la ventana del lease (P2-04) con fallback 120 · +test env · columnas `attempt_count/last_error/claim_expires_at` DIFERIDAS a E1 (migración única `live_orders`+`execution_events`, validadas en PG→C1) · XTB cancel real **PARKED** (bridge sin `POST /orders/{id}/cancel`). Ruff 0 · pytest worker 8✓.
94. **E1 V2.14 ExecutionEvent scaffold (2026-09-07, P1-01):** [relevo E1](./traspaso-relevo-e1-v2-14-execution-event-scaffold-2026-09-07.md) — migración `022_live_orders_exec` (`live_orders` +`attempt_count/last_error/claim_expires_at` · tabla `execution_events` con `execution_id` PK) · ORM · `execution_event.py` (`ON CONFLICT execution_id DO NOTHING RETURNING` = idempotencia real sin counter; `apply_fill_idempotent` GATED, default captura sin materializar Position/Ledger, H4) · tests 5 + guard offline · DB local NO migrada (022→C1 real-PG).
95. **E2 V2.14 trozo PARTIAL — dedup OPEN (2026-09-07, P2-2):** [relevo E2 partial](./traspaso-relevo-e2-v2-14-incident-dedup-partial-2026-09-07.md) — `PostgresOperationalIncidentStore.put` ya NO re-lanza el IntegrityError cuando dos pollers abren OPEN del mismo `(account,kind)` activo: rollback→refetch→si activo existe = OPEN deduplicado (1 por `(account,kind)`, sin auto-heal/cierre) · 20 tests incident verdes · **E2 NO cerrrado**: full position/cash recon (P1-02) + durable máquina-drift (P2-01) + PG 2-sesión pendientes.
96. **V2.14 E2-full (P2-01 + P1-02) + C1 real-PG + Release-tag GREEN (2026-09-08):** [relevo order-drift durable](./traspaso-relevo-e2-v2-14-order-drift-durable-2026-09-08.md) · decreto cierre ([`plan decreto`](./plan-v2-14-financial-execution-reconciliation-2026-09-07.md)) — **E2 CERRADO**: P2-01 writer durable `live_drift` (`publish_order_live_drifts`, kinds accionables `cancel_broker_side/fill_unseen/state_mismatch`, `query_unavailable` transitorio NO abre veto) + P1-02 recon posición LR-1 continua bajo el MISMO gate, ambos en el recovery worker (`_drain_once`) · C1: migración `022` → PG real scratch `bolsa_c1_scratch` 001→022 + batería real-PG dedup OPEN 2-sesión + drift dur verbose + constraints 021 intactos · guards head real-PG a `022_live_orders_exec` · ruff whole-tree 0 · mypy 436 files Success (fix 3 errores por run CI real 34207066169) · pytest verdes · **Release-tag CI** tip `v2.14-beta` → run 34208259186 **`conclusion=success` GREEN** (artifact). **ELEVADO a `main`** `78dd3f9a` == tag `v2.14-beta` (auditable desde GitHub) · **bump `1.43.0-beta`** (este commit).
97. **V2.14 cierre auditoría + 3 hotfixes de propositura + elevación V2.14.1 (2026-09-08):** [cierre auditoría V2.14 + hotfixes](./traspaso-relevo-cierre-auditoria-v214-hotfixes-2026-09-08.md) · [elevación V2.14.1](./traspaso-relevo-tag-v2-14-1-beta-2026-09-08.md) — auditoría interna de la V2.14 detecta y **remedia un schema-drift real**: `upsert_bars` (`ON CONFLICT (instrument_id,timeframe,timestamp)`) exigía un índice único que Alembic no creaba → sync de mercado abortaba y la BD quedaba a 0 barras. Hotfixes: `dbc0ad1f` provenance self-reported + contract gate G12/G13 · `cd9e3524` A1 account-isolation rutas EJECUCIÓN (lectura queda sin gate, deuda residual) · `326c0a8a` migración `023_ohlcv_bars_unique_reconcile` + repoblado (44.7k barras 1d). **Elevación `v2.14.1-beta`** == `main` · **bump `1.43.0-beta` → `1.43.1-beta`** (este commit).
98. **V2.14.2 elevation — A1 CERRADO en rutas de LECTURA/estudio (2026-09-08):** [relevo V2.14.2](./traspaso-relevo-tag-v2-14-2-beta-2026-09-08.md) — extiende la account-isolation a los endpoints de LECTURA/estudio que declaran `account_id`: nuevo `require_owned_account_if_present` (`dependencies.py`) → cuenta ajena = 404 · rutas en `ai_governance` (effectiveness/decision-sessions/memory/learning-summary/session+replay/outcome/trials/edge-reports/propose), `instrument_daily_opinions` (query/auto-telemetry/auto-propose/eod-batch), `paper-desk/daily-report`, `risk/ops-self-eval` · tests de aislamiento por cuenta añadidos. Hallazgo A1 del informe de lectura pasa de **[cerrado parcial]** a **[cerrado]** (la condición multi-owner real sigue `[runtime-aislamiento]`). **Elevación** == `main` · **bump `1.43.1-beta` → `1.43.2-beta`** (este commit).
99. **Respuesta + certificación auditor externo V2.14.2 (2026-09-08):** [certificación V2.14.2](./respuesta-auditor-externo-v2-14-2-2026-09-08.md) — tria la auditoría externa V2.14.2 contra el código real y **certifica GREEN el Release-tag CI** de `v2.14.2-beta` (`2a98886c`): run `34224783087`, `conclusion=success`, job `certify` success → el P1-02 del auditor ("CI no certificado por tag") es **falso positivo** (el tag y el run existen). CONFIRMADO-correcto: OHLCV→schema 023, UNKNOWN no-re-POST+lease, ExecutionEvent fail-closed, OperationalIncident dedup, recon continua sin auto-heal, FSM live, Paper Desk. Deuda real catalogada para V2.15 (solo enlistada, 0 cambios de código): P1-01 account-less→global (single-owner, no urgente), liveness/readiness, provenance env obligatoria, columnas muertas `claim_expires_at/attempt_count/last_error`, T1/T2, FSM declarativa, matriz cobertura CI, baterías `[runtime]` LIVE.
100.  **Incidente pérdida de listas propias + 'estudio' (2026-09-08):** [incidente](./traspaso-incidente-perdida-list-2026-09-08.md) — **DIAGNÓSTICO confirmado (solo lectura)**: la BD `bolsa_v1` quedó en estado bootstrap/seed (2 listas: `ibex35`[35] sano con precio + `estudio`[0] stub vacío; `decision_sessions`/`daily_opinions`/`users`=0) mientras se conservan `ohlcv_bars`=44733 y 1 cuenta. No es bars/freshness (ibex35 carga `current`). Listas propias y membresía 'estudio' eran estado runtime **no versionado** y **no hay pg_dump/backup** → no recuperable desde el repo. Causa con más peso: BD quedó en estado drift/0 filas (schema-drift V2.14) y el remedio solo reconstruyó catálogo IBEX+barras. **Decisión RESUELTA**: sin backup externo ⇒ usuario asume pérdida y recrea listas/'estudio' a mano desde la UI. **Pendiente**: faena de prevención (script db dump / plantilla).
101.  **RELEVO v2.15 backups/prevención (2026-09-08):** [relevo](./traspaso-relevo-v2-15-backups-prevencion-2026-09-08.md) — **RELEVO preparado (handover, sin código aún)** para agente siguiente. Faena 1 PREVENCIÓN: pg_dump de `bolsa_v1` a `db-backups/` gitignored + scripts `scripts/lib/backup.mjs`, `scripts/db-dump.mjs`, `scripts/db-restore.mjs` (+list/cron:win) y ganchos `pnpm db:dump/db:backup/db:restore/db:backup:list/db:backup:cron:win`, `.gitignore db-backups/`, `DB_BACKUP_KEEP`, doc DEV_STARTUP. Faena 2 VERSIONAJE: cerrar doc pendiente V2.14.x y abrir **V2.15 hardening** (1-2 items del §4: provenance+readiness o limpieza lease) → bump `1.44.x-beta`, tag `v2.15-beta`, Release-tag CI GREEN real antes de afirmar. Línea roja: no `git add -A`, no tocar núcleo congelado (FSM/live_orders/ExecutionEvent/ledger/outbox/recon). Contiene texto de traspaso para el agente que ejecute.
102.  **V2.15 EJECUTADO — backups `bolsa_v1` + 1er ciclo hardening provenance/readiness → bump `1.44.0-beta` (2026-09-08):** rama `stage/v2.15-backups-prevencion-2026-09-08` desde `2a98886c` · PR **#59** → merge `808432aa` (CI PR verde: quality/battery/scan pass, tras C1 fix ruff I001 con `--config pyproject.toml`). Faena 1: `scripts/lib/backup.mjs` + `db-dump/db-restore/db-backup-list/db-backup-cron-win.mjs` + hooks `pnpm db:*` + `.gitignore db-backups/` + `DB_BACKUP_KEEP` + doc DEV_STARTUP (criterio cumplido: `db:dump` deja backup gitignored; restore a BD scratch 35 instruments). Faena 2 (item provenance+readiness): `GET /api/health/live` (liveness sin BD) + `GET /api/health/ready` (readiness PG→200/503) + `require_release_identity_env` (PRODUCT_VERSION/API_CONTRACT_VERSION obligatorias en `ENVIRONMENT=production`; dev/test/staging opcionales) · tests offline `test_provenance_gate.py` + `live/ready` en `test_health.py` · openapi/schema regenerado (solo aditivo). **Elevación** == `main` (commit `7c6d624e`) == **tag [`v2.15-beta`](./traspaso-relevo-tag-v2-15-beta-2026-09-08.md)** · **bump `1.43.2-beta` → `1.44.0-beta`** · **Release-tag CI GREEN real** run [34256252001](https://github.com/jvelasca/Bolsa_V1/actions/runs/34256252001) `conclusion=success` · job `certify` success · cierre [traspaso-relevo-tag-v2-15-beta](./traspaso-relevo-tag-v2-15-beta-2026-09-08.md).
103.  **V2.34/A14 — Strategy Intelligence (gramática controlada de Discovery) (2026-09-11):** [relevo A14](./traspaso-relevo-post-v2-34-a14-strategy-intelligence-2026-09-11.md) — Discovery pasa de catálogo de familias técnicas a **gramática controlada** (`REGIME + TREND FILTER + MOMENTUM + ENTRY TRIGGER + EXIT`, `TRIGGER`/`EXIT` obligatorios, máx. 3 opcionales) con enumeración determinista y presupuesto único (`GrammarBudget`, techo fijado por test anti-explosión: 1784 planes), **sin segundo motor ni segundo FSM**. Cierra el **gap bloqueante de promoción**: la vía declarativa del LAB (`_run_cpcv`/`_run_walk_forward`) gana rama que reutiliza `split_cpcv_paths` + `_simulate_rules_strategy` y produce **CPCV/PBO/DSR/WFE reales** (antes `robustness`/`walk_forward` quedaban `NOT_EVALUATED` y ninguna candidata de Discovery podía promocionar). Sana además las familias inertes del catálogo (`roc`/`srsi`/`sar`/`mom`/`wma`/`sd`/`obv`/`mfi`/`aroon`/`bears`/`bulls` cableados en `_series_for_spec`) y re-materializa el `executable` del campeón con los parámetros ganadores. Rollout reversible: `AUTO_ORCHESTRATOR_GRAMMAR` **OFF por defecto** (con OFF, Discovery idéntico a A13). Verificación: ruff/import-linter/mypy (470 ficheros) verdes · offline **1232 passed** · PG certificación **17 passed** sin skips (`lifecycle-pg`, `paper-forward-pg`, `grammar-discovery-pg`) · Alembic head `035_paper_forward_evidence` **sin migración** · invariantes H1/H2 y barreras LIVE intactas. Commit de fase `e6fbab83`; **elevación** == `main` == **tag `v2.34-beta`** · **bump `1.58.1-beta` → `1.59.0-beta`** (CHANGELOG).
104.  **V2.35/A15 — Observabilidad y gobernanza de la gramática de Discovery (2026-09-11):** [relevo A15](./traspaso-relevo-post-v2-35-a15-grammar-observability-2026-09-11.md) — hace **gobernable el rollout** de la gramática de A14 (hoy `AUTO_ORCHESTRATOR_GRAMMAR` OFF) con **observabilidad de solo lectura**, sin cambiar decisiones, presupuestos, gates ni migración. Nuevo `DiscoveryEmissionSummary` + API aditiva `discover_for_instrument_with_summary(...)` en `strategy_discovery_engine.py` (procedencia catálogo vs gramática, `trials_used`, cupos `catalog_cap`/`grammar_cap`, flag `grammar_enabled`, warm-up `bar_count_ok`); `discover_for_instrument` delega y sigue **byte-idéntico** a A13 con gramática OFF (test de regresión). `OrchestratorResult` gana conteos aditivos (`catalog_candidates`, `grammar_candidates`, `lab_grammar_evaluated`, `shadow_started`, `shadow_grammar_started`) derivados de la evidencia real (evaluaciones del LAB y replay shadow ejecutado; sin provider de barras `shadow_started=0`, fail-closed honesto). El worker AUTO emite **logs estructurados** por instrumento y un `cycle_summary` por ciclo, más `GrammarObservabilityCounters` de proceso (`grammar_counters()`), sin persistencia en DB. Tests herméticos de resumen OFF/ON, cuadre `catálogo+gramática=total`, determinismo, warm-up, regresión A13, contadores y `cycle_summary` del worker, y procedencia LAB/shadow del orquestador. Verificación: ruff + import-linter (4/0) + mypy (470 ficheros) verdes · offline **1243 passed** (único fallo: flake ambiental documentado de `test_a9_scheduler_process_pg_zero_human` sobre BD local sucia, pasa en aislamiento) · PG certificación **6 passed** sin skips · Alembic head `035_paper_forward_evidence` **sin migración** · invariantes `AUTO ⇒ SIMULATED` / LIVE / fail-closed intactas. **Bump `1.59.0-beta` → `1.60.0-beta`** (CHANGELOG).
105.  **Audit-pack consolidado A13+A14+A15 (2026-09-11):** [audit-pack](./audit-pack-v2.35-consolidado-a13-a14-a15-2026-09-11.md) — **punto de entrada único para auditoría externa desde GitHub**, sin acceso al entorno. Cubre las tres últimas fases del núcleo de Strategy Intelligence (A13 Paper Forward → tag `v2.33.0-beta`; hardening H1+H2, commit `61e613b1`; A14 gramática controlada → tag `v2.34-beta`; A15 observabilidad → tag `v2.35-beta`), con **matriz afirmación→punto de código→test** por fase, comandos de **reproducción** (calidad + herméticos + PG sin skips), **invariantes congelados**, checklist de auditoría y **inventario honesto tag→commit→veredicto de CI** desde `v2.11-beta` (incluyendo los dos tags cuyo Release-tag CI **no** quedó verde: `v2.12-beta` y `v2.22-beta`). Estado auditado `main == f47e0ceb` == tag `v2.35-beta`. Documento de solo lectura: no toca código, no añade migración.
106.  **V2.35.1 — ESTUDIO hard gate (P1-01 de la auditoría externa de v2.35-beta) (2026-09-11):** cierra el único hallazgo **P1** de la auditoría: el AUTO podía **abandonar ESTUDIO** y operar la allowlist CSV como universo cuando ESTUDIO fallaba, estaba vacío o el resolver no existía. Nuevo contrato: **ESTUDIO es obligatorio para AUTO; la allowlist solo intersecta; nunca lo sustituye**. En `_instruments_for_cycle` (`auto_orchestrator_worker.py`), los **cinco** caminos que devolvían la allowlist (resolver ausente, excepción, `resolution is None`, `status != "ok"` —cubre `unavailable`/`empty`/desconocido— e `instrument_ids` vacío) ahora devuelven `()` ⇒ **no se opera** (fail-closed: el worker sigue vivo y reintenta). Se conserva la intersección `ESTUDIO ∩ allowlist` cuando ESTUDIO es `ok`. Sin vía de escape hermética (también estricto sin `resolve_universe`). Docstring del worker y comentario de `orchestrator_universe.py` corregidos (dejaban de llamar "fallback" a la allowlist y contradecían el comportamiento). Tests nuevos: `test_unavailable_estudio_never_falls_back_to_allowlist`, `test_estudio_empty_never_falls_back_to_allowlist`, `test_estudio_error_never_falls_back_to_allowlist` y `test_loop_does_not_operate_when_estudio_unavailable_with_allowlist`; se retira el que certificaba el fallback y se migran los dobles de test a un universo ESTUDIO real. **Bump `1.60.0-beta` → `1.60.1-beta`** (CHANGELOG). Alembic head `035_paper_forward_evidence` **sin migración**; invariantes `AUTO ⇒ SIMULATED` / LIVE / fail-closed intactas. **Además, en la misma versión se resuelven los tres P2 de la auditoría** (cada uno en rama aislada + integración): **P2-01** separa la Promotion Gate automática (`decide_promotion`/`evaluate_promotion`, sin override) de la admin (`decide_admin_promotion`/`evaluate_admin_promotion`, única con `shadow_validated`), elimina `OrchestratorDeps.shadow_override` y el parámetro `shadow_validated` de `run_cycle` (H1 intacto); **P2-02** separa `CycleGrammarCounters` (por ciclo) de `GrammarObservabilityCounters` (proceso) y añade la línea `process_summary`; **P2-03** introduce `DiscoveryBudgetAllocator` explícito por carriles (catálogo / gramática simple / compuesta / adaptive placeholder, reparto determinista por resto mayor) que sustituye el sesgo de orden de `_grammar_reserve` (gramática OFF byte-idéntica a A13). La integración destapó y corrigió un gap de A14: `RunSmaGridOptimizeAndSave.execute` no reenviaba `grammar_variants` (el allocator da cupo real a la gramática, que ahora sí llega al LAB). Verificación: ruff + lint-imports (4/4) + mypy (470) verdes; 1490 tests domain+application+worker; 17/17 PG con `*_PG_REQUIRED=1` y 0 skips.
107.  **V2.36 — Strategy Intelligence adaptativa (incremento 1: carril `adaptive`) (2026-09-11):** [relevo v2.36](./traspaso-relevo-v2-36-adaptive-allocator-2026-09-11.md) — primer incremento del bucle `evidence → aprender → ajustar búsqueda`: activa el carril `adaptive` del `DiscoveryBudgetAllocator` (hasta ahora peso `0.0`, hueco semántico) con pesos derivados de **evidencia real ya persistida del LAB**, de forma determinista, auditable y fail-closed. El aprendizaje **no ocurre en el motor**: se materializa en un `DiscoveryEvidenceSnapshot` inmutable y versionado (`math_version` `discovery_evidence_v0`), calculado **fuera del hot path** por un job batch/CLI y **inyectado** como dependencia, de modo que el discovery sigue siendo una **función pura dada `(instrument_id, snapshot)`**. **Dominio**: entidad `DiscoveryEvidenceSnapshot` + contrato `DiscoveryEvidenceSnapshotRepository` (`save`/`get_latest`/`get_by_hash`/`list_recent`) + lectura agregada `family_evidence_summary` (por `preset_key`, orden canónico) en el protocolo de trials. **Aplicación**: `bolsa_application/discovery_evidence.py` con builder determinista (`build_discovery_evidence_snapshot`, `compute_family_weights`, `compute_lane_weights`, `snapshot_hash`), redondeo fijo, orden canónico y hash estable; **fail-closed** por muestra mínima por familia y total (sin evidencia ⇒ `0.0` explícito), peso acotado a `[0, 0.5]` contra multiple testing. **Persistencia**: migración aditiva `036_discovery_evidence_snapshots` (`down_revision=035_paper_forward_evidence`), tabla con `snapshot_hash` único, `math_version`, ventana y `payload` JSONB, `downgrade()` completo, sin backfill; repo inmutable por hash (`save` idempotente). **Job batch/CLI** `apps/api-python/scripts/build_discovery_evidence_snapshot.py`: idempotente de verdad por hash (corte `window_to` = `created_at` del trial más reciente, dato-dependiente, no reloj) con `--dry-run`. **Worker**: flag `AUTO_ORCHESTRATOR_ADAPTIVE_ALLOCATOR` **OFF por defecto** (con OFF, byte-idéntico a v2.35.1: no lee BD ni altera el allocator); con ON lee el snapshot vigente **una vez por ciclo** (`_refresh_adaptive_snapshot` + `_make_adaptive_snapshot_provider`, una sesión por operación) y lo inyecta en el allocator de todos los instrumentos del ciclo (fail-closed: sin snapshot ⇒ `0.0`, nunca el env por accidente). **Alcance explícito**: el incremento 1 asigna **cupo real y observable** al carril adaptativo; NO añade espacio de búsqueda nuevo ni emisión adaptativa (incremento 2). Catálogo y gramática simple/compuesta intactos. Tests: determinismo, orden-insensibilidad y reproducibilidad del hash, fail-closed, cotas, cupo adaptativo sin romper el global (`test_discovery_evidence.py`, 17); worker flag/injection/una-lectura-por-ciclo/fail-closed; PG tabla+índices, `save` idempotente, `get_latest`/`get_by_hash`, agregación por familia y roundtrip `up/down` de la `036`. Verificación local: ruff + lint-imports (4/4) + mypy (474) verdes · offline **1518 passed** (domain + application + worker) · PG con gates `A14_GRAMMAR_PG_REQUIRED`/`LIFECYCLE_PG_REQUIRED`/`AUTO_ORCHESTRATOR_PG_REQUIRED` **10 passed** + snapshot PG **6 passed** + lifecycle **57 passed** (anti-explosión `len(plans) == 1784` intacto) · Alembic head `036_discovery_evidence_snapshots` · invariantes `AUTO ⇒ SIMULATED` / LIVE / H1 / H2 / fail-closed intactas. **Bump `1.60.1-beta` → `1.61.0-beta`** (CHANGELOG). **ELEVADA**: `main == cb147d89` == tag **`v2.36-beta`**, Release-tag CI **GREEN verificado** (run `34604803938`, `success`); audit-pack de fase [`audit-pack-v2.36-adaptive-allocator-2026-09-11.md`](./audit-pack-v2.36-adaptive-allocator-2026-09-11.md).
108.  **V2.37 — Hardening de V2.36 (P2-01/02/03) + Adaptive Discovery Generation (2026-09-11):** [relevo v2.37](./traspaso-relevo-v2.37-hardening-adaptive-generation-2026-09-11.md) · [audit-pack](./audit-pack-v2.37-hardening-adaptive-generation-2026-09-11.md) — segundo incremento de Strategy Intelligence: cierra los **tres P2** de la auditoría externa de `v2.36-beta` y pasa de "cuánto presupuesto recibe una familia" a **"qué hipótesis merece explorarse"**, sin relajar gates y con el aprendizaje fuera del hot path. **P2-01 (evidencia rica)**: la v0 saturaba el score en 1.0 (`clamp(avgScore,0,1) × success_ratio`); nueva señal compuesta `math_version=discovery_evidence_v1` con componentes monótonos (`1-exp(-x)` para `is_score`, `tanh` para Sharpe, `1-exp(-(pf-1))` para profit factor, `exp(-dd/50)` para drawdown, evidencia posterior ponderada por nivel ADR-012) combinados por media ponderada con **_shrinkage_ por cobertura** hacia un ancla neutral 0.5 (métrica ausente no puntúa 0 ni premia la ausencia); `family_evidence_summary` agrega Sharpe/PF/drawdown medios con `metricCoverage` explícita y nunca inventa ceros; nuevo `posterior_evidence_summary` agrega shadow/paper forward por familia (join por `trial_id` en `research_evidence`). La v0 se conserva reproducible (`--math-version discovery_evidence_v0`). **P2-02 (política exploración/explotación)**: `DiscoveryBudgetAllocator.exploration_floor_ratio` (default 0.5) reserva una fracción mínima del presupuesto de candidatas a los carriles exploratorios (catálogo + gramática) que el carril `adaptive` **nunca** puede absorber — materializa "champion cannot teach itself"; con ratio 0 el reparto es el histórico de v2.36 (test de regresión). **P2-03 (freshness + fingerprint)**: `DiscoveryEvidenceSnapshot` gana `evidence_fingerprint` (huella del research dataset agregado, determinista y sin reloj) y `is_fresh(now, max_staleness_days)`; documenta las **tres identidades** (`snapshot_hash`=conocimiento, `id`=instancia, `created_at`=persistencia — "más nuevo" ≠ "más reciente en conocimiento"); migración aditiva **`037_discovery_evidence_freshness`** (columna nullable, sin backfill, `downgrade()` completo); el worker descarta por **fail-closed** un snapshot `stale` (`AUTO_ORCHESTRATOR_ADAPTIVE_MAX_STALENESS_DAYS`, default 30; `0` desactiva). **Incremento 2 (Adaptive Discovery Generation)**: nuevo módulo `bolsa_application/discovery_search_policy.py` (`SearchPolicy` determinista y versionado `discovery_search_policy_v0`) que convierte el prior por familia en **cuotas de emisión por familia** con dos tramos (explotación top-k por peso + exploración uniforme garantizada, `exploration_ratio` default 0.25); el carril `adaptive` con cupo **emite candidatas reales** en `strategy_discovery_engine` (prefijo `ADAPTIVE_FAMILY_PREFIX`), reutilizando el catálogo curado (sin espacio de búsqueda nuevo), determinista (`(instrument_id, snapshot, budget)` ⇒ mismas candidatas) y fail-closed (sin política ⇒ carril no emite, byte-idéntico a v2.36). Flag **`AUTO_ORCHESTRATOR_ADAPTIVE_GENERATION` OFF por defecto**; el provider de snapshot se cablea si cualquiera de los dos flags está ON. Observabilidad aditiva: `DiscoveryEmissionSummary` gana `adaptive_candidates`/`adaptive_policy_hash`/`adaptive_exploration_quota`/`adaptive_families`; contadores de ciclo/proceso suman `adaptive_candidates`/`adaptive_discoveries`. Granularidad progresiva: el payload publica el hueco aditivo `familyGranularity` (régimen / región de parámetros / clase de instrumento), sin entrar en el hash. Tests nuevos: `test_discovery_search_policy.py` (determinismo, fail-closed, exploración garantizada, emisión adaptativa dentro del presupuesto global y determinista, histórico sin política); v1 sin saturación, cobertura neutral, drawdown/PF/posterior, fingerprint y suelo de exploración en `test_discovery_evidence.py`; worker freshness fail-closed/staleness 0/flag de generación; PG métrica rica + cobertura, posterior por familia, fingerprint persistida y roundtrip `037`. **Fix de deuda CI (`13020eeb`)**: los tests anteriores existían pero no estaban cableados en ningún job (los workflows enumeran ficheros); se incorporan a `python-offline` y a `grammar-discovery-pg`/`lifecycle-pg`. Verificación local: ruff + lint-imports (4/4) + mypy **475** verdes · offline **1547 passed** · PG discovery/snapshot **12 passed** + snapshot detallado **10 passed** + lifecycle **6 passed** (anti-explosión `len(plans) == 1784` intacto) · job batch `--dry-run` OK (`mathVersion=discovery_evidence_v1`, fingerprint presente, `adaptive=0.0` sin evidencia) · Alembic head `037_discovery_evidence_freshness` · con ambos flags OFF, byte-idéntico a v2.36 · invariantes `AUTO ⇒ SIMULATED` / LIVE / H1 / H2 / fail-closed intactas. **Bump `1.61.0-beta` → `1.62.0-beta`** (CHANGELOG). **ELEVADA**: `main == 13020eeb` == tag **`v2.37-beta`**, Release-tag CI **GREEN verificado** (run `34619419865`, `success`, 10/10 jobs requeridos + `certify`).
109.  **V2.38 — Granularidad por región de parámetros (incremento 3) (2026-09-11):** [relevo v2.38](./traspaso-relevo-v2.38-param-region-2026-09-11.md) · [audit-pack](./audit-pack-v2.38-param-region-2026-09-11.md) — tercer incremento de Strategy Intelligence: la evidencia adaptativa deja de agregarse solo por **familia H0** (`preset_key`) y pasa a granularidad por **región de parámetros** dentro del grid de la familia. **Decisión explícita de alcance**: régimen e instrument class quedan **fuera** porque **no existen como dato persistido** (el régimen solo vive en memoria en `bolsa_analytics.cognitive.market_state`; `instruments.type` es un enum de un único valor y `sector` es texto libre vacío); incorporarlos exige primero persistirlos como fuente de verdad. **Núcleo puro**: nuevo `bolsa_application/discovery_param_region.py` (`math_version=discovery_param_region_v0`) con `param_region_for_point` (ordinal en el producto cartesiano determinista + hash corto de los valores), `compose_granularity_key`/`split_granularity_key` (clave compuesta canónica `familia` o `familia|region`) y `param_region_label`; **fail-closed** (punto fuera del grid ⇒ sin región, no se aproxima), sin LLM/red/BD. **Persistencia por trial**: migración aditiva **`038_research_trials_param_region`** (columna `param_region String NULL`, sin backfill, `downgrade()` completo, guards idempotentes); write-path completo — el motor etiqueta la candidata (`discovery_param_region`) en los carriles catálogo y adaptive, el runner la propaga (`_REGION_KEYS`, antes se descartaba) y `optimization_runs` la persiste (`ResearchTrial.param_region`, entidad + Protocol + repo SQL + modelo de tabla). **Agregación por clave compuesta**: `family_evidence_summary` y `posterior_evidence_summary` agrupan por `preset_key + param_region` y devuelven `paramRegion`; el job batch mergea por clave compuesta; **retrocompatible** (todo `NULL` ⇒ clave = familia ⇒ `snapshot_hash` **idéntico** a v2.37). **Snapshot**: `compute_family_weights` mintea la clave compuesta, `familyGranularity` deja de estar vacío y publica `{family, paramRegion}` reales (aditivo, **fuera del `snapshot_hash`**, dentro de `evidence_fingerprint`). **Search policy**: sube a `discovery_search_policy_v1` e incluye `granularityKeyVersion` en su hash/metadata (la fórmula de reparto no cambia: ya era agnóstica a la clave). **Motor**: resuelve la clave compuesta (`split_granularity_key`) y **filtra `param_points()` a la región** con el helper `_points_for_region`; clave desconocida o región inexistente ⇒ **no emite** (fail-closed). **Rollout**: flag **`AUTO_ORCHESTRATOR_ADAPTIVE_PARAM_REGION` OFF por defecto**; con OFF el worker colapsa las claves compuestas a familia (`_collapse_regions`) y el sistema es **byte-idéntico a v2.37**. Observabilidad aditiva: `DiscoveryEmissionSummary` gana `adaptive_region_emissions`/`adaptive_region_count`; contadores de ciclo/proceso suman `adaptive_region_emissions` y los logs reportan `adaptive_regions=N/M`. Tests nuevos: `test_discovery_param_region.py` (determinismo, estabilidad ante reordenación, fail-closed, ida y vuelta de la clave compuesta, anti-explosión intacta); `test_discovery_evidence.py` (claves compuestas aíslan regiones, hash estable y sensible a la región, `familyGranularity` poblado/vacío, coexistencia histórica+regionada); `test_discovery_search_policy.py` (claves compuestas + `granularityKeyVersion`, filtrado por región, región desconocida fail-closed); worker (flag de región OFF/ON, `_collapse_regions`); PG (`param_region` + roundtrip, agregación por región, posterior por clave compuesta, roundtrip `038`). Verificación local: ruff OK (ficheros tocados; el repo arrastra deuda preexistente fuera de alcance) · mypy Success en los módulos tocados · offline **1703 passed** · PG con gates `A14_GRAMMAR_PG_REQUIRED=1` **14 passed** · anti-explosión `len(plans) == 1784` intacto (solo se etiqueta, no se añade espacio de búsqueda) · job batch `--dry-run` OK · sin región, `snapshot_hash` **idéntico** a v2.37 · `lint-imports` **no ejecutable** en la máquina de la sesión (Windows Application Control, `os error 4551`): los 4 contratos revisados manualmente contra `.importlinter` · Alembic head `038_research_trials_param_region` · invariantes `AUTO ⇒ SIMULATED` / LIVE / H1 / H2 / fail-closed intactas. **Bump `1.62.0-beta` → `1.63.0-beta`** (CHANGELOG).
110.  **V2.38.1 — Hotfix de los 2 P2 de la auditoría de V2.38 (2026-09-11):** [audit-pack](./audit-pack-v2.38.1-hotfix-audit-2026-09-11.md) — sellado de `v2.38-beta` (commit `41b96a41`, Release-tag CI GREEN) y **auditoría externa** de esa versión (8,9/10; P0 = 0, P1 = 0, **P2 = 2**, P3 = 2). **P2-01 (equivalencia real)**: el write-path etiquetaba `discovery_param_region` **siempre, sin consultar el flag**, de modo que con OFF el snapshot contenía regiones y `_collapse_regions` colapsaba con `max(peso)` — no equivalente a la fuerza sobre el agregado familiar (divergencia medida ≈ **7,4 %**: `0.747` vs `0.803`; el snapshot solo persiste `family_weights`/`sample_sizes`, así que el colapso no puede recomputarla). **Fix**: el motor recibe la decisión como dependencia inyectada (`discover_for_instrument_with_summary(..., emit_param_region: bool = True)`) y el worker pasa `adaptive_param_region_enabled()`; con OFF **no se genera región** ⇒ evidencia idéntica a V2.37 y colapso no-op (equivalencia **real**, no aproximada). `_collapse_regions` se conserva como puente para **evidencia histórica** ya persistida con región (transición ON→OFF), con docstring corregido. **P2-02 (determinismo del fingerprint)**: `evidence_fingerprint` ordenaba solo por `presetKey` (orden entre regiones de una misma familia a merced del orden de entrada); ahora ordena por clave compuesta. **P3**: `MATH_VERSION_SEARCH_POLICY_V0`/`_V1` coexisten (antes `_V0` valía `..._v1` reescrito in-place, irreproducible) + alias `MATH_VERSION_SEARCH_POLICY`; y el test tautológico del `snapshot_hash` se documenta y refuerza. Tests nuevos: `test_emit_param_region_false_omits_region_key`, `test_emit_param_region_true_labels_region`, `test_emit_param_region_false_is_byte_identical_to_v237_params`, `test_fingerprint_is_order_insensitive_across_regions`, `test_fingerprint_is_order_insensitive_across_families_and_regions`, `test_math_versions_are_distinct_and_reproducible`, `test_region_policy_uses_v1_math_version`, `test_runner_passes_emit_param_region_false_when_flag_off` (+ `_true_`). Verificación local: ruff con **invocación exacta de CI** (`--config pyproject.toml`) `All checks passed` · mypy Success · offline **1497 passed** (suites relevantes). Sin migración nueva (Alembic head sigue en `038`). **Bump `1.63.0-beta` → `1.63.1-beta`**.
111.  **Lección de proceso (sellado v2.38):** la verificación local de `ruff` debe usar **la misma invocación que CI** (`--config pyproject.toml` con los mismos paths): el root de resolución altera la clasificación first/third-party de los paquetes `bolsa_*` y 10 `I001` pasaron desapercibidos en local. El primer tag del sellado falló en CI por esto; se corrigió con amend **solo-imports** (verificado línea a línea en `17ea3332 → 41b96a41`), force-push y re-tag.
112.  **V2.39 — Régimen de mercado por trial (incremento 4) (2026-09-11):** [relevo v2.39](./traspaso-relevo-v2.39-regimen-por-trial-2026-09-11.md) · [audit-pack](./audit-pack-v2.39-regimen-por-trial-2026-09-11.md) — cuarto incremento de Strategy Intelligence: la evidencia gana la segunda dimensión de granularidad que V2.38 dejó apuntada. **Decisión de fuente**: el régimen se deriva de las **propias barras del trial** (as-of, determinista), NO del régimen macro cognitivo, porque `bolsa_analytics.cognitive.market_state` se alimenta de `fetch_macro_snapshot_dict` (valores _live_ de Yahoo, `date.today()`, `closes[-1]`) sin serie histórica persistida: etiquetar un trial pasado con el régimen de hoy sería inventar dato. **Núcleo puro**: nuevo `bolsa_application/discovery_market_regime.py` (`math_version=discovery_market_regime_v0`), un solo eje `trend_up`/`trend_down`/`range`/`high_vol` (tendencia por pendiente normalizada por volatilidad; volatilidad por rango relativo medio high-low-close; `high_vol` prioritario; **fail-closed** con `<60` barras, NaN/inf, high-low ausentes o ventana degenerada ⇒ `""`). **Persistencia**: migración aditiva **`039_research_trials_regime`** (`regime String NULL`, sin backfill, `downgrade()` completo); `OptimizeSmaGridResult.regime` calculado en los 4 constructores del dataclass; write-path completo (`ResearchTrial.regime`, Protocol, repo SQL, `_regime_for_trial` fail-closed). **Agregación**: `family_evidence_summary`/`posterior_evidence_summary` amplían `GROUP BY` con `regime`; snapshot publica `regimeGranularity` (fuera del `snapshot_hash`, dentro del `evidence_fingerprint` ordenado por `(presetKey, paramRegion, regime)`) y `regimeCounts`. **El régimen es dimensión paralela**: la clave `familia|region` y `_collapse_regions` **no se tocan**. **Rollout**: flag `AUTO_ORCHESTRATOR_ADAPTIVE_REGIME` **OFF por defecto** con `emit_regime` fijado por el composition root (una candidata no puede encenderlo); con OFF no se calcula régimen ⇒ evidencia idéntica a V2.38.1. Tests: `test_discovery_market_regime.py` (17, hermético), 8 en `test_discovery_evidence.py`, 6 en `test_optimization_runs.py`, 6 en `test_auto_orchestrator_worker.py`, 4 PG (roundtrip + agregación + posteriores + migración 039). Verificación local: ruff con **invocación exacta de CI** `All checks passed` · mypy Success · offline **212 passed** (suites V2.39) · PG 18 passed. Cableado en `python-ci.yml` y `release-tag-ci.yml`. **Bump `1.63.1-beta` → `1.64.0-beta`**; Alembic head `039_research_trials_regime`.
113.  **V2.39.2 — Cierre de flaky: el ledger se secuencia por estado, no por reloj (2026-09-13):** [audit-pack](./audit-pack-v2.39.2-cierre-flaky-2026-09-13.md) — segunda pasada de la auditoría interna, centrada en los **flaky** residuales de `v2.39.1`. Al instrumentar forensemente el entrelazado de `executed_at` cuando la cadena `balance_after` rompía, el «flaky» resultó ser un **P1 real de producción**: el `executed_at` del ledger se derivaba de `datetime.now()`, que bajo concurrencia **no ordena** (dos transacciones serializadas por el `with_for_update` de la cartera pueden leer el reloj en orden invertido respecto al commit, y el consumidor que ordena por `(executed_at,id)` reconstruye una secuencia falsa — el desempate por `id` es un **UUID v4 aleatorio**, no rescata el orden real). **Evidencia**: se capturó el entrelazado donde el salto entre dos asientos consecutivos **no coincidía con su `amount`**. **Fix**: `SqlAlchemyLedgerRepository.next_executed_at(account_id)` deriva el instante del **estado persistido** (`max(now, último + 1µs)`, leído en la misma transacción del llamador, ya bajo el lock de la cartera) ⇒ estrictamente creciente con el orden de aplicación. Conectado en las **cuatro** rutas de asiento: trade, custodia, depósito y retiro. **Segundo P1**: `ApplyCustodyFees` calculaba `balance_after` con cash **PRE-lock** (bug simétrico al que `ExecuteTrade` ya había corregido en EXEC-B-CONC) — ahora toma el cash **POST-lock** que `deduct_cash` ya devolvía, en las dos ramas (PENDING y periodo actual). **Hallazgo de entorno**: el «pool amplio» del chaos (`pool_size=64`) agotaba el `max_connections=100` del PostgreSQL local al convivir con otros engines, produciendo `FATAL: sorry, too many clients already` **en ráfaga** — un error de entorno que se leía como «flaky de concurrencia» y **enmascaraba el veredicto**; acotado a **24** (el escenario serializa igual sobre la fila de cartera y un pool grande no aceleraba nada): **0/10 fallos** y ~38 s. **Honestidad de test**: `test_two_workers_claim_disjoint_unknown_batch` hacía `gather` de dos `claim_unknown_batch` con **rollback inmediato** y exigía disjunción — eso **refutaba** la exclusión mutua en vez de certificarla (dependía del entrelazado del event loop); reescrito para retener las **dos** transacciones abiertas y verificar la propiedad real del lease (el segundo obtiene **vacío**; tras liberar, el relevo cubre el lote). **A9**: el bucle antirrecompra esperaba «a que ocurra una re-compra», pero el camino correcto es que **nunca** ocurra ⇒ agotaba el plazo en cada pasada (~518 s); ahora usa un sondeo **corto y acotado** (`_RESTART_WATCH_S = 20 s`) y los tres bucles comprueban `proc.poll()` para fallar al instante con el log del subproceso ⇒ **~28 s** y **8/8** en verde. Tests nuevos: `test_trade_usa_el_secuenciador_del_ledger_no_el_reloj`, `test_secuenciador_es_estrictamente_creciente` (**verificados por mutación**). Verificación local: ruff `All checks passed` · import-linter **4 contratos OK** · mypy **272 ficheros** Success · aplicación **1470 passed** · infraestructura **137 passed, 1 xfailed** · recovery+idempotencia **8 passed** · chaos **10/10** · A9 **8/8**. Sin migración nueva (Alembic head sigue en `039`). **Bump `1.64.1-beta` → `1.64.2-beta`**.
114.  **Deuda de cobertura de CI detectada en el cierre v2.39.2:** el job `quality` de `python-ci.yml` corre una **lista explícita** de tests, no las suites completas; en particular **no ejecuta** `packages/py/infrastructure/tests/chaos/test_load_concurrency_flow.py` (el escenario de estrés que cerró el bug del ledger), ni `test_live_order_recovery_concurrency_pg.py`, ni el A9 por proceso — todos requieren PostgreSQL. El fix del ledger sí queda cubierto en CI por la suite de aplicación (1468 passed) y por la regresión pura del secuenciador, pero la **certificación de concurrencia se valida localmente** contra la DB aislada `bolsa_v1_chaos`. Se documenta como deuda honesta para la auditoría externa.
115.  **V2.39.2 — Tercera pasada: la gramática no emite planes sin evidencia, y la tabla outbox no envenena suites (2026-09-14):** [arranque auditor](./arranque-auditor-v2-39-2-cierre-flaky-tercera-pasada-2026-09-14.md) · [audit-pack](./audit-pack-v2.39.2-cierre-flaky-2026-09-13.md) · tag **`v2.39.2-beta`** (código `9444b364`, certificado GREEN; re-sellado para incluir los docs de auditoría). La **batería exacta del CI** destapó tres hallazgos que **no** eran flaky. **(1) P1 — gramática inoperable (tres causas, medidas):** el **100 % de los 1784 planes** producía **<2 columnas operables** (1184 con 1, 600 con 0), así que el `PBO CSCV` no tenía matriz y `robustness`/`walk_forward` quedaban **sin evidencia en silencio** (misma ruta que el orquestador real ⇒ bug de producción). _Causa 1:_ trigger y trend filter Donchian usaban `line="upper"` con `gt`, y `close > upper` es **matemáticamente imposible** (el canal incluye la barra actual ⇒ `max(high) ≥ close`): **0 disparos incluso en serie estrictamente creciente**; pasa a la **banda media** como el preset `donchian_breakout` de producción. _Causa 2:_ el eje de permutación (introducido por el `P2-01` previo) podía permutar el **trigger** dejando el **exit homónimo** mirando las mismas EMAs — un cruce alcista y otro bajista de las mismas series **no coinciden nunca** ⇒ trigger inalcanzable; ahora el exit homónimo se permuta **con** el trigger por par (`_AXIAL_TRIGGER_EXIT_PAIRS`) y el eje rota **preferentemente** sobre los bloques opcionales. _Causa 3:_ `trigger_ema10_cross_ema50` + `trend_ema20_gt_ema50` son **estructuralmente incompatibles** (el cruce de EMA10 precede necesariamente a la confirmación de EMA20, y los gates se exigen simultáneos): 210 barras cumplen ambas, **0 cruces**; dos vetos de inanición **deterministas y fail-closed** (`_mutually_unreachable_trigger_exit`, `_conjunctive_ema_starvation`) los retiran de la enumeración. **Resultado: 1684 planes, 0 degenerados**; el `1784` histórico deja de ser el invariante (lo es el **techo del `GrammarBudget`** y que ninguna candidata quede sin evidencia posible). La serie de integración del test (rampa donde `close > sma200` eran **0 barras**) se reescribe con ciclos de tramo alcista > período del canal y retrocesos que cruzan las EMAs: el test pasa de **fallar a los 146 s** a **5,8 s**. Regresión: `test_grammar_variants_produce_at_least_two_operable_columns`, `test_grammar_donchian_trigger_is_reachable`. **(2) P1 — lista con instrumentos imborrable:** `SqlAlchemyListRepository.delete` borraba `instrument_lists` sin vaciar `instrument_list_items`, y la FK `list_id` **no** es `ON DELETE CASCADE` ⇒ `DELETE /api/lists/{id}` devolvía **500 en vez de 204** (`ForeignKeyViolation`); ahora los items se borran en la **misma transacción** justo antes. Regresión: `test_delete_list_with_items_does_not_violate_fk`. **(3) P2 — `lifecycle_outbox` envenenaba suites:** `claim_batch` es una barrida **global** (FIFO por posición, **sin filtrar por posición**) con sanitizado de huérfanas; `test_financial_integrity_pg` dejaba una cabeza FIFO `dead` sin limpieza y otras suites filas `pending`/`processing`, y el worker de `test_lifecycle_outbox_worker_pg` reclamaba filas **ajenas** consumiendo el **único** disparo de su hook inyectado antes de que su propia fila pasara a `processing` (`status='applied' expected='processing'`). **Fix:** teardown en `finally` + aislamiento de la tabla (purga `pending`/`processing` antes y después, sin tocar `applied`/`dead`). **(4) P3 — gate de CI fantasma:** el job `grammar-discovery-pg` declara `A14_GRAMMAR_PG_REQUIRED=1` pero el test **no leía ese gate** ⇒ podía quedar `skipped` con el job **en verde**; un step nuevo inspecciona el log (`-rs`) y **falla** si algo quedó `SKIPPED`, independientemente de qué gate lea cada test. **Verificación:** ruff `All checks passed` · mypy **477 ficheros** Success · batería **exacta del job `quality`** `1306 passed, 0 failed` · gramática + A14 `40 passed` · `apps/api-python/tests` + `packages/py/infrastructure/tests` **521 passed × 2 pasadas consecutivas** (antes 2 failed en cada intento). **CI de GitHub GREEN** en los 5 workflows de `main` `9444b364` (`quality`, `lifecycle-pg`, `paper-forward-pg`, `grammar-discovery-pg` los cuatro `success`). Sin migración nueva (Alembic head `039`).
116.  **V2.39.3 — Cierre P1/N1 (lock de cuenta) + P2/N2 (secuenciador forzado) + fix `totalSamples` (2026-09-15):** [audit-pack](./audit-pack-v2.39.3-cierre-ledger-2026-09-15.md) — cierra los hallazgos de la **auditoría interna sobre `v2.39.2-beta`**. **(1) P1/N1 — lock de cuenta:** `next_executed_at` era por cuenta pero el lock que lo protegía era de **cartera** (`with_for_update` sobre `PortfolioRow`), de modo que dos carteras de la misma cuenta no se excluían y podían emitir `executed_at` **duplicado**; nuevo `SqlAlchemyAccountRepository.lock_account(account_id)` (`SELECT ... FOR UPDATE` sobre `investment_accounts`) cableado como lock externo antes del lock de cartera en `trade.py`, `cash.py` (deposit+withdraw) y `custody.py` (orden determinista cuenta → cartera). **(2) P2/N2 — secuenciador forzado:** se elimina el parámetro `executed_at` de `append_trade`/`append_fee`/`append_custody_fee`/`append_cash_movement`, que ahora secuencian internamente con `next_executed_at` (la secuencia deja de ser disciplina del caller); en `trade.py` se quitan `_ledger_ordering`/`_FEE_ORDER_GAP` y la llamada manual al secuenciador. **(3) Auditoría 2 — `totalSamples` inflado:** `compute_lane_weights()` y el payload sumaban `sample_sizes.values()` sin filtrar (contaban familias descartadas por `min_samples`); nuevo `_effective_total_samples()` filtra por `family_weights` y unifica puerta de decisión con lo publicado. **(4) Test de concurrencia multi-portfolio:** `test_multi_portfolio_ledger_sequence.py` (chaos, PG real) — 2 carteras de la misma cuenta, ráfagas concurrentes, `executed_at` estrictamente creciente y cadena `balance_after` encadenada. **(5) Nota `custody_obligation`/`custody_obligations`:** coexisten (la 006 no borra la 005); la lista del script de limpieza es correcta, **no es fallo**. Sin migración nueva (Alembic head `039`).
117.  **V2.40.5/AUTO-1A — Position Materialization & Partial-Fill Integrity (2026-09-17):** [audit-pack](./audit-pack-v2.40.5-auto-1a-position-materialization-2026-09-17.md) · [arranque auditor](./arranque-auditor-v2-40-5-auto-1a-2026-09-17.md) — **bump `1.65.4-beta` → `1.65.5-beta`; sin migración (Alembic head `041_unique_natural_keys`)**; commit de fase `d15f0a18` (26 ficheros, `+3538/−120`) y tag anotado **`v2.40.5-beta` → `d15f0a18`**, con `Python CI` **GREEN** en `main` (run `35194186488`, 5/5) y `Release-tag CI` **GREEN** (run `35194658271`, 10 jobs requeridos + `certify`). Cierra el **P0** de contabilidad de posición que señalaron las dos auditorías de `v2.40.4-beta`: el AUTO contabilizaba la cantidad **pedida**. Ahora **`POSITION = Σ APPLIED`** (read-model puro `PositionLedger` con `MeasurementStatus` y la sobreventa como violación explícita, sin descartar en silencio una fila ilegible), `_settle` separa aplicado de no aplicado (`_Settlement(applied, unapplied, requested_qty)`, modo _structural_ en el camino sin `finance_applier`), el exit se clampa contra la posición **materializada** (`applied_qty <= held` ⇒ `exit_qty_over_position`), la cola de un llenado parcial queda como **capital en `RETRY`** (`fill_not_materialized`/`fill_partially_materialized`/`fill_unapplied`) y la autoridad canónica tras un reinicio se reconstruye desde Σ `APPLIED` (`REBUILT`; libro ilegible ⇒ `POSITION_PROJECTION_UNKNOWN` ⇒ aperturas vetadas y salidas protectoras permitidas). Skips de gestión con rastro (`PositionManagerSkip` + `auto_position_skip` con `attention="high"`). **El «flake» del scheduler declarado en `v2.40.4` queda CERRADO**: era el test detectando un bug de dinero (el helper de equity sumaba contexto de fills no aplicados como realizado) ⇒ el bucle ×30 con PG real da **0 fallos**; se corrige la frase «criterio: re-ejecutar el job» en `PROJECT_STATE`. Mutaciones medidas: contabilizar la pedida ⇒ **6 rojos** · tratar los no aplicados como conocidos ⇒ **1** · quitar el filtro `APPLIED` del helper de equity ⇒ **1 determinista** (**0/30** en el bucle) · quitar el clamp ⇒ **1** · skips a `None` ⇒ **2**. Deuda declarada: liberación explícita del capital en `RETRY` e **índice parcial `execution_events(account_id, status)`** ⇒ `AUTO-1` (Portfolio Reservation Engine).
118.  **V2.41/AUTO-1 — Portfolio Reservation Engine (2026-09-17):** [audit-pack](./audit-pack-v2.41-auto-1-portfolio-reservation-2026-09-17.md) · [arranque auditor](./arranque-auditor-v2-41-auto-1-portfolio-reservation-2026-09-17.md) — **bump `1.65.5-beta` → `1.66.0-beta`; migración nueva `042_portfolio_reservations` (head `041_unique_natural_keys` → `042_portfolio_reservations`)**; commit de fase [`e6b0dd5b`](https://github.com/jvelasca/Bolsa_V1/commit/e6b0dd5b) (21 ficheros, `+4207/−97`) y tag anotado **`v2.41-beta` → `e6b0dd5b`**, con `Python CI` **GREEN** en `main` (run [`35201047304`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35201047304), 5/5) y `Release-tag CI` **GREEN** (run [`35201538048`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35201538048), jobs requeridos + `certify`). Sustituye la **reserva artesanal intra-tick** (`committed[]` + `_working_snapshot` dentro de `plan_v2_tick`) por un **motor de reservas explícito**: nuevo módulo puro `bolsa_analytics.cognitive.portfolio_reservation` (`PortfolioReservation` con identidad y **siete dimensiones** — `reserved_cash`, `reserved_risk`, `asset_exposure`, `sector_exposure`, `correlation`, `strategy_capacity`, `liquidity_capacity`; `ReservationStatus` `OPEN`/`RELEASED_BY_FILL`/`_CANCEL`/`_RESTART`/`_ROLLBACK`; `ReservationLedger` determinista con `reserve`/`release`/`release_by_fill`/`release_by_cancel`/`release_by_restart`/`rollback`/`live`/`by_sector`/`by_strategy` y **`replay`**) y `PortfolioRiskState` (`gross_risk`, `net_risk`, `reserved_risk`, `pending_risk`, `sector_risk`, `strategy_risk`, `correlation_adjusted_risk` como **cota superior** con `MeasurementStatus` fail-closed). **Coste real en el sizing**: `TradingCostModel` + `estimate_trading_cost` (comisión del calendario real de la cuenta, spread/slippage/gap en bps) derivan `ExpectedLoss`/`WorstCaseLoss`/`GapAdjustedLoss`; `compute_allocation` acepta `cost_model` (`risk_real = stop_loss + comisión + spread + slippage`, campos nuevos `risk_real`/`risk_real_pct`/`trading_cost` y cap codes `trading_cost`/`cost_unmeasured`) y **sin `cost_model` es retrocompatible**. **`plan_v2_tick`**: el libro del tick sustituye a `committed[]` y publica `V2TickPlan.reservations` + `risk_state`; cada aprobación **reserva en el mismo acto** y, si la reserva no es construible, la decisión **se degrada a veto** (`reservation_failed`); las candidatas siguientes deciden contra las reservas **vivas** (cash/riesgo/sector/exposición). Reason codes nuevos en `auto_reason_codes.py` (`reservation_created`/`_released_fill`/`_released_cancel`/`_released_restart`/`_released_rollback`/`_failed`/`_unmeasurable`/`_already_live`, agrupados en `RESERVATION_REASONS`). **Persistencia durable**: `reservation_store.py` (`ReservationStore` Protocol + `InMemoryReservationStore` + `PostgresReservationStore` idempotente con `ON CONFLICT DO NOTHING` + `UPDATE`) y migración aditiva **`042`** (tabla `portfolio_reservations` + índices `(account_id, status)`, `(account_id, sector)` y `(account_id, created_at DESC)` + **el índice que faltaba** `execution_events(account_id, status)`, que cierra la deuda de `V2.40.4` §5.1 y `V2.40.5` §5.2 — se crea **plano**, no parcial, para no dejar fuera la lectura de `APPLIED`; sin backfill y con `downgrade()` completo, espejo 1:1 en `tables.py`). **Worker**: las reservas vivas son la **autoridad** de `reserved_cash`/`pending_risk` del libro pendiente (una traza de `execution_events` de un instrumento con reserva viva **no** se suma otra vez) y el productor anterior queda como **reconciliación de arranque**; liberación por fill (parcial incluida), cancelación y reinicio; vetos `reservation_already_live` (instrumento ya reservado) y `reservation_unmeasurable` (libro ilegible). **Bug real corregido en el camino**: `coerce_applied_fill_fact` solo aceptaba `applied_at` como `str`, así que **todo** hecho leído de PostgreSQL quedaba **sin fecha** (fold no determinista del `PositionLedger` y ventana imposible en la reconciliación) ⇒ normaliza `str` **y** `datetime` → ISO. **Gate**: 49 herméticos nuevos (`test_portfolio_reservation_ledger.py` + `test_portfolio_reservation.py`) + 4 PG (`test_portfolio_reservation_pg.py`: roundtrip de la migración, supervivencia al reinicio, liberación por fill parcial/total, `RETRY` como capital reservado sin doble conteo, orden muerta liberada) con gate fail-if-skipped **`AUTO_RESERVATION_PG_REQUIRED`** cableado en `auto-v2-durable-pg` (`python-ci.yml`) y `lifecycle-pg` (`release-tag-ci.yml`), más el `--ignore` en los jobs offline y la head `042` en el test de snapshot de evidencia. **Matriz de mutaciones medida**: `_working_snapshot` sin reservas vivas ⇒ **7 rojos** · liberación parcial sin escalar dimensiones ⇒ **2** · doble conteo traza+reserva ⇒ **1** · `applied_at` solo `str` ⇒ **2** (uno PG) · coste no medible asumido 0 ⇒ **1**. Verificación local: `ruff` limpio · `mypy` **486** ficheros 0 issues · `lint-imports` 4/4 · job `quality` completo (comando extraído del YAML) **exit 0** (**1858 passed**, 0 skipped) · `pytest packages/py` **2779 passed** (+1 skip de Ollama ausente y 1 xfail) ⇒ **0 rojos preexistentes** · PG: reservas durables **4**, durable+contexto+claves+snapshot **29**, proceso **2**, bucle ×30 del scheduler **0 fallos**. **Deuda declarada**: `correlation`/`strategy_capacity`/`liquidity_capacity` quedan **no medidas** en el camino del tick (⇒ `correlation_adjusted_risk` no descuenta diversificación; poblarlas es `AUTO-3`/`AUTO-4`), la tabla de reservas **crece con cada aprobación** (sin política de retención), `lease_generation` se persiste pero **no** hay lógica de lease entre procesos y el productor desde `execution_events` sigue existiendo como reconciliación de arranque. Next = **`AUTO-2` — Position Manager 2.0 (`V2.42` / `1.67.0-beta`)**.
119.  **V2.42/AUTO-2 — Position Lifecycle FSM & Real Protection (slice 2a) (2026-09-17):** [audit-pack](./audit-pack-v2.42-auto-2-position-lifecycle-2026-09-17.md) · [arranque auditor](./arranque-auditor-v2-42-auto-2-position-lifecycle-2026-09-17.md) · [relevo para 2b](./traspaso-relevo-post-v2-42-auto-2-2026-09-17.md) — **bump `1.66.0-beta` → `1.67.0-beta`; SIN migración (Alembic head sigue en `042_portfolio_reservations`: el ciclo de vida vive en el JSONB `sim_auto_positions.position_state`)**; commit de fase [`6e53294f`](https://github.com/jvelasca/Bolsa_V1/commit/6e53294f) (17 ficheros, `+2914/−99`), commit de arreglo de test [`35e38c24`](https://github.com/jvelasca/Bolsa_V1/commit/35e38c24) (1 fichero, `+59/−1`) y tag anotado **`v2.42-beta` → `35e38c24`**, con `Python CI` **GREEN** en `main` (run [`35214904914`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35214904914), 5/5) y en la ref del tag (run [`35214985401`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35214985401), 5/5) y `Release tag CI` **GREEN** (run [`35214985392`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35214985392), 10 jobs ejecutados + `certify`: **9 requeridos** más el opt-in `playwright (integrated E2E)` `skipped`); el **primer sellado** del tag (sobre el commit de fase, run `35213906948`) puso **rojo** `auto-v2-durable-pg` por la **lotería de la cola SIM** del test durable V2 (el mismo commit había pasado 5/5 minutos antes en `main`, run `35213904170`) ⇒ **flaky preexistente, no regresión**, arreglado en `35e38c24` (test-only: identidad determinista que llena) con el tag re-apuntado. Cierra el agujero que dejaron `AUTO-1A`/`AUTO-1`: el **`stop_update` que proponía la gestión se descartaba**, así que `current_stop` quedaba **congelado en el valor de nacimiento** (ni break-even ni trailing en AUTO) y un `PROTECT` colapsaba a `hold → hold_no_op` **mudo**, con una posición de stop rebasado que podía quedarse sin vender. Instala: **(1) FSM explícito, persistido y fail-closed** (`position_lifecycle.py`, puro: 13 estados — incluidos `RECONCILIATION_REQUIRED`/`PROTECTION_MISSING` —, 14 eventos, `ALLOWED_TRANSITIONS` como tabla explícita; una transición no listada **no avanza** y se journaliza, y la familia degradada **no es terminal**: un hecho observable la re-verifica por la misma tabla, con `RECONCILED` exigiendo estado resuelto explícito), aditivo sobre `PositionState` (`lifecycleState` en el JSONB; `status` sigue siendo el hecho de cantidad/break-even y pasa a ser **proyección** del FSM: cero blast radius en los ~30 lectores de `.status`); **(2) un estado no verificable degrada, nunca se lee como "sin protección"** (estado desconocido o inconsistente con el hecho de cantidad ⇒ `RECONCILIATION_REQUIRED` + `PROTECTION_MISSING`, atención alta, stop de emergencia conservado); **(3) ratchet de stop real en `R`**: `position_manager_stop_update` surface el `stop_update` (solo finito y positivo; `None`/NaN/0/negativos **no** son stop utilizable), el worker lo aplica con `apply_position_current_stop` (**H2 nunca-empeorar**) y lo **persiste aunque no haya orden** (un ratchet no vende), con **trailing en `R`** (`highWatermark − trail_distance_r × initial_risk`; anchura tight 0.75/medium 1.0/wide 1.25; **solo tras T1**; sin inventar un porcentaje del precio si falta `initial_risk`) y `apply_position_mark` como único writer del extremo favorable, que ahora **viaja** con la posición marcada y permite seguir ratcheando tras un reinicio (medido en PostgreSQL real); **(4) ningún `PROTECT` mudo**: se journaliza el ratchet aplicado, el rechazado por empeorar, la petición sin stop utilizable (`protect_requested`) y la transición rechazada; **(5) un solo motor de protección** (`protection_compat.py`, modos declarados `pct` — compat flag-off reproduciendo los umbrales `v2.39.x` — y `r` — V2, delegando en analytics — más la traducción del vocabulario legacy al del FSM; `ProtectionConfig` queda como **value object** que delega, no como motor); **(6) una sola política T1/T2** en los dos caminos AUTO (`resolve_exit_policy(template_id)`, MODERATE 0.3/0.3: antes `template_id=None ⇒ policy=None ⇒` fallback 0.5/1.0 y los caminos cerraban T1 con fracciones distintas); y **(7) ninguna salida protectora se veta** por reconciliación degradada o medición incompleta (el límite queda declarado: **tomar beneficio sí espera** al veredicto). **Matriz de mutaciones medida** (11 mutaciones aplicadas, medidas y revertidas): aceptar transiciones no listadas ⇒ **3 rojos** · no degradar en la rehidratación ⇒ **2** (uno PG) · perder el clamp nunca-empeorar ⇒ **1** · descartar el `stop_update` ⇒ **3** · que la reconciliación `CRITICAL` vete las salidas protectoras ⇒ **2** · no armar el trailing con el T1 ⇒ **1** · no degradar la adopción ⇒ **1** · volver al fallback 0.5/1.0 ⇒ **3** · que el shim evalúe T1 antes que el trailing ⇒ **2** · `PROTECT` sin stop utilizable mudo ⇒ **1** · trailing armado antes de T1 ⇒ **3**. **Gate**: 46 herméticos nuevos (`test_position_lifecycle.py` 29 + `test_auto_v2_lifecycle_stop.py` 17), 35 de integración del worker, 9 legacy portados por el shim flag-off y **3 PG** con gate fail-if-skipped **`AUTO_V2_LIFECYCLE_PG_REQUIRED`** cableado en `auto-v2-durable-pg` (`python-ci.yml`) y `lifecycle-pg` (`release-tag-ci.yml`), con el fichero en el `--ignore` de los jobs offline (no skip mudo). Verificación: `ruff` `All checks passed!` · `mypy` **487** ficheros 0 issues · `lint-imports` **4 kept / 0 broken** · `pytest packages/py` **2825 passed** (+1 skip de Ollama ausente, 1 xfail) ⇒ **0 rojos preexistentes** · CI: `quality` `1869 passed, 38 skipped` y `python` offline del tag `1880 passed, 35 skipped` (los skips son las suites gated por `DATABASE_URL`/`*_PG_REQUIRED`, que corren en sus jobs dedicados; las cifras "0 skipped" de packs anteriores se midieron con esos flags exportados en la sesión) · PG: durable **36 passed**, bloque `lifecycle-pg` del tag **141 passed** + aislamiento por cuenta **45 passed**. **Deuda declarada (2b/AUTO-3)**: `TIME_EXIT`/`THESIS_EXIT` siguen **fuera** del FSM, el ATR de AUTO sigue siendo **sintético** (`precio × 2 %`) y **no hay horizonte de tiempo**, así que el **criterio de salida completo** de `AUTO-2` **no** se cierra en 2a; la dualidad `sim_auto_positions` (autoridad del camino AUTO) vs `position_states` (ADR-033) queda **declarada**, no unificada; no se instala el `ProtectionPlan` por estrategia end-to-end (se unifica el **valor** y el **dueño** de la política T1/T2, no el contrato); `ProtectionConfig` sigue exportada como value object; y el FSM **declara, no repara** (resolver un estado degradado es de la reconciliación; hoy el worker no emite `RECONCILED`). Next = **`AUTO-2` slice 2b** (`TIME_EXIT`/`THESIS_EXIT`, ATR real, horizonte de tiempo y, si toca, migración `043`): punto de entrada [`traspaso-relevo-post-v2-42-auto-2-2026-09-17.md`](./traspaso-relevo-post-v2-42-auto-2-2026-09-17.md). **Auditoría externa del 2026-09-17 (post-sellado): SIN P0** y sello confirmado (5 runs, tag↔commit, sin migración; **M1/M3/M4/M5/M7 reproducidas con los rojos exactos**), con **7 hallazgos de código** declarados como **deuda y criterio de aceptación de 2b** (§9 del pack: `RECONCILED` sin verificar, no-op del trailing sin journal, `PARTIAL_EXIT` arma trailing sin T1, sin pico cae al precio de entrada, `+inf` no fail-closed, `lifecycleState: null` inconsistente sin degradar, 7 transiciones que retroceden) más la **errata** de cifras del pack. Decisión del owner: **no se toca el código de 2a**.
120.  **V2.42.1/AUTO-2 — slice 2b: `TIME_EXIT`, `THESIS_EXIT`, ATR real y cierre de H-1..H-7 (2026-09-17):** [audit-pack](./audit-pack-v2.42.1-auto-2-slice-2b-2026-09-17.md) · [arranque auditor](./arranque-auditor-v2-42-1-auto-2-slice-2b-2026-09-17.md) · [relevo para la fase siguiente](./traspaso-relevo-post-v2-42-1-auto-2-slice-2b-2026-09-17.md) — **bump `1.67.0-beta` → `1.67.1-beta`; SIN migración (Alembic head sigue en `042_portfolio_reservations`: el techo y el nivel de invalidación viven en el JSONB `sim_auto_positions.position_state`, decisión **D5**)**. Cierra las tres deudas que declaró 2a y los **siete hallazgos** del §9 de su pack. **(E1 · `TIME_EXIT`)** `resolve_holding_horizon` (horizonte por plantilla de política con `DEFAULT_MAX_HOLDING_PERIOD_DAYS`, import perezoso para no crear ciclo `analytics ↔ application`) y el techo **congelado en el nacimiento** (`holdingDeadlineAt` en el JSONB, emitido en `to_dict()` solo si existe, con round-trip y rehidratación): la gestión recibe `now` **y** `expires_at` (sin ambos `TIME_STOP` era **inalcanzable por construcción**) y al vencer **vende** con `time_exit`, dejando el FSM en `EXIT_PENDING`; `TIME_EXIT` es evento nuevo del FSM. **(E3 · `THESIS_EXIT`, decisión D2 firmada por el owner)** el nivel de invalidación se congela del plan o del stop estructural (`invalidationPrice`), el juicio se apoya en el **peor adverso persistido** (`worst_adverse_price` sobre `maeR`), y la invalidación **confirmada vende** (`EXIT` real, ya no `REVIEW`) con A/B del camino mesa; la atribución del motivo es del **motivo decisorio** (`primary_reason`, no el flag) ⇒ **un stop-out no se disfraza de salida por tesis**; bajo reconciliación `CRITICAL` sigue mandando el veto de la rama de arriba (la invalidación **no** es salida protectora) y el veto queda **declarado**, no silenciado. **(E2 · ATR real, decisión D3: cablear + medir, veto detrás de flag)** `AtrSource` calcula el ATR desde barras (fail-closed sin barras ni proveedor; `_as_ohlcv_bar` normaliza dict/`OhlcvBar` y `math.isfinite` filtra no finitos), el worker **deja de fabricar el 2 % en el origen** (`_v2_atr_geometry` devuelve `(atr, atrSource)`; el sintético se **declara** en el journal como `atr_geometry` **una vez por (símbolo, origen)** y `matrix` de contadores por origen queda como medición interna), y `AUTO_ENGINE_SIM_V2_ATR_REQUIRED=1` (**default OFF**) veta el sintético con motivo propio **`atr_unknown`** (antes habría caído por R/R calculado sobre un ATR inventado). **(H-1..H-7 cerrados)** `RECONCILED` exige **degradado de origen** + estado resuelto verificado + **coherencia con el hecho de cantidad** (sin cantidad no se puede verificar ⇒ se rechaza); todo `PROTECT` **con efecto** deja traza (memo `_v2_protect_noop_stop`) y la **marca del tick persiste cuando cambia de verdad** (`_mark_observation_changed`, con **dos sensores independientes**: pico favorable y `maeR`, cada uno con su test); `PARTIAL_EXIT` ya no arma el trailing; `compute_trail_stop` **sin pico devuelve `None`** (no cae al precio de entrada); los guardas de finitud usan `math.isfinite`; `lifecycleState: null` **explícito** degrada en la rehidratación; y el FSM es **forward-only** (`_LIFECYCLE_LADDER` + `_forward_target`: una transición cuyo destino nominal ya quedó atrás es idempotente en vez de retroceder). **Matriz de mutaciones medida** (13 mutaciones aplicadas, medidas y revertidas con verificación de contenido exacto): techo no congelado ⇒ **5 rojos** · sin `expires_at` ⇒ **1** · sin `thesis_invalid` ⇒ **1** · stop-out atribuido como tesis ⇒ **1** · volver a `REVIEW` ⇒ **2** · ignorar el ATR real ⇒ **2** · veto inerte ⇒ **1** · trailing sin pico al precio de entrada ⇒ **2** · `RECONCILED` desde cualquier estado ⇒ **1** · marca no persistida (sensor precio) ⇒ **1** · `lifecycleState: null` sin degradar ⇒ **1** · escalera que retrocede ⇒ **1** · marca no persistida (sensor `maeR`) ⇒ **1**. Dos mutaciones nacieron **verdes y no eran agujero de cobertura sino mutación mal puesta** (M5: al desactivar la rama, el `full_exit` del plan pasaba por la puerta de atrás; M10/M13: los dos sensores de la marca **se tapaban entre sí**) ⇒ se rehicieron para romper el **comportamiento** y se añadieron los tests que faltaban (pico favorable **sin** memoria en R y un **segundo** extremo adverso con el pico quieto). **Gate**: 11 tests herméticos nuevos de worker en `apps/api-python/tests/test_auto_v2_lifecycle_clock_thesis.py` (nacimiento congelado, `TIME_EXIT` real, invalidación que vende, no-atribución del stop-out, durabilidad de la marca adversarial, ATR real declarado y veto) más **8** en `test_exit_plan.py`, **9** en `test_position_lifecycle.py` y **3** en `test_position_decision.py`, **6** en `test_auto_v2_entry.py` y **2+1** en el camino mesa (`test_position_manager.py`, `test_v127_golden_path_fail.py`) y **3 PG** de durabilidad (techo congelado superviviente al reinicio, invalidación confirmada, geometría con ATR real) bajo el gate fail-if-skipped **`AUTO_V2_LIFECYCLE_PG_REQUIRED`** que ya existía; el fichero hermético lo cubre en `quality` (`python-ci.yml`) el **pase de directorio** `apps/api-python/tests` y va **explícito** en la lista por fichero de `python` (`release-tag-ci.yml`), y los ficheros PG siguen en el `--ignore` de los jobs offline (un skip mudo no certifica). Verificación medida en el árbol final: `ruff` limpio · `mypy` **487** ficheros 0 issues · `lint-imports` **4 kept / 0 broken** · bloques offline de CI (targets y `--ignore` **extraídos del YAML** por un runner que verifica que cada ruta existe) **1914 passed / 0 failed / 0 skipped** en `quality` y **1925** en `python` del tag · matriz **13/13 rojos** · **CI real del sello**: commit `4ea8c72a` + tag anotado `v2.42.1-beta`; `Python CI` **GREEN 5/5** en `main` (run [`35268151108`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35268151108)) y en la ref del tag, y `Release tag CI` (run [`35268256718`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35268256718)) **GREEN 10/10 con `certify` en `success`** (job `python` offline **1925 passed / 35 skipped**, `mypy` 487 ficheros 0 issues; job `lifecycle-pg` con **PG real** **144 + 45 passed**, donde se certifican los 3 PG de durabilidad nuevos de 2b). **Errata declarada de la propia fase**: el primer wiring añadió a `quality` una ruta **inexistente** bajo `packages/py/application/tests` (el fichero vive en `apps/api-python/tests`), que con la invocación de pytest es **exit 4** y habría puesto el job **rojo**; lo detectó el verificador de rutas del runner al re-medir con la lista **extraída del YAML** en vez de una copia a mano (el origen del error), y está corregido antes del sello. **PG real: NO medido en esta máquina** (motivo medido y declarado: el `connect` del DSN no responde ni rechaza, se cuelga) ⇒ la certificación de durabilidad la aporta CI. **Deuda declarada de la fase siguiente (2c / `AUTO-3`)**: falta la **evidencia de journal de un día completo** que el §4 del roadmap exige como criterio de salida de `AUTO-2`; el **régimen no es un evento del FSM** (`REGIME_EXIT` existe en el camino legacy con precedencia absoluta pero no como evento del FSM); `RISK_EXIT` no existe (llega con el gobernador de `AUTO-3`); el **veto de ATR nace OFF** (D3 pedía medir primero); el emisor de `RECONCILED` sigue sin existir en el camino del worker (H-1 endurece la puerta, no la abre); y el horizonte depende de la **plantilla** (no hay política por estrategia). Next = cerrar `AUTO-2` con **evidencia** (slice 2c corto: corrida golden de un día con `time_exit`/`thesis_exit` en el journal + medición del ATR real; sólo el flip del veto exige firma del owner) y después **`AUTO-3`** — Risk & Market Governor (`V2.43` / `1.68.0-beta`): punto de entrada [`traspaso-relevo-post-v2-42-1-auto-2-slice-2b-2026-09-17.md`](./traspaso-relevo-post-v2-42-1-auto-2-slice-2b-2026-09-17.md).
121.  **V2.42.2/AUTO-2 — slice 2c, CIERRE: evidencia de un día + ATR medido + cero política legacy (2026-09-18):** [audit-pack](./audit-pack-v2.42.2-auto-2-slice-2c-2026-09-18.md) · [arranque auditor](./arranque-auditor-v2-42-2-auto-2-slice-2c-2026-09-18.md) · [relevo para `AUTO-3`](./traspaso-relevo-post-v2-42-2-auto-2-slice-2c-2026-09-18.md) — **bump `1.67.1-beta` → `1.67.2-beta`; SIN migración (Alembic head sigue en `042_portfolio_reservations`)**. Cierra el **criterio de salida de `AUTO-2`** (§4 del roadmap), que exigía dos cosas que no existían y que 2b dejó declaradas: **(1)** el día **no sabía por qué cerró** y **(2)** la política legacy **se evaluaba** (devolviendo `None`) en el camino V2 cuando `held == 0`/`price <= 0`, así que «cero lecturas» no era estructuralmente cierto. **(Evidencia del día)** dueño único `day_exit_reason` (`auto_reason_codes`: `TIME_STOP → time_exit`, `THESIS_INVALIDATION → thesis_exit`, `STRUCTURAL_STOP → structural_stop`, …, y `undeclared` para el cierre sin motivo declarado — nunca se atribuye lo que no se declaró), `SimJournalRow.reason` (aditivo) para que el motivo viaje en la **fila del día** (con V2 ON, la etiqueta decisoria del día vía `_v2_last_exit_label`, vaciada en cada `auto_turn` para no arrastrar atribuciones rancias; en el camino legacy, el motivo de protección) y `AutoDailyReport.exit_reasons`/`atr_sources` (tuples deterministas) con **`sum(exit_reasons) == exits`** e `as_dict()`. `AutoSimulationWorker.atr_source_counts()` pasa a **público** (antes contadores privados solo legibles desde los tests): es el número con el que D3 decide (o no) flipar el veto. **(Cero legacy estructural)** en `auto_turn` el `else` de `protection_exit_reason` (con la supresión del T1 parcial legacy, `_t1_done`) queda detrás de `not self._v2_enabled` ⇒ con V2 ON **no se evalúa en ningún caso**, y el gate es un **sensor que lanza** (`AssertionError` en `protection_exit_reason`/`protection_exit_fraction`) que corre el día completo igual. **(Evidencia reproducible)** `apps/api-python/scripts/v2_42_2_golden_day_evidence.py --out dia.json` corre el día hermético (stores `InMemory*`; sin DB ni red) y emite JSON con motivos, procedencia del ATR, techo congelado, **lecturas de la política legacy** y libro al cierre, **saliendo ≠ 0** si el criterio no se cumple; salida medida: `exit_reasons = {time_exit: 1, thesis_exit: 1, structural_stop: 1}`, `exits 3 = positions_created 3`, `healthy true`, `BALANCED`, `legacy_policy_reads 0`, `atr {real: 15, fallback: 0, missing: 0}` (`veto "0"`, OFF), `holding_deadline_at 2026-10-30T09:00:00Z`, libro plano. **(Tests)** 5 puros nuevos en `test_auto_daily_journal.py` (conteo de motivos, `undeclared` que no se disfraza, orden determinista, ATR medido, ATR no inventado) y **3 herméticos de día completo** en `apps/api-python/tests/test_auto_v2_golden_day_evidence.py` (los tres desenlaces el mismo día con atribución exclusiva y `structural_stop` fuera de los motivos de gestión del journal rico; el mismo día con la política legacy sustituida por el sensor que explota; y la medición de ATR cuando el real falta). **Matriz de mutaciones medida** (7 mutaciones aplicadas, medidas y revertidas verificando contenido exacto): `TIME_STOP → thesis_exit` ⇒ **2 rojos** · fila de cierre sin motivo ⇒ **1** · el `else` legacy sin el guard de V2 ⇒ **1** · medición de ATR vacía ⇒ **2** · `STRUCTURAL_STOP → thesis_exit` ⇒ **2** · cierre sin motivo atribuido a `time_exit` ⇒ **3** · reporte sin motivos ⇒ **4**. **(CI)** el día golden lo cubre el **pase de directorio** `apps/api-python/tests` en `quality` y va **explícito** en `python` del tag; y se añade **`packages/py/application/tests/test_auto_daily_journal.py` a AMBOS** jobs: existía desde `v2.24` y **no estaba en ninguna lista**, así que sus gates no corrían en CI (deuda de cobertura detectada y cerrada aquí). Verificación medida: `ruff` limpio · `mypy` **487** ficheros 0 issues · `lint-imports` **4 kept / 0 broken** · bloques offline extraídos del YAML (41 rutas + 8 PG a `--ignore` en `quality`; 50 + 8 en el tag) ⇒ **1935 passed / 0 failed / 0 skipped** y **1946 passed / 0 failed / 0 skipped** (medido por **JUnit XML**: bajo `subprocess` en Windows el stdout de pytest llega truncado y se pierde la línea de resumen). **(Runner de CI versionado)** `scripts/verify/offline_ci_run_yaml.py` pasa al repo: extrae targets/`--ignore` **del YAML**, **verifica que cada ruta existe** (una ruta inexistente es `exit 4` = job rojo) y mide por JUnit XML; se versiona porque vivía fuera y **dos veces** midió otra cosa que CI. **Errata declarada de la propia fase**: la corrida local de `quality` quedó colgada ~15 min porque la detección de ficheros PG del runner exigía `_pg` pegado al final del nombre y se le escapaban `test_a9_scheduler_process_pg_zero_human.py` y `test_auto_scheduler_real_pg_zero_human_intervention.py`, cuyo `connect` al DSN local cuelga (corregido: `test_*_pg*.py`); no afectó a CI, que tiene su lista propia. **Cambio observable declarado** (solo camino legacy, caso degenerado): una SELL con `qty <= 0` ya no emite `hold_no_op`. **Límites declarados**: el día evidencial es **hermético** (no sesión de mercado ni PG); la `ProtectionConfig` **se sigue construyendo** en el constructor (el camino `V2=0` debe conservar `v2.39.x`: el criterio habla de **lecturas** con V2 ON); el **veto de ATR nace OFF**; `REGIME_EXIT`/`RISK_EXIT` **no** son eventos del FSM (llegan con `AUTO-3`); el **productor del nivel de invalidación** no existe aún (`TradePlan` no lo manda: el test del día lo escribe por un seam declarado); el emisor de `RECONCILED` sigue sin existir; y el horizonte es por **plantilla**, no por estrategia. **CI real del sello (2026-09-18)**: commit `3e8aa359` + tag anotado **`v2.42.2-beta`**; `Python CI` **GREEN 5/5** en `main` (run [`35312788454`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35312788454); `quality` **1935 passed, 38 skipped**, `auto-v2-durable-pg` **39 passed, 0 skipped**) y **GREEN 5/5** en la ref del tag (run [`35312807393`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35312807393)); `Release tag CI` (run [`35312807338`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35312807338)) **GREEN** con **10 jobs requeridos + `certify` (aggregate + artifact)** en `success` (job `python` offline **1946 passed, 35 skipped**, `mypy` 487 ficheros 0 issues; job `lifecycle-pg` con **PG real** **144 + 45 passed**), y `playwright (integrated E2E)` `skipped` por opt-in. Next = **`AUTO-3` — Risk & Market Governor (`V2.43` / `1.68.0-beta`)**: punto de entrada [`traspaso-relevo-post-v2-42-2-auto-2-slice-2c-2026-09-18.md`](./traspaso-relevo-post-v2-42-2-auto-2-slice-2c-2026-09-18.md).
122.  **V2.43/AUTO-3 — slice 1: ejes `MarketRegime` × `RiskRegime` × `OperationalState`, tabla de decisión y gate de ENTRADAS (2026-09-18):** [audit-pack](./audit-pack-v2.43-auto-3-risk-market-governor-2026-09-18.md) · [arranque auditor](./arranque-auditor-v2-43-auto-3-risk-market-governor-2026-09-18.md) · [relevo para el slice 2](./traspaso-relevo-post-v2-43-auto-3-slice-1-2026-09-18.md) — **bump `1.67.2-beta` → `1.68.0-beta`; SIN migración (Alembic head sigue en `042_portfolio_reservations`)**; commit de fase **`7ca4a0e1`** (23 ficheros, `+3677/−58`) y tag anotado **`v2.43-beta`**, que apunta al **commit de sellado** docs-only (que aquí **sí** entra en el tag, a diferencia del patrón de `v2.42.2`, para que la ref sellada no cite refs inexistentes), con `Python CI` **GREEN 5/5** en `main` (run [`35322991385`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35322991385): `quality` **1983 passed, 38 skipped**, `auto-v2-durable-pg` **39 passed**) y **GREEN 5/5** en la ref del tag (run [`35323452519`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35323452519)), más `Release tag CI` **GREEN** con `certify` en `success` (run [`35323452639`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35323452639)); **auditoría externa abierta en [issue #62](https://github.com/jvelasca/Bolsa_V1/issues/62)** (puntos de entrada fijados a la ref del tag). Primer slice de `AUTO-3` (§5 del roadmap): el cambio de fondo es Conceptual — **"el mercado está bajista" y "AUTO tiene prohibido abrir" dejan de viajar mezclados** en un único eje de régimen. **(1) Los tres ejes con fuente**: `MarketRegime` (hecho de mercado, derivado de barras) y `OperationalState` (permiso derivado) viajan **por separado** en el journal, y el veto de permiso tiene **motivo propio** (`governor_exit_only`/`governor_halted`, ambos en `_NO_TRADE_REASONS`) distinto de `regime_invalid`. **(2) Tabla `(MarketRegime × RiskRegime × DD × Vol × Liquidez) → OperationalState`** en `bolsa_analytics.cognitive.operational_governor` (nuevo, **puro**: sin I/O, sin reloj, sin red): los cinco `_*_CAP` **son la política a la vista**, `_STATE_SEVERITY`/`RISK_SCALE_BY_STATE` declarados, `resolve_operational_state` = techo de severidad más estricto (kill switch fuerza `HALTED`), `OperationalAssessment` con `risk_scale`/`binding_axis`/`to_dict()`; **total** (todo eje con default `UNKNOWN`) y **monótona** (más riesgo nunca es más permisivo), con **`UNKNOWN` nunca libre** (techo `ENTRY_RESTRICTED` o `EXIT_ONLY`, con un test por eje). **(3) `drawdown_pct` medido, por fin, en AUTO V2**: el worker alimenta un `EquityMarkBook` **inyectable** una vez por tick con la equity marcada a mercado (base declarada + P&L realizado de las ventas aplicadas + no realizado de marcas vs entrada) y `build_worker_snapshot(drawdown_pct=...)` lo publica — porque el snapshot V2 **nunca** recibía drawdown (siempre `None` ⇒ el eje de riesgo no podía existir); sin dato ⇒ `UNKNOWN` (fail-closed), **nunca 0**, y `_v2_governor_drawdown_pct()` devuelve `None` con el flag OFF (ni se mide ni se paga el cómputo). **(4) Bandas de volatilidad y liquidez** con umbral declarado (`HIGH_VOL`/`LOW_VOL` medidos mandan sobre la disponibilidad del ATR; liquidez contra `min_liquidity_notional`, estricto), y **`LOW_VOL` deja de ser valor muerto**: `MATH_VERSION_MARKET_REGIME_V1` (`AUTO_ENGINE_SIM_V2_REGIME_MATH=v1`, opt-in) añade la rama `low_vol` con umbral declarado y `TRIAL_REGIMES_V1`, **sin cambiar `v0` ni una etiqueta** (`is_valid_regime(..., math_version=...)`; versión desconocida ⇒ `NO_REGIME`, fail-closed) y `map_trial_regime`/`_REGIME_CONSERVATIVE_PRIORITY` actualizados. **(5) Gate en el motor (solo ENTRADAS)**: `PortfolioDecisionConfig.governor: OperationalAssessment | None = None` (aditivo; `None` = no consultado = histórico) y en el **paso 3.b** (tras el régimen, antes de liquidez) `EXIT_ONLY → HOLD/governor_exit_only`, `HALTED → HOLD/governor_halted`, estado no canónico ⇒ `HALTED` (fail-closed); `REDUCED`/`RESTRICTED` escalan el riesgo (0,75/0,50) y `RESTRICTED` sube además el listón de edge (`min_edge × restricted_edge_factor`, con factor `≥ 1` **prohibido por construcción**): el motor **lee** el permiso, no reconstruye política, y **solo endurece**. **(6) Env saneada como bloque** (`_governor_env_overrides`): un corte no creciente, un número no finito o un factor `< 1` descartan **todos** los umbrales de env y quedan los defaults declarados (nunca a medias, nunca tumbando el tick). **(7) Journal**: `PortfolioDecision.market_regime`/`risk_regime`/`operational_state` con `to_dict()` que **solo** emite `marketRegime`/`riskRegime`/`operationalState` cuando el gobernador se consultó (flag OFF = payload histórico byte a byte) y `_journal_entry` que los publica en toda decisión no-trade; un descarte previo al motor **no inventa** dimensiones. **(8) Alias aditivos** por colisión de nombre (`MacroRegime` en `weight_rules`, `FinancialIntegrityState` en `reconcile_financial_integrity`, export `GovernorMarketRegime` en `cognitive/__init__`): ningún eje existente cambia de significado. **Matriz de mutaciones medida** (7 mutaciones, §4 del pack): `_MARKET_CAP["HIGH_VOL"]` mal mapeado ⇒ **1 rojo**; `_LIQUIDITY_CAP["UNKNOWN"]` libre ⇒ **3**; `EXIT_ONLY` deja pasar entradas ⇒ **4**; `strictest_state` devolviendo el menos severo ⇒ **11**; `drawdown_pct` no cableado ⇒ **3**; journal sin las tres dimensiones ⇒ **2**; `risk_scale` ignorado en el sizing ⇒ **2**; **dos mutaciones nacieron verdes y hubo que rehacerlas** (una rompía comportamiento por otro motivo; otra quedaba tapada porque `to_dict()` y `_journal_entry` eran dos sensores que se cubrían entre sí). **Gate**: **48 tests nuevos** (25 puros en `test_operational_governor.py` — totalidad sobre el producto cartesiano de **1 728** combinaciones, techo declarado por eje, monotonía por dominancia de vectores, `UNKNOWN` nunca libre, `assess_*` extremo a extremo —, 20 de gate en `test_auto_v3_governor_gate.py` — byte-identidad con el flag OFF, vetos con motivo propio, escalado de tamaño, listón de edge, tres dimensiones en el journal, env saneada como bloque y `v1` que añade `low_vol` sin cambiar `v0` — y 3 de evidencia en `apps/api-python/tests/test_auto_v3_governor_evidence.py`) más **evidencia reproducible** `apps/api-python/scripts/v2_43_governor_evidence.py`: corre la escalera de drawdown por el camino **real** (`_v2_snapshot` → `plan_v2_tick`, sin PG ni red) con **control con el flag OFF en cada tramo**, emite JSON y **sale ≠ 0** si la tabla no gobierna; medido: 0 % ⇒ `ENTRY_ALLOWED` (cantidad ×1,00 sobre su control) · 6 % ⇒ `ENTRY_REDUCED` (**×0,75**) · 12 % ⇒ `ENTRY_RESTRICTED` **vetada** por `edge_below_threshold` (el control **aprobaba** ⇒ frenó el listón) y ×0,50 con el factor relajado · 15 % ⇒ `EXIT_ONLY` (`governor_exit_only`) · 22 % ⇒ `HALTED` (`governor_halted`), más la **pata no realizada** (posición viva −10 % sobre 60 000 de exposición ⇒ 6 % de DD con la equity base intacta) y el control OFF (el mismo −22 % no cambia la decisión, no emite claves y **ni mide** drawdown). El gate de aplicación va **explícito** en `python-ci.yml` (`quality`) y `release-tag-ci.yml` (`python`); la tabla pura y la evidencia las recogen los pases de directorio. Verificación medida en el árbol final: `ruff` **All checks passed** · `mypy` **487** ficheros 0 issues · `lint-imports` **4 kept / 0 broken** · suites del slice **48 passed** · bloques offline con targets/`--ignore` **extraídos del YAML** (42 rutas + 8 PG en `quality`; 51 + 8 en el job del tag) ⇒ **1983 passed / 0 failed / 0 skipped** y **1994 passed / 0 failed / 0 skipped** (las dos cifras incorporan los 48 nuevos: la comprobación de que lo nuevo **sí** corre en CI). **Límites declarados**: el gobernador **solo gobierna ENTRADAS** (no cierra: el camino de salida no cambia), `REGIME_EXIT`/`RISK_EXIT` **no** son eventos del FSM (slice siguiente, con la decisión de **precedencia y atribución** del día), `HALTED` sin productor propio, umbrales **declarados y no calibrados** (`min_liquidity_notional` nace en **0,0** ⇒ el eje de liquidez no ata salvo liquidez desconocida; `restricted_edge_factor` en **2,0** ⇒ `RESTRICTED` veta a las candidatas del rango alcanzable), evidencia **hermética** (no sesión de mercado) y **PG real NO medido** en la máquina del autor (declarado; el slice no toca ningún fichero PG por diseño). Next = **slice 2 de `AUTO-3`**: `REGIME_EXIT`/`RISK_EXIT` como eventos del FSM y la decisión de precedencia/atribución — punto de entrada [`traspaso-relevo-post-v2-43-auto-3-slice-1-2026-09-18.md`](./traspaso-relevo-post-v2-43-auto-3-slice-1-2026-09-18.md).
123.  **REMEDIACIÓN v2.43.1 — cierre de los 5 hallazgos de la auditoría externa de `v2.43-beta` (3 de ellos de seguridad financiera) (2026-09-18):** [documento de cierre](./cierre-v2.43.1-remediacion-auditoria-2026-09-18.md) — **bump `1.68.0-beta` → `1.68.1-beta`; SIN migración (Alembic head sigue en `042_portfolio_reservations`); el gobernador NO se toca** (ejes, tabla, gate y evidencia intactos: `v2_43_governor_evidence.py` byte a byte igual y **exit 0**), y **`v2.43-beta` no se mueve** (ref nueva y aditiva). Cierra la **cola que la propia auditoría declaró pendiente** (`position_ledger.py` + el resto de `portfolio_reservation.py`) más los hallazgos reportados en el hilo del [issue #62](https://github.com/jvelasca/Bolsa_V1/issues/62). **R1 · doc↔código**: el comentario de `release()` afirmaba que `reserved_cash` se puede sumar **sin filtrar por estado** mientras la propiedad filtra por `.live()`; el comentario pasa a declarar el invariante real (una reserva no viva tiene sus dimensiones a 0 lo cumple `release`, **no** la propiedad) y el filtro como **cinturón**, con test de doble vía `Σ all() == Σ live()` por las **cuatro** vías de liberación (total, parcial, por fill, rollback) — la propiedad **no** se toca. **R2 · seguridad financiera**: `build_portfolio_risk_state` tenía la guarda `if position_total is not None or reserved_risk is not None` **siempre verdadera** (`reserved_risk` es una propiedad `float`, nunca `None`) ⇒ el `(position_total or 0.0)` convertía "hay posiciones sin `risk_amount`" en **0** y publicaba `gross_risk` **por debajo del real** y en silencio, justo en el caso que el sistema declara no poder medir (mismo patrón `or 0.0` que el bug ya cerrado de `total_samples` en `discovery_evidence.py`); ahora el total solo se publica si el riesgo de las posiciones está **medido** (`gross_risk`/`net_risk` `None` si no; `measurement` degrada) y en el camino del tick `_risk_state_for` traduce el `risk_used=None` del snapshot a `0.0` **solo** si `unmeasured == 0` (cartera vacía = cero **medido**, no "no medido"). **R3 · seguridad financiera**: `AppliedFillFact` no validaba `side` y el fold decide con `if is_buy: … else: <venta>`, así que un lado no interpretable (`"hold"`, `"BUY"`) **se doblaba como VENTA** — reducía la posición, realizaba P&L contra un coste ajeno y lo publicaba en `LedgerPosition`/`equityIfClosed` (dirección del error **medida**: no re-aplica al libro físico, pero el P&L del instrumento queda irreal el resto del tick); el tipo ahora lo rechaza y la fila ilegible se declara **al leer** (`coerce_applied_fill_fact` ⇒ rechazada ⇒ `measurement`), de modo que el `else` del fold es una venta **por construcción** (queda dicho en el comentario del fold). **R4 · contabilidad de posición**: la rama `elif` del fold publicaba `averageEntry: 0.0` en una posición **plana** cuando el redondeo a 4 decimales dejaba residuo en `cost_basis` (y `None` si era exactamente 0: **dos representaciones del mismo hecho**, y la primera invita a marcar contra cero); ahora plana ⇒ `average_entry = None` por los tres caminos (cierre limpio, con residuo y sobreventa), y la historia de lo comprado vive en `fills`. **R5 · seguridad financiera**: `ReservationEvent` no validaba nada, así que un `kind` desconocido se aplicaba por la rama de **liberación** (relajaba el libro) y un alta **sin reserva** se perdía en silencio ⇒ `replay()` podía reconstruir un libro declarando **menos capital comprometido del real**; el evento valida el `kind` (constantes canónicas `RESERVATION_EVENT_RESERVE`/`_RELEASE`, exportadas y usadas por `reserve`/`release`/`replay`) y exige la reserva en el alta. **Matriz de mutaciones medida** (5 mutaciones aplicadas **una a una**, medidas y revertidas con reversión verificada: los 5 suites afectados vuelven a **144 passed**): `gross_risk` con `or 0.0` ⇒ **2 rojos** (el puro + el del camino del tick) · `side` sin validar ⇒ **1** · rama del residuo (línea **exacta** de `HEAD`, obtenida con `git show HEAD:…`) ⇒ **1** · `ReservationEvent` sin validar ⇒ **1** · liberación total que no escala a 0 (simula el "camino futuro" que el comentario temía) ⇒ **2**. **Nota de método declarada**: la primera versión de la mutación de R4 dividía por `quantity` en todos los casos y daba **4 rojos**, pero **no era el código pre-fix** ⇒ descartada y re-medida con la línea real: una mutación más fuerte que el bug no mide el sensor del bug. **Verificación local medida (árbol final)**: `ruff` **All checks passed** · `mypy` **487** ficheros 0 issues · `lint-imports` **4 kept / 0 broken** · evidencia del gobernador **exit 0** · bloques offline **extraídos del YAML** ⇒ `quality` **1983 → 1991 (+8)** y `python` del tag **1994 → 2002 (+8)**, **0 failed / 0 skipped**: el delta es **exactamente** el número de tests nuevos en **ambos** bloques ⇒ **todos** entran por las listas existentes y ninguno queda fuera de la red de CI (la deuda que `v2.42.2` tuvo que cerrar a mano para `test_auto_daily_journal.py`). **Cambio observable declarado (uno, y es R2)**: `V2TickPlan.risk_state` es un read-model **en memoria** sin serialización ⇒ **el journal del camino V2 no cambia** y la **byte-identidad con `AUTO_ENGINE_SIM_V2_GOVERNOR=0` sigue intacta** (los tests de byte-identidad del slice 1 pasan); lo único que cambia es `gross_risk`, de **suelo** a `None` con posiciones sin `risk_amount`, que **es** el fix. Diff: **8 ficheros, `+375/−12`** (3 de código, 4 de tests, 1 de versión). **Límites declarados**: no se corrige lo que es **slice 2** (`HALTED` sin productor, `REGIME_EXIT`/`RISK_EXIT` fuera del FSM, umbrales sin calibrar); **no** se mide PG real en la máquina del autor (no hace falta: el parche no toca ningún fichero PG) y `PositionLedger` sigue siendo read-model sin tabla propia. Sello: commit de fase **`48856912`** (11 ficheros, `+721/−12`) + tag anotado **`v2.43.1-beta`** (apunta al commit de sellado docs-only, convención de `v2.43-beta`), con la petición de remediación publicada **en el hilo del issue #62**. **CI real medida**: `Python CI` **GREEN 5/5** en `main` (run [`35343011292`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35343011292): `quality` **1991 passed, 38 skipped** en 118,90 s — **1983 → 1991 = +8**, los ocho tests nuevos corriendo en CI — y `auto-v2-durable-pg` **39 passed**) más `Gitleaks` **GREEN** (run [`35343011254`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35343011254)); tag anotado **`v2.43.1-beta`** → commit de sellado **`b27280de`** (docs-only, dentro del tag), con `Python CI` **GREEN 5/5** en la ref del tag (run [`35344945191`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35344945191): `quality` **1991 passed, 38 skipped** en 82,41 s, `auto-v2-durable-pg` **39 passed**) y `Release tag CI` **GREEN** con 10 jobs requeridos + `certify` en `success` (run [`35344945138`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35344945138): `python` offline **2002 passed, 35 skipped**, `lifecycle-pg` con PG real **144 + 45 passed**). Next = **slice 2 de `AUTO-3`**: `REGIME_EXIT`/`RISK_EXIT` como eventos del FSM y la decisión de precedencia/atribución.
124.  **V2.43.2 — Hardening de contabilidad de posición + Exit Governance (AUTO-3 slice 2) (2026-09-19):** [audit-pack](./audit-pack-v2.43.2-hardening-y-auto-3-slice-2-2026-09-19.md) · [arranque auditor](./arranque-auditor-v2.43.2-hardening-y-auto-3-slice-2-2026-09-19.md) · [relevo para la fase siguiente](./traspaso-relevo-post-v2-43-2-auto-3-slice-2-2026-09-19.md) · **orden de trabajo del cierre: [relevo matriz de mutaciones + sello](./relevo-cierre-v2.43.2-matriz-mutaciones-y-sello-2026-09-20.md)** (fragmento exacto de cada una de las 13 mutaciones, sonda a usar y trampas) — **bump `1.68.1-beta` → `1.68.2-beta`; SIN migración (Alembic head sigue en `042_portfolio_reservations`); el gobernador NO se toca** (`v2_43_governor_evidence.py` **byte a byte igual** y **exit 0**, con su `"bump"` conservado en `1.68.0-beta` por el motivo declarado en `v2.43.1` §8.2) y **`v2.43-beta`/`v2.43.1-beta` no se mueven** (ref nueva y aditiva). **SELLADO**: tag anotado **`v2.43.2-beta`** sobre el commit de sellado docs-only (commit de fase **`ef35e3aa`**, 29 ficheros, `+3814/−66`), con **CI real medida en `main`**: `Python CI` **GREEN 5/5** (run [35497681654](https://github.com/jvelasca/Bolsa_V1/actions/runs/35497681654), con `auto-v2-durable-pg` fail-if-skipped) y `Gitleaks` **GREEN** (run [35497681645](https://github.com/jvelasca/Bolsa_V1/actions/runs/35497681645)). CI en la **ref del tag** `v2.43.2-beta` (`13b54ceb`): `Release tag CI` **GREEN** (jobs requeridos + `certify`, run [35498499879](https://github.com/jvelasca/Bolsa_V1/actions/runs/35498499879)) y `Python CI` **GREEN** 5/5 (run [35498499864](https://github.com/jvelasca/Bolsa_V1/actions/runs/35498499864)) — ver §12.1 del pack. **Fase 1 — hardening (6 hallazgos, 2 P0, todos de la misma familia: un dato ininterpretable que se degrada hacia el lado PERMISIVO sin declararlo).** **(H1, P0)** el fold sumaba `realized_qty += fact.quantity` (la cantidad **solicitada**) en vez de lo que **casó**: una venta rechazada o el exceso de un oversell avanzaba la cantidad cerrada sin inventario que cerrar y, como `remaining_qty = quantity − realized_qty`, **podía reportar como plana una compra real posterior** (la dirección en la que el sistema cree tener **menos** riesgo del real); ahora `realized_qty += matched` (igual que `cost_basis`/`realized_pnl`) y `LedgerPosition` gana **`sold_qty`** (Σ ventas ejecutadas, el hecho del venue) y **`unmatched_exit_qty = sold_qty − realized_qty`**, aditivos y publicados en `to_dict()`. **(H2, P0)** el snapshot de trabajo **resucitaba R2**: `(snapshot.risk_used or 0.0) + committed_risk` convertía "alguna posición no declara su riesgo" en un **total medido** y el motor dejaba de vetar por medición incompleta; ahora el riesgo reservado solo se suma si la base es **medible** (`snapshot.risk_is_complete`), se conserva `None` si no, y se **reenvía `risk_measurement`** al rebuild (un `risk_used` explícito se lee como afirmación `COMPLETE` y borraba el `UNKNOWN` de la base); el rebuild solo deriva un suelo de los `risk_amount` si el llamante **no** declaró un agregado no medible. **(H3, P1)** `max(0.0, (entry − stop) * qty)` publicaba **riesgo `0.0` medido** con un stop del **lado equivocado** ⇒ "stop mal puesto" se leía como "posición sin riesgo"; ahora la geometría la valida `stop_distance()` (la misma casa que el resto del motor) ⇒ `risk_amount = None` ⇒ degrada y **veta**. **(H4, P1)** el fold no garantizaba la idempotencia por `execution_id` (la PK la garantiza en la **escritura**, no en el fold): ahora deduplica y **declara** los repetidos (`duplicate_execution_id:<id>`) degradando la medición. **(H5, P1)** el libro no tenía **cuota de cuenta**: `account_id` viaja al hecho, el fold agrupa por `(account_id, instrument_id)` y una **colisión entre cuentas** del mismo símbolo **degrada la medición** en vez de publicar una cantidad fusionada que la reconciliación tomaría por real. **(H6, P2)** un hecho **sin `applied_at`** se ordenaba como el **más antiguo** (`str(None or "")` precede a cualquier ISO) y torcía el coste medio (que depende del orden de compras y ventas); la clave pasa a `(tiene_fecha, applied_at, execution_id)` y los hechos sin fecha van **al final**, declarado. **Fase 2 — Exit Governance (AUTO-3 slice 2).** **(1) Taxonomía extendida, no duplicada**: `ExitReason` gana `KILL_SWITCH`/`REGIME_EXIT`/`RISK_EXIT` insertados en la `EXIT_REASON_PRECEDENCE` **que ya existía** (orden por autoridad de deshacer riesgo), y `REGIME_EXIT` deja de ser un **override post-hoc fuera del `Literal`** (antes el manager hacía `exit_reasons.append(REGIME_EXIT)` a mano): nace en el `ExitPlan` y compite por precedencia; `secondaryReasons`/`primaryExitReason` hacen que el journal **deje de perder la atribución múltiple**. **(2) `portfolio_risk`/`manual` dejan de ser inalcanzables** desde AUTO. **(3) El gobernador gobierna el ciclo de vida**: `RISK_OFF` (o banda `EXIT_ONLY`) ⇒ `RISK_EXIT`; `HALTED` ⇒ `KILL_SWITCH`; régimen exit-only ⇒ `REGIME_EXIT`, los tres con **reafirmación defensiva** de venta TOTAL (ningún camino — recon, fracción de T2, ratchet de stop — deja posición abierta contra el gobernador) y **coercers canónicos fail-closed** (valor no reconocido ⇒ `UNKNOWN`, nunca permisivo); los tres motivos entran en el **journal del día** y en `_journal_exit_request` para **avanzar el FSM** (`EXIT_REQUESTED`, sin lo cual un reinicio en mitad del exit no veía el estado de salida). **Ojo, dos ejes con el mismo nombre**: el **estado** operacional `EXIT_ONLY` **no** liquida (veta aperturas) mientras que la **banda de drawdown** `EXIT_ONLY` **sí** liquida. **(4) `HardKillSwitch`** (módulo puro nuevo): `resolve_operational_state` aceptaba `halted` desde el slice 1 pero **`plan_v2_tick` no lo pasaba nunca** (parámetro muerto); ahora hay productor con **8 motivos tipificados** (motivo no canónico ⇒ `ValueError`), estado **latcheado** (no se auto-libera; reavisar cuenta `reengagements` y conserva el motivo original), liberación **solo con reconciliación explícita**, entradas **vetadas siempre** y salidas protectoras **permitidas** por defecto, y la parada es **independiente del flag** (`if cfg.governor_enabled or halted`). **(5) Frescura por dimensión** (módulo puro nuevo): cuatro relojes (`market_data`/`atr`/`quote`/`volume`) con umbral declarado; `market_data` no fresca (stale **o sin dato**) ⇒ `blocks_new_entry` (veta **aperturas**, **nunca** un halt: las salidas protectoras siguen vivas), y un instante ininterpretable ⇒ `unknown`, **nunca epoch 0**. **(6) Reservas de SALIDA vivas (F9)**: `committed_positions()` **saltaba** las reservas `sell` y la reconciliación solo consumía fills `buy`, así que la cola de un `RISK_EXIT` parcial era invisible (exposición sobreestimada + la MISMA orden podía re-emitirse al reiniciar); ahora la venta viva **se netea** contra la compra del mismo instrumento, el worker crea **reserva durable antes de emitir** (`_v2_reserve_exit`) y la libera casando **por lado** (`SIDE_SELL`). **Verificación medida (árbol final)**: `ruff` **All checks passed** · `mypy` **487** ficheros 0 issues · `lint-imports` **4 kept / 0 broken** (602 ficheros) · evidencia del gobernador **exit 0** · bloques offline con targets **extraídos del YAML** ⇒ `quality` **2031** y job `python` del tag **2042** (base `1991`/`2002` ⇒ **+40 en AMBOS**, exactamente los tests nuevos: 5 + 4 + 8 en ficheros existentes más **tres ficheros nuevos** — `test_hard_kill_switch.py` 7, `test_data_freshness.py` 8 y `apps/api-python/tests/test_auto_v44_exit_governance.py` 8 con **Golden Day dinámico** `ENTRY → RISK_EXIT → FLAT` y **reinicio en mitad de un `RISK_EXIT`** — de modo que **ninguno queda fuera de la red de CI**), `packages/py/analytics` **862 passed**, `packages/py/application` **1705 passed, 5 skipped**, suites del slice **45 passed**. **Matriz de mutaciones MEDIDA** (§10 del pack, sonda `v2_43_2_mutation_audit.py`, 2026-09-20): **9 de 13 muerden** (M1–M5, M7–M9, M11) y **4 nacen verdes** (M6, M10, M12, M13) con su causa declarada en el §10.1; los **tres agujeros reales** (M6, M12, M13) quedan declarados y reproducibles, sin tocar código ni añadir tests. **Errata declarada de la pasada (método)**: la primera verificación de `ruff` se hizo **sin** `--config pyproject.toml` y concluyó «2 avisos preexistentes en `HEAD`» cuando con la invocación **de la casa** eran **7 avisos, y los 7 de ficheros de este parche** (el flag cambia la clasificación first-party de `isort`); se comprobó la dirección del error con un **worktree limpio en `HEAD`** (que sí pasa `All checks passed!`) y se corrigió con `--fix` **antes** de sellar — la misma familia de error que el runner versionado de `v2.42.2` vino a cerrar. **Límites declarados**: **PG real NO medido** en la máquina del autor (el `connect` del DSN se cuelga) ⇒ la durabilidad de las reservas de salida la certifica CI; la **parada dura es en memoria** (un reinicio olvida el halt) y **`release_kill_switch` no tiene productor automático** (el emisor de `RECONCILED` sigue sin existir, deuda de `AUTO-2`); **`force_protective_exits` existe pero el worker no lo consulta**; **`atr`/`volume` de la frescura van `unknown`** (solo `market_data` es eje activo); el gobernador sigue **default OFF** con umbrales **sin calibrar** (deuda del slice 1); `PositionLedger` sigue siendo read-model sin tabla propia y el `limit` de `list_applied` sigue siendo un suelo. **Aviso de numeración**: el plan de trabajo llamó «v2.44» a la fase 2; la versión de paquete es `1.68.2-beta` porque el roadmap reserva `V2.44`/`1.69.0-beta` para `AUTO-4` — si se renombra el roadmap, hazlo explícito para no auditar dos cosas con el mismo nombre. Next = **`AUTO-4` — Portfolio Optimizer** (`V2.44` / `1.69.0-beta`) según el roadmap.
125.  **BRIEF DE AUDITORÍA v2.43.2 — guía de ataque para el auditor externo (2026-09-20):** [audit-brief](./audit-brief-v2.43.2-hardening-y-exit-governance-2026-09-20.md) — **documento nuevo de solo lectura** que ordena por qué atacar primero la ref sellada **`v2.43.2-beta`** (commit de fase `ef35e3aa`), con `ruta:línea` **verificada en el árbol** y el **comando exacto** de cada medida; **no** sustituye al [pack](./audit-pack-v2.43.2-hardening-y-auto-3-slice-2-2026-09-19.md) — si hay contradicción, **manda el pack**. Fija los **dos P0** (H1 `realized_qty = min(ΣBUY, ΣSELL)` en `position_ledger.py:280-292`; H2 `risk_is_complete` / `risk_measurement` en `auto_v2_entry.py:1173,1196` y `auto_portfolio_snapshot.py:436,449`), los **cuatro P1 del hardening** (H3 `auto_v2_entry.py:599-611` + `stop_distance`; H4 dedupe `position_ledger.py:359-372`; H5 cuota de cuenta `:98,375,382-394`; H6 `_fold_sort_key` `:320-336`) y la **fase 2 de Exit Governance** (precedencia `exit_plan.py:20-22,44-56,208` + `secondary_reasons`; ciclo de vida `position_manager.py:282-285,323,328-333`; `HardKillSwitch` `hard_kill_switch.py:67,83-98,105,119` + `or halted` `auto_v2_entry.py:820`; frescura `data_freshness.py:36,48,103,132`; reservas de salida F9 `portfolio_reservation.py:847,875-876` + `auto_simulation_worker.py:1887,1920,1946,2018,2066,3445`). **Declara de antemano** que las **cuatro mutaciones verdes** (M6, M10, M12, M13) son **confirmación, no hallazgo nuevo** (M6/M12/M13 son los tres agujeros reales declarados en el §10.1; M10 es redundante, sin sensor propio) y recuerda la **nota de método** (una mutación verde no es por defecto agujero de cobertura: hay que descartar que el mutante sea más fuerte o más débil que el bug). Incluye las **preguntas abiertas** del arranque como guion de ataque (lectores de `realized_qty`; cota de `remaining_qty`; `quantities()` vs la degradación por colisión; determinismo de `_fold_sort_key`; rama muerta del manager; **`EXIT_ONLY` de estado vs de banda**, la más afilada; halt pegado para siempre; pureza de `hard_kill_switch.py`; tensión `unknown` bloquea vs "no mido"; reserva de venta duplicada **antes** de emitir; `net_qty` neto que oculte posición; colisión de nombres con el `REGIME_EXIT` legacy; y el delta `+40/+40` sin tests gated), más la lista de **"qué NO es un hallazgo"** del §10.2, los **comandos exactos de CI** (con el runner versionado que **extrae la lista del YAML**), la **errata de método** del `--config pyproject.toml` del §13 y el **formato de hallazgo** (`P0/P1/P2 · afirmación · ruta:línea · comando · salida · ¿ya declarado en el §10?`), a responder en el hilo del [issue #62](https://github.com/jvelasca/Bolsa_V1/issues/62).
126.  **V2.43.3 — AUTO-3 reliability closure: kill durable, identidad de salida y orden UTC (2026-09-20):** [pack de cierre](./audit-pack-v2.43.3-auto-3-reliability-closure-2026-09-20.md) · [relevo de arranque del auditor](./arranque-auditor-v2.43.3-auto-3-reliability-closure-2026-09-20.md) — **bump `1.68.2-beta` → `1.68.3-beta`; MIGRACIÓN NUEVA `043_exit_identity_and_kill_state` (head `042_portfolio_reservations` → `043_exit_identity_and_kill_state`); el gobernador NO se toca** (`v2_43_governor_evidence.py` byte a byte igual y **exit 0**, `"bump"` conservado en `1.68.0-beta`), y **`v2.43`/`v2.43.1`/`v2.43.2` no se mueven** (ref nueva y aditiva). Cierra los **cinco hallazgos P0/P1** que la auditoría de `v2.43.2-beta` dejó declarados — la parada dura y la identidad de una salida vivían en la **RAM del proceso**: **(P0-1)** `auto_kill_state` (una fila por `(account_id, engine_id)` con `engaged`/`reason`/`engaged_at`/**`engagement_id`**/`reengagements`/rastro de liberación) + `KillSwitchStore` (Protocol + in-memory + PG) + **orden de arranque nuevo** `LOAD KILL STATE → vetar entradas → readopt → reconcile`, con `engage_kill_switch_durable`/`release_kill_switch_durable` (la liberación **exige `reconciliation_id`**), persistencia **fail-closed** (un fallo de escritura jamás levanta la parada) y **productores reales** (`RECONCILIATION_FAILURE` si el libro no se pudo medir con reservas vivas; `DUPLICATE_EXECUTION` al reconciliar). **(P0-2)** `exit_order_id` (ULID) + `auto_exit_orders` (`INTENT → RESERVED → EMITTED → PARTIAL/FILLED`, `EMERGENCY`/`ABANDONED`) **persistido antes de reservar y de emitir**, identidad que viaja a `logical_order_id`/`venue_order_id`/`execution_id` (cada fill atribuido a su intent) y **se retira `_v2_exit_seq`** como fuente de identidad (permitía reutilizar una identidad histórica); reserva enlazada por `portfolio_reservations.exit_order_id` y reconciliación que **casa por `exit_order_id`**. **(P1-4)** política **B** declarada: si la reserva no es durable ⇒ intent **de emergencia** (`EMERGENCY` + `emergency=true`) y la salida sigue; si el intent de emergencia **tampoco** es durable ⇒ `engage_kill_switch_durable("SYSTEM_ERROR")` y **la orden NO se emite** (el call-site veta con `exit_intent_not_durable`; antes el `None` se ignoraba y el SELL salía igual). **(P1-5)** el fold ordena por **instante UTC** (`_applied_instant`; ISO con offset y `Z`, naive = UTC, ilegible = sin fecha) y un hecho sin fecha **degrada la medición** a `PARTIAL`/`UNKNOWN` en vez de ordenarse como el más antiguo. **(P0-3)** nuevo `test_auto_v44_exit_crash_matrix.py` (matriz **C1–C4**: crash antes de reservar / tras reservar / tras emitir / tras fill parcial, con reinicio modelado como RAM perdida y stores intactos, más los dos casos de la política B y la coherencia del HALT) y Golden Day + reinicio RISK_EXIT ampliados con la ventana **crash-antes-de-reservar**; nuevo `test_auto_v44_exit_identity_pg.py` (roundtrip de la 043, INTENT y HALT que sobreviven a un **reinicio real por segunda sesión**, enlace reserva→intent con fill parcial) con gate **fail-if-skipped** `AUTO_RESERVATION_PG_REQUIRED=1` en `auto-v2-durable-pg` (+ `lifecycle-pg` del tag) y `--ignore` en el job hermético. **Verificación medida**: `ruff` **0** · `mypy` **489 ficheros, 0 issues** · `lint-imports` **4 kept / 0 broken** · bloque `quality` del YAML (runner `offline_ci_run_yaml.py --with-pg-ignores`) **2042 passed, 0 skipped, 0 failed** y el bloque `python` del tag **2053 passed, 0 skipped, 0 failed** · matriz de crash+governance **17 passed** · las seis suites del cierre juntas **110 passed** · **matriz de mutaciones `v2_43_3_mutation_audit.py`: 6 de 6 muerden** (M1/M2 kill durable, M3/M4 identidad de salida, M5 política B, M6 orden UTC) con **huella del árbol intacta**. **Límite declarado**: los tests PG de la 043 **no se midieron localmente** (sin PostgreSQL alcanzable en la máquina del autor) ⇒ los certifica el CI con el gate fail-if-skipped; **ya medido**: `auto-v2-durable-pg` **39 → 43 passed** (`+4`, los cuatro tests de la 043 contra PostgreSQL real). **SELLADO (2026-09-20)**: commit de fase **`379b8cc9`** (24 ficheros, `+3688/−42`) y tag anotado **`v2.43.3-beta`** sobre el commit de sellado docs-only (convención de `v2.43-beta`…`v2.43.2-beta`; `v2.43`/`v2.43.1`/`v2.43.2` **no se mueven**, ref nueva y aditiva), con `Python CI` **GREEN 5/5** en `main` (run [`35507944960`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35507944960): `quality` **2042 passed, 38 skipped** en 115,73 s, `auto-v2-durable-pg` **43 passed**) y `Gitleaks` **GREEN** (run [`35507944962`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35507944962)). **En la ref del tag (`52b97126`)**: `Python CI` **GREEN 5/5** (run [`35508331525`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35508331525)) y `Release tag CI` **GREEN** con `certify (aggregate + artifact)` en `success` (run [`35508331431`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35508331431): job `python` offline **2053 passed, 35 skipped**, `lifecycle-pg` con PG real **148 + 45 passed**; `playwright (integrated E2E)` `skipped` por opt-in). **Deuda diferida declarada** (fuera de esta versión): API de ledger por `(account_id, instrument_id)`, separación `pending_entry`/`pending_exit` en `committed_positions`, renombrado de `EXIT_ONLY`, frescura de quote como condición de ejecución, hysteresis/calibración del gobernador, gobernador ON por defecto, tags firmados/CI attestation y Portfolio Optimizer (`AUTO-4`).
127.  **V2.44/AUTO-4 — Portfolio Optimizer + valor esperado económico: el ranking deja de ser la decisión (2026-09-20):** [audit-pack](./audit-pack-v2.44-auto-4-portfolio-optimizer-2026-09-20.md) · [arranque del auditor](./arranque-auditor-v2.44-auto-4-portfolio-optimizer-2026-09-20.md) · [plan de fase](./plan-v2-44-auto-4-portfolio-optimizer-2026-09-20.md) (su §7 = estado de ejecución con las **desviaciones declaradas**) — **bump `1.68.3-beta` → `1.69.0-beta`; SIN migración (Alembic head sigue en `043_exit_identity_and_kill_state`); el gobernador NO se toca** (`v2_43_governor_evidence.py` byte a byte igual y **exit 0**, `"bump"` conservado en `1.68.0-beta`). Hasta `v2.43.3`, `plan_v2_tick` recortaba al `TOP_N` y decidía **una a una** en el orden del ranking (la primera entraba y las siguientes solo si la anterior dejaba hueco): **el ranking ERA la respuesta**. Ahora, con `AUTO_ENGINE_SIM_V2_OPTIMIZER=1`, el `top_n` es el **tamaño del CONJUNTO CANDIDATO** y la **cartera elige la combinación** que maximiza **valor esperado económico** sujeto a riesgo; **con el flag OFF (default) el camino V2 es byte-idéntico** (no se construye ninguna candidata, no se llama al optimizador, `V2TickPlan.optimizer` queda `None` y el journal **no gana ninguna clave**). **(1)** `expected_value.py` (puro): `Expected R = p·avg_win_r + (1−p)·avg_loss_r`, `Expected € = Expected R × risk_amount` y **neto** = bruto − coste de ida y vuelta (`TradingCostModel`, casa única del coste), con la **disciplina de medición del repo**: dato ininterpretable (probabilidad fuera de rango, media ausente, signo imposible, geometría **invertida**) ⇒ `UNKNOWN`/`PARTIAL` **declarado en `notes`** y **jamás** un `0.0` que se colaría como "una oportunidad más"; el `avg_win_r` puede **derivarse** del `target` y se declara como derivada. **(2)** `portfolio_optimizer.py` (puro): **enumeración exacta acotada** de subconjuntos con desempate **declarado** (máximo `Σ net_expected_currency` → menor `Σ risk_amount` → lexicográfico), **restricciones duras** (capacidad, capital, concentración sectorial **de la combinación**, correlación y liquidez **fail-closed** si el límite está activo y el dato falta, permiso de riesgo nuevo), **la opción de no operar compite** (`empty_set_wins`: si nada factible es positivo, no se opera) y **tope de combinatoria fail-closed** (`optimizer_enumeration_cap_exceeded` ⇒ el tick **cae al ranking**, sin greedy silencioso). **(3)** Cableado en `plan_v2_tick` (tunables desde env; candidatas no elegidas journalizadas con su **motivo REAL** y su **score real**, nunca `edge_below_threshold`; el tamaño lo fija el **propio motor** por sonda `decide_portfolio` con la **misma** caída de ATR; el **gobernador y el kill switch siguen mandando** y el optimizador solo decide **qué** se evalúa y **en qué orden**); `V2Signal` gana 4 campos **aditivos** (`target_price`, `p_win`, `avg_win_r`, `avg_loss_r`) y `auto_reason_codes` añade `OPTIMIZER_REASONS`. **Verificación medida**: `ruff` **0** · `mypy` **489 ficheros, 0 issues** · `lint-imports` **4 kept / 0 broken** (607 ficheros) · CI real (runs `35510546044` y `35510840734`; los bloques offline completos no llegaron a término en la máquina del autor) ⇒ `quality` **2042 → 2075 passed** y tag **2053 → 2086 passed** (**+33 en AMBOS**, 0 skipped, 0 failed; el test de aplicación va **explícito** en las dos listas) · **33 tests nuevos** (12 + 12 puros, 8 de cableado, 1 en el worker real) · **matriz de mutaciones `v2_44_mutation_audit.py`: 7 de 7 muerden** (objetivo/desempate, vacío compite, tope, EV no medido, correlación fail-open, motivo honesto en el journal, el propio flag) con **huella del árbol intacta**. **Límite declarado**: **NO hay productor de economía en el camino del tick** (`_v2_signals` no aporta `p_win`/medias: el productor real es de `AUTO-7`) ⇒ con el flag **ON** todas las candidatas son `optimizer_expected_value_unmeasured` y el tick **no opera** (fail-closed **declarado y fijado en test**); con el flag **OFF por defecto** el camino de producción **no cambia**. Otras desviaciones declaradas: `max_drawdown_used_pct` no existe (el veto de drawdown entra como `new_risk_allowed`), la correlación usa el dato de hoy (`TradeContext`, no matriz por pares), el objetivo es EV neto **con desempate** por riesgo (no frontera eficiente con pesos), umbrales **declarados y no calibrados**, duplicidad declarada de gates (conjunto vs foto reservada) y el gate «Golden Day» medido **en dos capas** (tick + worker real). **SELLADO (2026-09-20)**: commit de fase **`f692159d`** (18 ficheros, `+2728/−2`) y tag anotado **`v2.44-beta`** sobre el commit de sellado docs-only (convención de `v2.43-beta`…`v2.43.3-beta`; los cuatro tags anteriores **no se mueven**, ref nueva y aditiva), con `Python CI` **GREEN 5/5** en `main` (run [`35510546044`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35510546044): `quality` **2075 passed, 38 skipped** en 89,26 s — el **`+33`** exacto de los tests nuevos —, `auto-v2-durable-pg` **43 passed** sin cambio porque no hay migración, `paper-forward-pg` 2, `grammar-discovery-pg` 21, `lifecycle-pg` 13), `Gitleaks` **GREEN** (run [`35510546045`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35510546045)), `Optimize lab` **GREEN** (run [`35510546041`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35510546041)) y `Fase 2 scientific` **GREEN** (run [`35510546060`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35510546060)). **En la ref del tag (`c95819c1`)**: `Release tag CI` **GREEN** con `certify (aggregate + artifact)` en `success` (run [`35510840734`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35510840734): job `python` offline **2086 passed, 35 skipped** — los mismos **+33** sobre los 2053 de `v2.43.3-beta` —, `lifecycle-pg` con PG real **148 + 45 passed**, `a7-gate` 7, `shared` 778 tests / 94 ficheros, `playwright (integrated E2E)` `skipped` por opt-in), `Python CI` **GREEN 5/5** (run [`35510840771`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35510840771): `quality` **2075 passed, 38 skipped** y `auto-v2-durable-pg` **43 passed**, idénticos al commit de fase) y `Gitleaks` **GREEN** (run [`35510839339`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35510839339)).
128.  **V2.45/AUTO-5 — Golden Day 2.0: el día real y su embudo (2026-09-20):** [audit-pack](./audit-pack-v2.45-auto-5-golden-day-2-0-2026-09-20.md) · [arranque del auditor](./arranque-auditor-v2.45-auto-5-golden-day-2-0-2026-09-20.md) · [plan de fase](./plan-v2-45-auto-5-golden-day-2-0-2026-09-20.md) (su §7 = estado de ejecución con las **desviaciones declaradas**) — **bump `1.69.0-beta` → `1.70.0-beta`; SIN migración (Alembic head sigue en `043_exit_identity_and_kill_state`)** y **el gobernador NO se toca** (`v2_43_governor_evidence.py` byte a byte igual y **exit 0**, `"bump"` conservado en `1.68.0-beta`). **El invariante que instala**: AUTO-5 cierra el «Hermetic Golden Path» del roadmap con un día certificado en **dos capas** — por commit, el **embudo hermético** del día (`seen == traded + rejected + expired + missed`, con motivo en cada estado no operado y **coste de oportunidad** declarado); al sellar el tag, el **día real** con el **proceso de scheduler** y **PostgreSQL real**. **(1)** `auto_daily_journal.py` (puro) gana campos **aditivos** (embudo + `rejection_reasons`, atribución por estrategia `strategy_traded`/`strategy_exits`, MAE/MFE por operación y **coste de oportunidad** de las rechazadas): el embudo es una **partición disjunta y exhaustiva** (estado fuera del vocabulario ⇒ embudo abierto `opportunity_status_unknown`; rechazada sin motivo ⇒ decisión en silencio `rejection_without_reason`; `seen` del productor que no cuadra con las filas ⇒ `funnel_seen_mismatch`) y rige la **disciplina de medición del repo**: lo no medible se declara (`UNKNOWN`/`PARTIAL` + `notes`), **jamás** un `0` que se leería como "coste cero" (el coste se mide con el **precio posterior** observado y el MAE/MFE se **recoge** del `mfe_mae` del JSONB; la calibración es `AUTO-7`). **(2)** Identidad de estrategia **sin migración**: `strategyVersion` viaja en el `payload` de las decisiones (entradas y rechazos; sin versión la clave se **omite**) y el cierre la **hereda** de la posición, y `_strategy_version_from_source` aprende la fuente `auto-2.0:<version>` del pipeline V2 (antes sólo `active-strategy:` ⇒ el fill/cierre del camino V2 quedaba sin atribuir). **(3)** Capa hermética por commit: `test_auto_daily_journal.py` (embudo cerrado, motivo en cada rechazo, estado desconocido, `seen` descuadrado, coste declarado, MAE/MFE declarado) y `test_auto_v2_golden_day_evidence.py` (el día del worker demuestra `time_exit` + `thesis_exit` con **dos** versiones atribuidas, MAE/MFE completo, la identidad en el `payload` y el rechazo tipado con su coste con `TOP_N=1`). **(4)** Capa real del tag (`apps/api-python/tests/test_golden_day_v2_process_pg.py`, NUEVO): el **proceso real** `python -m bolsa_api.workers.scheduler_worker` con V2 ON abre **≥3** posiciones (una por instrumento, ids **deterministas** por barrida pura que garantizan `fills > orders`) y cierra el libro — se detiene el proceso, se lleva el **techo de mantenimiento durable** (`holdingDeadlineAt`) al pasado y el reinicio lo **rehidrata** ⇒ venta por `time_exit`, todo `APPLIED`, cada fill con su transacción y libro canónico plano — con gate fail-if-skipped propio **`AUTO_GOLDEN_DAY_V2_PG_REQUIRED`**, guard anti-skip dedicado en `lifecycle-pg` del tag y el fichero en el `--ignore` de los jobs offline de ambos workflows. **(5)** **Matriz de mutaciones MEDIDA** (`apps/api-python/scripts/v2_45_mutation_audit.py`, 8 mutaciones: motivo tipificado, estado no catalogado, `seen` descuadrado, coste declarado, MAE/MFE declarado, atribución por estrategia, identidad aditiva y fuente `auto-2.0`) con **huella del árbol intacta**. **Desviaciones declaradas (plan §7)**: el **precio del camino SIM real es plano** (`flat_price_script`) y el horizonte de la plantilla es de **21–90 días**, así que el día real **no cierra por T1/trailing dentro del presupuesto de un test** ⇒ la fase de cierre dispara el `time_exit` por el **seam durable** (techo congelado → pasado → reinicio) y el cierre por **geometría** (T1/trailing/régimen) se certifica en la capa hermética con precio y reloj **inyectados**; el MAE/MFE se **recoge** (no se calibra) y la correlación es la de hoy, no una matriz por pares. Límite de método medido y declarado: el barrido de residuos del conftest borra toda cuenta ajena y todo instrumento `inst-%` al cerrar cada **sesión** de pytest, así que **dos sesiones de pytest en paralelo contra la misma base se borran los datos entre sí** (se reprodujo: el motor quedaba en `retry_scheduled` contra filas ya borradas) — el gate del tag corre el fichero en **paso dedicado**. **Corrección de procedencia frente al arranque del plan**: la capa real **sí se midió en local** (el contenedor `bolsa-postgres` estaba sano en `localhost:5432`): `AUTO_GOLDEN_DAY_V2_PG_REQUIRED=1` ⇒ **`1 passed`** en **10,05 s**, con seis corridas en solitario de 8,89–10,05 s. **Verificación medida**: `ruff` **0** (3 `I001` propios corregidos) · `mypy` **489 ficheros, 0 issues** · `lint-imports` **4 kept / 0 broken** · las 3 suites de la fase **44 passed** · matriz de mutaciones **8 de 8 muerden** con huella intacta · baseline offline del YAML (JUnit) `quality` **2087 passed, 0 skipped** y tag `python` **2098 passed, 0 skipped** (**+12 exacto en AMBOS** = los 12 tests nuevos: 9 del agregado + 2 del día del worker + 1 del worker). **SELLADO (2026-09-20)**: commit de fase **`ad800262`** (17 ficheros, `+2581/−16`) y commit de corrección **`0fc85c17`**; `Python CI` **GREEN 5/5** en `main` (run [`35520898909`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35520898909): `quality` **2087 passed, 38 skipped** en 72,43 s — el **`+12`** exacto —, `auto-v2-durable-pg` **43 passed** sin cambio porque no hay migración, `paper-forward-pg` 2, `grammar-discovery-pg` 21, `lifecycle-pg` 13) y `Gitleaks` **GREEN** (run [`35520899318`](https://github.com/jvelasca/Bolsa_V1/actions/runs/35520899318)); tag anotado **`v2.45-beta`** sobre el commit de sellado docs-only (convención de `v2.43-beta`…`v2.44-beta`; los cinco tags anteriores **no se mueven**, ref nueva y aditiva), con el día real corriendo en un **paso dedicado** de `lifecycle-pg` y su guard anti-skip.
