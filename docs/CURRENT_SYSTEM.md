# CURRENT_SYSTEM — estado real de Bolsa V1

> **Padre:** [engineering-index](./engineering/engineering-index-2026-08-03.md) §1 (Architecture).
> **Para quién:** el siguiente chat, un auditor, Cursor. No es el historial (`PROJECT_STATE.md`).
> **AsOf:** 2026-08-28 · **ADR-031** tesis ≠ plan ≠ permiso · **ADR-032** Operational Core v1.9 **CERRADO (modelo)** · **ADR-033** Operational Authority v1.10 **CERRADA (H1→P4)** · **ADR-034** Operational Integrity **OI-1…PA-1 + OE-1 CERRADOS** · **ADR-035** Operational Reliability v1.12 **OR-1/3/4/5/6 CERRADOS · OR-2 cerrado vía DEX-1+DEX-2** · V1.13 Durable Execution **D0 + DEX-1…DEX-5 CERRADOS · pack v113 stampado** · **F1–F4** · **ExitPermission** · **INFRA CI-by-tag** · **ADR-023 Accepted BETA-D** · **A3-wire** `d704263` · **C1–C6** consolidación v1.8.1 **CERRADA**. Alembic `014` en `bolsa_v1`.
> **AsOf:** 2026-08-31 · **Producto:** **V1.42 Operating Excellence F2–F8** **CÓDIGO** · **Tag tip:** **`v1.42-beta` → `5e3fb1a4`** ([`traspaso-relevo-tag-v1-42-beta-2026-08-31.md`](./engineering/traspaso-relevo-tag-v1-42-beta-2026-08-31.md) · [`arranque-auditor-v1-42-beta-2026-08-31.md`](./engineering/arranque-auditor-v1-42-beta-2026-08-31.md)) sobre tip `v1.41.3-beta`. **V1.41.3** Honesty Residuals **CERRADO** · **V1.41.2** Operational Honesty · **V1.41** Daily Desk · **V1.40** Exit Management UX · **V1.39** Position Operating UX · **V1.38** Entry Operating UX · **V1.37** Operational Truth · **V1.36** Daily Operating UI · **V1.35** Position Operating Hardening — cerrados. **Previo tip certificado:** **`v1.41.3-beta` → `a8101ab7`** [GREEN](https://github.com/jvelasca/Bolsa_V1/actions/runs/33382034705). **Package version:** `1.35.0-beta` (congelado; ≠ product — [`versioning.md`](./engineering/versioning.md)). **BETA / no producción.** Relevo V1.42 F8 [`traspaso-relevo-v1-42-f8-paper-auto-2026-08-31.md`](./engineering/traspaso-relevo-v1-42-f8-paper-auto-2026-08-31.md) · spec [`spec-v142-operating-excellence-2026-08-31.md`](./engineering/spec-v142-operating-excellence-2026-08-31.md) · [ADR-042](./adr/042-operating-excellence.md). Hoja de ruta [`roadmap-v127-path-to-10-2026-08-28.md`](./engineering/roadmap-v127-path-to-10-2026-08-28.md).
> **Auditoría:** tip stamp **`v1.42-beta` → `5e3fb1a4`** · arranque [`arranque-auditor-v1-42-beta-2026-08-31.md`](./engineering/arranque-auditor-v1-42-beta-2026-08-31.md) · previo **`v1.41.3-beta` → `a8101ab7`** [GREEN](https://github.com/jvelasca/Bolsa_V1/actions/runs/33382034705) · arranque [`arranque-auditor-v1-41-3-beta-2026-08-31.md`](./engineering/arranque-auditor-v1-41-3-beta-2026-08-31.md) · tip CI previo **`v1.41.2-beta` → `ebb11e07`** [GREEN](https://github.com/jvelasca/Bolsa_V1/actions/runs/33374118789) · roadmap [`roadmap-v127-path-to-10-2026-08-28.md`](./engineering/roadmap-v127-path-to-10-2026-08-28.md).

---

## Stack

React 19 + Vite (`apps/web`) · FastAPI `:8000` (`apps/api-python`) · PostgreSQL · SQLAlchemy + **Alembic = autoridad de migraciones** · Prisma en `packages/database` **degradado** (seed/léxico, no schema dual) · JWT multi-user (ADR-027 C).

## Identidad

Doble cara congelada: **QROS** (Lab, ADR-011) + **Investment OS** (mesa) unidos por el **Decision Spine**. Lab/Radar **fuera** del spine (D3, ADR-019). LLM **nunca** ejecuta.

## Features activas

Embudo / Lista AUTO / Finalistas · DÍA D · CORE-R · CORE-P · FA/FIE · SEMI · **Hoy** `/mesa` (ADR-037 + **ADR-040**: label producto «Hoy»; vistas `?view=` resumen/posiciones/oportunidades/decisiones/journal/confirmar; incidente primero; Confirm = firma; candidatos nunca COMPRAR; **V1.19** Opportunity Discovery + funnel; compat `?focus=libro|spine`) · **Mercado** = terminal `/trading` (Watchlist|Gráfico|Operaciones|**DECISIÓN**; **sin** HoyCommandStrip ni MesaOperationalBar en el dock) · **Cartera** (posiciones/órdenes/historial) · **Asesor** `/research` · **Laboratorio** `/backtests` · UX mesa U0–U6 · Decision Board absorbido en Hoy → Decisiones (`/decision-board` → `/mesa?view=decisiones`; API intacta) · **Decision Journal** solo lectura (Hoy → Journal / `/decision-journal`, ADR-029 + ADR-036) · **TradePlan** · **PositionState** · **ExitPlan** · **ExecutionPlan F4 PAPER** · **ExitPermission** · PortfolioFit v1 · DS-05 · DS-03 · paper/DEMO · Libro AUTO UI (BETA-D) · **OE-1** · **DEX-3** banner en Hoy · **Consola ops** `/operational-console` (diagnóstico; no L1 — ADR-040).

## Features congeladas

`PAPER_D_EXECUTE` **default off** — DEMO opt-in **autorizado** (stamp 2026-08-26; ADR-023 BETA-D); ≠ venue Live · ≠ thaw estricto. Belief / gobernanza IA · Track B B1–B12 cerrado (no reabrir) · `pending-delete` E8 N · purge storage N · thaw **estricto** (60d/50/70/55) sigue deuda — [`deuda-thaw-estricto-runbook-2026-08-25.md`](./engineering/deuda-thaw-estricto-runbook-2026-08-25.md) · W+2 remasure 2026-08-26 (P1=28 · confirm_seed=1 · P3/P4 API gap · no Accept) [`traspaso-relevo-thaw-estricto-remeasure-2026-08-26.md`](./engineering/traspaso-relevo-thaw-estricto-remeasure-2026-08-26.md). Broker live XTB cableado (VS-1 + XL-2) pero mesa default **paper**; mock fail-closed; **no** thaw AUTO.

## Auth (real)

Auth viva = **JWT + cookie HttpOnly** (R-12 / ADR-027). `APP_PASSWORD` es un **overlay opcional de login en dev**, no el modelo de auth. La frase histórica «auth JWT diferida (D4)» está **obsoleta**. El JWT incluye claim opcional `role` (`admin` \| `operator`); el helper `require_role(request, role)` en `bolsa_api.auth.roles` devuelve **403 Forbidden** si el rol autenticado no coincide — hoy se usa en tests y queda disponible para rutas admin futuras; la mayoría de endpoints solo exigen sesión JWT válida.

## Ejecución (camino actual)

Tres capas (ADR-031): **tesis** (`DecisionPackage`) ≠ **plan** (`TradePlan` v0+F1) ≠ **permiso** (`check_opening`). Ranking / TOP / dictamen **no** son BUY.

```
Dato → Assessment → run_decision_runtime → DecisionPackage (tesis)
  → TradePlan (v0 status + F1 campos) en propose (`data.tradePlan` + `runtime.tradePlan`) y echo en confirm
  → Policy Gate + check_opening (Fit de cesta + DS-05 freshness + DS-03 mandate)
  → ConfirmRecommendationIntent (SEMI, humano; TTL + precio + H3 orphan fail-closed; **OI-2** risk_signature exige TradePlan TRIGGERED en apertura; **V1.26** geometría fail-closed + `signedStop` en API→gate→Position snapshot; **OI-3** execute exception → ExecutionRecord `unknown`; **OI-4** PaperOrder CREATED→FILLED; **OI-5** PositionRevision en protect/reduce; **OI-6** PortfolioReconciliation detect/report; **LR-1** LiveLedgerReconciliation live↔ledger; **BA-1** envío vía IBrokerAdapter; **PH-1** protect persist None ≠ `protect_applied`; **XL-1/XL-2** XTB rejected/submitted ≠ fill; filled→ledger; **VS-1** venue Paper|Live; **OR-1** idempotencia E2E: `decision_id` canónico · ids estables · short-circuit pre-submit; **OR-2** crash/restart: `DurableSubmitIntent` pre-submit · `UNKNOWN` reconstruible · mapeo intent↔`venue_order_id` · no re-POST · **DEX-1** PG `submit_intents` · **DEX-2** cert store/cliente fresco cross-PID; **OR-3** PaperOrder state machine: SUBMITTED/ACK/PARTIAL/REJECTED/CANCELLED/EXPIRED/UNKNOWN; **OR-4** recon → opening veto: OI-6 `drift` / LR-1 live `drift|unavailable` DENY aperturas; exits ALLOW; **DEX-3** `OperationalIncident` drift→OPEN→review→resolve(nota)→clear(solo recon clean) · veto `incident:unresolved` · sin auto-heal; **OR-5** suite A–L + retry + crash certificada en spine; **OR-6** SEMI certification: readiness `PAPER_READY`/`PAPER_DEGRADED`/`LIVE_EXPERIMENTAL`/`LIVE_BLOCKED` (sin %) + CTA `Ejecutar en PAPER|LIVE`)
     O  ExecutionRouter (AUTO paper, flag off)
     O  FillPendingOrder (pending_orders; mismo check_opening en aperturas; BrokerAdapter venue VS-1)
     O  POST /portfolio/trade (ExecuteGatedPortfolioTrade; buy = mismo gate **incl. sanity `instrument_data_status`**, sell = skip; **OI-1** sync PositionState)
  → IBrokerAdapter.submit (paper = PaperBroker → ExecuteTrade; live XTB = bridge; filled→ledger; mock LIVE = not_wired)
  → post_fill_position_sync (OI-1: manual / pending / HTTP → Position persistida cuando aplica)
```

SEMI y AUTO son **el mismo risk de cesta**, distinta autorización (D1). `DecisionPackage` es el contrato de identidad en el confirm (D2). `TradePlan` **no** sustituye el spine: lo extiende.

**`entriesBlocked` ≠ `check_opening`.** `entriesBlocked` (kill + incidentes + vetoed, fail-closed) es **prevención UX**: las superficies no generan propuestas (`proposeInstrumentSupervised` y side-doors Comprar). **No** es el veto de fill. `check_opening` sigue siendo la **autoridad de apertura**. `POST /ai/recommendations/propose` no recibe `entriesBlocked` y no se modifica en V1.41.3 ni en este contrato: propose ≠ execute; Confirm / opening gate firman. Detalle: [ADR-042](./adr/042-operating-excellence.md) · spec §A.2.

Hoy (ADR-040 + **ADR-041** + **V1.22–V1.24** + **V1.37–V1.41.3** + **V1.42 F6**): **Daily Desk 2.0** = cuatro cubos §B.7 (🔴 requiere acción · 🟢 oportunidades · 🟡 vigilar · ⚪ sin acción) desde POT/EOT/ExecutionState; sin paneles de ranking/KPI en el chrome (no segundo Mercado). Ranking Estudio / Libro / Decisiones / Consola detrás de «Ver detalles». Universo Daily Ops = **Estudio** (H1: caller no puede ampliar). Mercado = cockpit operativo con `InstrumentOperationalContext` + `OperationalPlanView` (V1.23) y honestidad semántica V1.24 (fases allowlist · vocabulario · plan contract). **V1.37** `OperationalTruth` = proyección canónica de posición abierta; **V1.38** `EntryOperatingTruth` = pre-posición; **V1.39** CTA primaria; **V1.40** `ExitRouteView`; **V1.41** Daily Desk; **V1.41.2** mismas superficies reciben `entriesBlocked`/`gateStatus` (kill + incidentes + vetoed) y `orderPending` (executionHint); **V1.41.3** cierra side-doors propose/buy, VETO↔CTA y cola Confirm. `protect_hint` thin no gana a `PositionDecision`. **V1.42 F2** `ExecutionState` = proyección canónica de ciclo de orden/fill/UNKNOWN ([spec](./engineering/spec-v142-operating-excellence-2026-08-31.md) · [ADR-042](./adr/042-operating-excellence.md) · [relevo F2](./engineering/traspaso-relevo-v1-42-f2-execution-state-2026-08-31.md)); **F2b** list intents · **F3** PositionOperatingTruth · **F4** TradeStory · **F5** Mercado panel **DECISIÓN** · **F6** Hoy cuatro cubos · **F7** SEMI · **F8** PAPER AUTO — **CÓDIGO** ([relevo F8](./engineering/traspaso-relevo-v1-42-f8-paper-auto-2026-08-31.md)). AdminRail = Overview/Cuentas/Fiscal/Consola. ⚙ = solo preferencias. `HoyCommandStrip` / `mapDecisionBoardToHoyQueue` se conservan como código/tests de honestidad C1, **no** montados en el dock. Caminos canónicos TradePlan (Ciclo C4): sesiones `session.tradePlan`; F3 `extra.payload.tradePlan`. `planSource` live si hay objeto TradePlan (canónico o fallback legacy); **sin plan → WATCH** + `planSource: projection` (nunca BUY/ARMED inventados; Ciclo C1). Heurística BLOCKED/WATCH usa `whyNot: legacy_projection`, no `fit` ficticio. Confirmar sigue siendo la firma (drawer + `/confirm`).

## Limitaciones conocidas (no son bugs de esta rebanada)

- **ADR-033 Operational Authority v1.10:** D0 docs + **H1+H2+P1+P2+P3+P4 CERRADOS** — Position persistida; ticket firma riesgo (P2); cadena ExitPlan → ExitPermission → SEMI; Consola mesa P4.1+P4.2. Next = broker / protect persist (fase explícita posterior). [`033-operational-authority-position-persistence.md`](./adr/033-operational-authority-position-persistence.md) · pack [`audit-pack-estado-global-2026-08-25-v110.md`](./engineering/audit-pack-estado-global-2026-08-25-v110.md) · relevo P4 [`traspaso-relevo-p4-consola-mesa-2026-08-25.md`](./engineering/traspaso-relevo-p4-consola-mesa-2026-08-25.md).
- **ADR-034 Operational Integrity OI-1…OI-6 + PaperBroker + BrokerAdapter + PH-1 + XL-1 + LR-1 + XL-2 + VS-1 + RV-1 + JP-1 + thaw stamp + PA-1 + OE-1:** Manual HTTP + pending SELL + Confirm honesty + Proteger persist + Lab exit sync (**OI-1**). Apertura SEMI sin TradePlan TRIGGERED → `risk_signature` fail-closed `no_tradeplan` (**OI-2**). Excepción de `execute_trade` → `unknown` ≠ error ≠ no-ejecutado (**OI-3**). PaperOrder CREATED→FILLED paper, ≠ broker (**OI-4**; lifecycle pleno = **OR-3**). PositionRevision historia auditada stop/status en snapshot (**OI-5**). PortfolioReconciliation detect/report cash/holdings/PositionState, no auto-heal (**OI-6**; opening veto = **OR-4**). **PaperBroker** venue PAPER (CREATED→SUBMITTED→fill→FILLED / UNKNOWN). **BrokerAdapter** puerto Paper|Live; mock LIVE `not_wired`. **PH-1** Confirm protect: persist None ≠ `protect_applied`. **XL-1** `XtbBrokerAdapter` LIVE vía bridge; rejected/submitted ≠ fill. **LR-1** LiveLedgerReconciliation live↔ledger detect/report; `unavailable` fail-closed (veto live = **OR-4**). **XL-2** bridge `filled` → `execute_trade` ledger; submitted intacto. **VS-1** mesa Paper|Live (`BROKER_VENUE` + toggle); default paper. **RV-1** Redis `bolsa:risk:broker_venue` (coalesce memory ?? redis ?? env). **JP-1** Alembic `012` hot columns dual-write; JSONB SoT. **Thaw stamp** DEMO opt-in `PAPER_D_EXECUTE` autorizado; default **OFF**; ≠ thaw estricto. **PA-1** preferencia cuenta `settings_json.brokerVenue` (memory ?? redis ?? account ?? env ?? paper); lazy Confirm/Fill; mesa/API risk = global; **UI cuenta = OR-6**. **OE-1** Autoeval SEMI·AUTO (`GET /api/risk/ops-self-eval` + script + chip mesa); measure ≠ Accept; recon OI-6 cableado (status `ok`/`drift`/…). [`034-operational-integrity-continuity.md`](./adr/034-operational-integrity-continuity.md). Tag histórico **`v1.11-beta` → `76d0f951`**.
- **ADR-035 Operational Reliability v1.12 — D0 + OR-1/3/4/5/6 CERRADOS · OR-2 cerrado vía DEX-1+DEX-2 · tag `v1.12-beta`:** Validar el sistema (timeout / retry / crash / drift / readiness), no expandir. `AI_SEMI` → TradePlan `RiskSignature`; `HUMAN_MANUAL` → ManualTradeRiskGate (I1). LIVE experimental · AUTO no modalidad · `PAPER_D_EXECUTE` off. **OR-1** retry Confirm paper: `decision_id` canónico · `intent_id`/`order_id` estables · short-circuit pre-`adapter.submit` · fail-closed sin `decision_id`. **OR-2** crash/restart: `DurableSubmitIntent` persistido antes de submit · recovery `UNKNOWN` sin re-POST · mapeo `intent` ↔ `venue_order_id` · **DEX-1** PG `submit_intents` + `PostgresSubmitIntentStore` + fase `send_attempted` · **DEX-2** cert cross-PID (store/cliente fresco → UNKNOWN · 0 re-POST). **OR-3** PaperOrder state machine: `CREATED`→`SUBMITTED`/`ACK`/`PARTIAL`/`FILLED` + `REJECTED`/`CANCELLED`/`EXPIRED`/`UNKNOWN`; PaperBroker boom → `UNKNOWN`; crash recovery `paperOrder.status=UNKNOWN`. **OR-4** recon → opening veto: OI-6 `drift` DENY aperturas; LR-1 `drift`/`unavailable` DENY solo venue live; exits ALLOW; sin auto-heal. **DEX-3** `OperationalIncident`: drift → OPEN → review → resolve (nota; no muta libros) → clear solo si recon `clean`; apertura `incident:unresolved`; UI Mesa parked. **DEX-4** Confirm = orquestador (`bolsa_application/confirm/` Identity·RiskGate·OpeningGate·ExitGate·Execution·SubmitIntent·PositionSync; API pública intacta). **DEX-5** invariantes operacionales (qty≥0 · filled≤ordered · terminal · 1 decision≤1 order · drift blocks · protect; property suite spine). **OR-5** suite A–L + retry + crash certificada en spine (`test_or5_broker_execution_scenarios.py`). **OR-6** SEMI certification: 4 estados (`PAPER_READY` / `PAPER_DEGRADED` / `LIVE_EXPERIMENTAL` / `LIVE_BLOCKED`; un FAIL crítico no se promedia) + CTA `Ejecutar en PAPER|LIVE` + UI preferencia cuenta. Spine **483**. Pack v113 [`audit-pack-estado-global-2026-08-26-v113.md`](./engineering/audit-pack-estado-global-2026-08-26-v113.md) · pack v112 histórico [`audit-pack-estado-global-2026-08-26-v112.md`](./engineering/audit-pack-estado-global-2026-08-26-v112.md). [`035-operational-reliability.md`](./adr/035-operational-reliability.md) · triage v111 [`audit-ext-v111-operational-reliability-triage-2026-08-26.md`](./engineering/audit-ext-v111-operational-reliability-triage-2026-08-26.md) · triage post-v1.12 [`audit-ext-v112-durable-execution-triage-2026-08-26.md`](./engineering/audit-ext-v112-durable-execution-triage-2026-08-26.md) · roadmap V1.13 [`roadmap-v113-durable-execution-2026-08-26.md`](./engineering/roadmap-v113-durable-execution-2026-08-26.md). **Next** candidatas post-`v1.13-beta` (`c8d5800`): UI Mesa incidente · OperationalPolicy · mass sim · thaw (solo con palabra explícita) · o operar SEMI.
- **ADR-032 Operational Core v1.9:** **F1–F4 + ExitPermission CERRADOS (modelo)** · broker adapter **mock + XL-1/XL-2 XTB** (money path filled→ledger; submitted ≠ fill). Thin 5.x/8.x = stand-in. **No** mappers thin nuevos. **No** ActionabilityScore. **No** `contract:gen`. [`032-operational-core-tradeplan-positionstate-execution.md`](./adr/032-operational-core-tradeplan-positionstate-execution.md).
- **F1 TradePlan v1:** `thesisId` · `entryCondition` · `target1`/`target2` · `initialRiskR` · `riskAmount` · `positionValue` · `expectedRR` · `portfolioFit` snapshot · `executionConstraints`. Status machine v0 intacta.
- **F2+F2.1 PositionState:** `positionId` · `tradePlanId` · status `OPEN`/`PARTIAL`/`PROTECTED`/`CLOSED` · planned/actual entry · initial/current stop · T1/T2 · qty/remaining · mark→`unrealizedR` · reduce→`realizedR` · MFE/MAE proxy en mark · stubs thesis/protect/trail. **P1 wire:** Confirm apertura + pending con snapshot → tabla `position_states`. **P3:** Confirm cierre → `applyReduce`. Thin **no** promocionados.
- **F3 ExitPlan:** `exitPlanId` · `positionId` · `tradePlanId` · status `IDLE`/`HINT`/`ARMED`/`TRIGGERED`/`DONE` · razones canónicas gap §3 · `suggestedAction` advisory. **P3 wire:** Confirm `exit_hint`/`reduce` + persist reduce. **No** auto-exit CTA · thin exitRadar **≠** ExitPlan · Lab `EvaluatePositionExits` intacto.
- **F4 ExecutionPlan PAPER:** `executionPlanId` · `exitPlanId` · `venue: PAPER` · status `DRAFT`/`PAPER_READY`→`JOURNALED`→`REPLAYED`→`VALIDATED` · broker → `BLOCKED`. **≠** ExecuteTrade · **≠** `PAPER_D_EXECUTE` · **≠** OCO.
- **ExitPermission:** `checkExitPermission` / `check_exit_permission` ALLOW/DENY · reasons canónicas · **H2** kill switch asimétrico (AUTO DENY; SEMI desriesgo ALLOW) · **≠** `check_opening` · **≠** auto-exit · **P3** gate Confirm cierres (`exit_permission`). Plan [`plan-exit-permission-2026-08-25.md`](./engineering/plan-exit-permission-2026-08-25.md).
- **V1.21 Operational Coherence (ADR-041, tag `v1.21-beta`):** Estudio = universo Daily Ops; `OperationalPlanView` (incl. trailing advisory vía `mapTrailPlan`); stop vigente = `currentStop`; T1 idempotente; AdminRail ≠ ⚙; higiene cuentas + cerrar extras OP-08. Pack [`audit-pack-estado-global-2026-08-27-v121.md`](./engineering/audit-pack-estado-global-2026-08-27-v121.md).
- **V1.22 freeze (ADR-040 §8 · ADR-041 §1.1 enmienda):** Mercado opera / Hoy resume. H1 `GetDailyOpsReport` ∩ Estudio. H2 `targetTouched` ≠ `targetManaged`. Cobertura Estudio **consensuada / absorbida** (jobs Actuar·Priorizar·Cobertura; Journal = propose; batch propose = epic posterior; sin `?view=` nuevo) — [`traspaso-relevo-hoy-cobertura-estudio-propuesta-2026-08-27.md`](./engineering/traspaso-relevo-hoy-cobertura-estudio-propuesta-2026-08-27.md) · freeze [`traspaso-relevo-v1-22-mercado-cockpit-freeze-2026-08-27.md`](./engineering/traspaso-relevo-v1-22-mercado-cockpit-freeze-2026-08-27.md). **UI Mercado 2.0 (panel DECISIÓN) + Hoy 2.0 (cuatro cubos) + SEMI F7 + PAPER AUTO F8:** [ADR-042](./adr/042-operating-excellence.md) / [spec V1.42](./engineering/spec-v142-operating-excellence-2026-08-31.md) — **F5–F8 CÓDIGO** ([relevo F5](./engineering/traspaso-relevo-v1-42-f5-mercado-decision-2026-08-31.md) · [relevo F6](./engineering/traspaso-relevo-v1-42-f6-hoy-cubos-2026-08-31.md) · [relevo F7](./engineering/traspaso-relevo-v1-42-f7-semi-2026-08-31.md) · [relevo F8](./engineering/traspaso-relevo-v1-42-f8-paper-auto-2026-08-31.md)).
- Ranking Estudio (`rankIndiceOperativo`) sigue en cliente. **Fórmula IO** es server (`indiceOperativo` en chip Composite; FE `resolveIndiceOperativo`). ≠ Actionability. ≠ permiso. **Ciclo I2 (`e31840d`).**
- **Ciclo I1 (`2bd5cd8`):** `POST /portfolio/trade` buy pasa `check_opening` (mismo helper que Confirm/Fill: Fit + DS-05 + DS-03). Sell no abre cesta. Spine 3 intactos. Router AUTO no fusionado. `PAPER_D_EXECUTE` off.
- **Sesión operativa 2026-08-27 (tag `v1.17-beta`):** HTTP buy cablea `instrument_data_status` (split/dividendo veta; gap-only no). Mesa 3 niveles (ADR-037 §7). Suitability sectorial en Priority. Scenario ACTUAL/DESPUÉS = estimación, no permiso. Libro `/operations` muestra ruta viva + Next Action del agregado.
- **V1.17.1 hardening (sin tag) + refinamiento Mesa:** candidateRiskR / notional desde TradePlan; Aggregate originalPlan vs currentPlan; Unknown sector warning; paper_auto sanity+EdgeReport; Redis SHA256; Mesa isError. **Refino post-hardening:** overlay TradePlan vivo del board sobre study (sizing scenario); sector coalesce PositionDto.sector; Priority penaliza sector null/Unknown; what-if INSUFFICIENT_DATA visible; chip Datos sin posiciones; badge Acción primaria; cartera vacía = 0R abierto (no null). **MR-1 honesty residual:** what-if declara no evalúa check_opening/DS-05/Fit; chip Datos muestra parcial (1/N) si N>1; una Acción CTA; título candidatos sin «operables». **V1.18 L1+L2a lineage:** origen por decisionId; snapshot Package write-once al fill (originThesis); soft-join instrumento = evolución. **Stress Risk MVP:** portfolioStressRiskR = cota concurrente stops (concurrent_stops_v0; cobertura completa o null; ≠ VaR/correlación; no gatea Confirm/AUTO/Priority/scenario). **OpportunityEvidence V1 (contrato):** Quality pura read-only + best-next-R informativo en @bolsa/shared ([opportunity-evidence.ts](../packages/shared/src/cognitive/opportunity-evidence.ts)); ≠ BUY ≠ Permission. **V1.19 Opportunity Discovery:** Quality 0–100 (strength+R/R) cableada a Priority; `OpportunityFunnel`/`OpportunityRanking` (fuente scan+studies, ≠ Action Queue); Mesa L3 «Mejores oportunidades» TOP 5 + embudo + CTA Señales; scan diario opt-in `OPPORTUNITY_DAILY_SCAN_ENABLED` (default off, propose cap 15, cero execute); `/operations`→`/mesa?focus=libro` · `/decision-board`→`/mesa?focus=spine`. Plan [plan-v119-opportunity-discovery-2026-08-27.md](./engineering/plan-v119-opportunity-discovery-2026-08-27.md). **V1.20 UX IA (ADR-040):** nav usuario Hoy·Mercado·Cartera·Asesor·Laboratorio; OpportunityScore **aparcado**. **Sigue deuda:** correlación/VaR · OpportunityScore (backlog) · backtest ≠ TradingPolicy · V1.18 B-read/backfill. Plan [plan-v1171-hardening-2026-08-27.md](./engineering/plan-v1171-hardening-2026-08-27.md) · [plan-mr1-mesa-honesty-residual-2026-08-27.md](./engineering/plan-mr1-mesa-honesty-residual-2026-08-27.md) · [plan-v118-position-lineage-l1-2026-08-27.md](./engineering/plan-v118-position-lineage-l1-2026-08-27.md) · [plan-v118-position-lineage-l2a-2026-08-27.md](./engineering/plan-v118-position-lineage-l2a-2026-08-27.md) · [plan-stress-risk-mvp-2026-08-27.md](./engineering/plan-stress-risk-mvp-2026-08-27.md) · [plan-opportunity-evidence-v1-2026-08-27.md](./engineering/plan-opportunity-evidence-v1-2026-08-27.md).
- **Ciclo I3 (`26901aa`):** HTTP `paper_auto` (`/execution-policies/{id}/route`, `/scans/jobs/{id}/execute`) exige `PAPER_D_EXECUTE` (403 `paper_auto_env_blocked` si off). `inform`/`alert`/`live_auto` no. Router no fusionado. **No thaw.**
- **Ciclo RX1 (`9289b53`):** `evaluate-exits?executeTrades=true` + position `full_auto` + linked `paper_auto` → mismo env gate antes del Router (403). Eval-only OK. **No** auto-exit producto · **no** thaw · I1/I2/I3 intactos.
- Dictamen (`DailyOpinionService`) no entra solo al Runtime; puede acabar en SEMI por alarma.
- Aperturas orphan sin package: con store cableado (producción) → **`orphan_opening_blocked`** (H3, ADR-031). Wiring de test sin store = legado.
- Confirm SEMI: TTL `expiresAt` y revalidación de último close vs `suggestedPrice` (banda 2 %).
- `pending_orders` fill: `POST /api/pending-orders/{id}/fill` (ya no `executeTrade` directo desde el monitor).
- Confirm SEMI: perfil activo vía `active_profile_id` → `check_opening` (H5 CERRADA; mismo SoT AUTO). Sin perfil → defaults moderate.
- Composite `portfolioConstraints` sigue `not_evaluated` (peso 0); Fit real en `check_opening`. Nota/UI: no es «pendiente de puntuar en Composite».
- **DS-03 Account Mandate Gate CERRADA (`41adb8e`):** tenure abierto server-side (`mandate_tenures` vía sync cliente) → `check_opening` VETO fail-closed sin mandato / mismatch estrategia AUTO. Adopción UI (`strategy-adoption`) sigue proyección cliente; el gate usa BD. Exits fuera. Batería `pnpm test:decision-spine` **75**.
- TradePlan Ciclo 4.0: stop ATR×1.5 + swing (más lejano), `entry_ready` por bias TA, size con equity de cartera. Confirm rebuild sin barras → `WATCH`/`no_stop`. **P2:** con TradePlan TRIGGERED, el ticket **usa** `quantity`/riesgo del plan (no % caja). Sin plan, % caja puede prellenar. Decision Board HTTP **sí** expone `tradePlan` (+ anchor) en sesiones desde Ciclo 4.9 (sin `contract:gen`).
- TradePlan Ciclo 4.1: `NO_NEW_LONGS` — long + `risk_off`/`crisis` → `BLOCKED`/`regime`. Shorts OK. Confirm sin régimen no veta. `check_opening` intacto.
- TradePlan Ciclo 4.2: `entrySetup` breakout/pullback/wyckoff/none; `entry_ready` = bias TA **y** setup≠none. Sin `contract:gen`.
- TradePlan Ciclo 4.3: `ARMED` = stop válido + setup≠none + `!entry_ready` (qty 0, `executionAllowed=false`, `whyNot: entry`, actionability 0.7). Golden A sigue `TRIGGERED`. Confirm sin barras no inventa `ARMED`. Sin `contract:gen`.
- TradePlan Ciclo 4.4: Wyckoff formal — spring + reclaim estricto (`WYCKOFF_RECLAIM_ATR_K=0.25` **o** close fuera del rango spring). SOS etiqueta interna. Sin `wyckoffPhase`. Sin `contract:gen`.
- TradePlan Ciclo 4.5: LPS etiqueta + SM single-window (`spring→reclaim→sos?→lps?`); `wyckoff` sigue = spring+reclaim. Sin multi-sesión. Sin `wyckoffPhase`. Sin `contract:gen`.
- TradePlan Ciclo 4.6: `_locate_wyckoff_spring` lookback 40; reclaim/SOS/LPS sobre spring vivo; hielo roto (cerradas) → none. LPS etiqueta. Sin store. Sin `wyckoffPhase`. Sin `contract:gen`.
- TradePlan Ciclo 4.7: `_resolve_wyckoff_spring` + `wyckoffSpringAnchor` en `DecisionSession.runtime`; bound por `decision_id` si hielo intacto; hielo roto → none. Sin Alembic. Sin `wyckoffPhase` en TradePlan. Sin `contract:gen`.
- TradePlan Ciclo 4.8 (**cierre línea SETUP Wyckoff**): `_wyckoff_effort_evidence` en anchor (`effort`); echo `wyckoffSpringAnchor` en propose/F3; Hoy dialog Setup (`entrySetup` + phase + effort); Board `semiF3.extra` anidado. Effort/LPS **etiqueta**. Sin Alembic. Sin `wyckoffPhase` TradePlan. Sin `contract:gen`.
- **Ciclo 4.9:** Board sesiones echo `tradePlan` + `wyckoffSpringAnchor` desde `runtime` (`DecisionSessionViewDto` a mano); Hoy usa plan vivo. **Ciclo C1:** sin plan → WATCH (nunca BUY). WhyNot labels `regime`/`orphan`/`rr`/`legacy_projection`. Sin Alembic. Sin `contract:gen`. Sin Actionability/IO server.
- **Ciclo 5.0 Thesis Health thin:** mapper Golden F (`mapThesisHealth` / `map_thesis_health`) → `runtime.thesisHealth` en propose; Board/Hoy echo; dialog «Revisar tesis» si `status=review`. **No** `TradePlan.status=REVIEW`. **No** trail/T1/MFE. `check_opening` intacto. Cola Hoy `REVIEW` (EXPIRED) ≠ thesis review.
- **Ciclo 5.1 Protect/T1 thin:** mapper Golden E (`mapProtectPlan` / `map_protect_plan`) → `runtime.protectPlan`; Board/Hoy «Proteger» si `status=protect_hint` (MFE≥1R). T1 = entry±1R; `suggestedProtectStop=entry`. **No** muta `structuralStop`. **No** trail/exit. `check_opening` intacto.
- **Ciclo 5.2 Exit Radar thin:** mapper (`mapExitRadar` / `map_exit_radar`) → `runtime.exitRadar`; prioridad exit > time_stop > trail; Hoy «Salida» si status≠none. Trail tip @ MFE≥1.5R; time-stop por `expiresAt`; exit por thesis/T1 explícito. **No** auto-exit · **no** EvaluatePositionExits · **no** mutar stop. `check_opening` intacto.
- **Ciclo 5.3 MFE/MAE thin:** mapper (`mapMfeMae` / `map_mfe_mae`) → `runtime.mfeMae`; peak MFE/MAE desde barras (fallback close_proxy); Hoy «Excursión» métricas (no CTA). **Ciclo C5:** `source` bars|close_proxy|none — proxy ≠ peak de barras; **no** mezclar proxy y bars en agregados futuros. **No** expectancy plena · **no** journal MFE · **no** mutar stop. `check_opening` intacto.
- **Ciclo 8.0 Expectancy thin:** mapper (`mapExpectancy` / `map_expectancy`) → `runtime.expectancy`; live proxy setup+`currentR`; Hoy «Expectativa» (≠ permiso). **Ciclo C5:** `sampleQuality` insufficient/preliminary/developing/useful; `status: ready` (READY_MIN_N=5) ≠ estadísticamente útil. **No** trail · **no** bracket · **no** journal histórica. `check_opening` intacto.
- **Ciclo 8.1 Trail thin:** mapper (`mapTrailPlan` / `map_trail_plan`) → `runtime.trailPlan`; ratchet peakMfeR−1R; status tip@1.5R / ratchet≥2R; Hoy «Trail» (hint only). Alinea Exit Radar tip; **no** muta `structuralStop` · **no** broker · **no** EvaluatePositionExits. `check_opening` intacto.
- **Ciclo 8.2 Bracket thin:** mapper (`mapBracketPlan` / `map_bracket_plan`) → `runtime.bracketPlan`; picture entry/stop/T1(1R)/T2(2R) + leg fracs display-only; Hoy «Bracket». Alinea Protect T1; **no** OCO · **no** broker · **sin** CTA. `check_opening` intacto. **Línea crecimiento thin 8.0–8.2 CERRADA.** **Congelada** hasta TradePlan v1 / PositionState / ExecutionPlan (v1.8.1 consolidación; no un mapper thin más).
- **Ciclo C1 Hoy honesty + HELP (`v1.8.1` P0):** F3/sesión sin TradePlan vivo → `WATCH` (nunca BUY/ARMED heurístico). BLOCKED/WATCH sin plan → `whyNot: legacy_projection`. HELP_CONTENT_AS_OF **2026-08-25** (AUTO BETA-D, spine, Hoy). Actionability v0 = ordinal de status, no score predictivo. Planes `plan-ciclo-*` = contexto histórico; autoridad = este fichero → ADR → código → tests.
- **Ciclo C2 Alembic única autoridad:** públicos `pnpm db:push` / `db:migrate` / `db:migrate:deploy` fail-closed. Bootstrap (`setup` / `db-ensure` / `db-check`) = `ensure_migrated`. Prisma = seed + `db:generate`. ADR-025 enmendado. Sin dual-schema.
- **Ciclo C3 ActionQueue:** `buildActionQueue` lista completa ordenada (D2 + actionability + dedup símbolo post-sort); Hoy = slice top-8. Sin HTTP. C1 intacto (nunca BUY sin plan).
- **Ciclo C4 TradePlan shape canónico:** Hoy `readCanonicalTradePlan` — sesiones `session.tradePlan`; F3 `extra.payload.tradePlan`. Fallbacks (`extra.tradePlan`, payload top-level) marcados `legacy`, no borrados. `HoyQueueItem.planSource`: `live` | `projection`. **No** Pydantic DTO · **no** OpenAPI · **no** `contract:gen` (v1.9 ADR-032). C1/C3/C5 intactos. Confirm/propose/spine/`check_opening` intactos. Sin Alembic.
- **Ciclo C5 MFE/Expectancy honesty:** `MfeMae.source` bars|close_proxy|none (Hoy sufijo `proxy` si close_proxy). Expectancy `sampleQuality` n<20 insufficient · 20–49 preliminary · 50–99 developing · 100+ useful. `ready` ≠ útil. UI: «muestra insuficiente (n=…)» antes de E±R. Advisory ≠ permiso. **No** mezclar proxy y bars en agregados futuros. **No** journal histórica. C1/C3 intactos.
- **Ciclo C6 stamp v1.8.1:** pack [`audit-pack-estado-global-2026-08-25-v181.md`](./engineering/audit-pack-estado-global-2026-08-25-v181.md). Spine **161** (pre-F1) · shared **84** · Prisma fail-closed. Tag **`v1.8.1-beta`**. Post-F1 **167** · post-F2 **172** · post-F2.1 **180** · post-F3 **192** · post-F4 **203** · post-ExitPermission **217** · post-H2 **226** · post-P1 **233** · post-P2 **245** · post-P3 **257** · post-P4 **260**. **INFRA CI-by-tag CERRADO** ([`release-tag-ci.yml`](../.github/workflows/release-tag-ci.yml)).
- OrderProposal / Journal **F1–F3 CERRADOS** (Alembic `010`). **Journal 2.0 Tesis (ADR-036) CERRADO:** `/decision-journal` = pestaña **Tesis** (`GET /decision-studies`, ViewModel sobre `decision_sessions` propose) + **Historial técnico** (timeline ADR-029). WATCH/sin stop → sin SL/TP. `userThesis` null. No Alembic nuevo. No duplica TradePlan. **Attribution thin Ciclo 6** intacta. **Expectancy plena** (journal aggregate) sigue parked.
- Diferido ADR-031: Alembic/tabla Wyckoff / `wyckoffPhase` contrato (parked), trailing **plena** broker / mutar stop (tras 8.1 thin), bracket **plena** OCO / piernas (tras 8.2 thin), thesis health **plena** (persistencia Confidence lifecycle), expectancy **plena**, thaw **estricto** 60d/50/70/55 (deuda abierta — runbook [`deuda-thaw-estricto-runbook-2026-08-25.md`](./engineering/deuda-thaw-estricto-runbook-2026-08-25.md); BETA-D Accepted — [`thaw-beta-adapted-remeasure-2026-08-25.md`](./engineering/thaw-beta-adapted-remeasure-2026-08-25.md)), broker live. **Línea integridad I1–I3 CERRADA** + residual **RX1**. **Ciclo 8.0–8.2 CERRADOS.** **v1.8.1 C1–C6** (tag parked). **No** auto-exit producto. **No** reabrir Wyckoff thin ni añadir módulos thin.

## Tests

| Comando                        | Qué cubre                                                            |
| ------------------------------ | -------------------------------------------------------------------- |
| `pnpm test:decision-spine`     | Cadena decisión (spine **540+**)                                     |
| `pnpm test:operative-journeys` | **V1.35** J01–J06 recorridos operativos (BUY→T1→T2→STOP→trail→recon) |
| `pnpm test:semi`               | UI/libro DEMO F3 (no es el spine)                                    |
| `pnpm test:operativa`          | DÍA D + CORE-R                                                       |
| `pnpm test:py`                 | Pytest amplio                                                        |

## Open risks (ops, no código)

`TRUSTED_PROXIES` prod — **BLOCKED_ON_OWNER** (IPs exactas del peer; matcher actual ≠ CIDR). In-repo DONE: código + default vacío + secret scanning. Runbook E1.3: [`ops-trusted-proxies-prod-runbook-2026-08-24.md`](./engineering/ops-trusted-proxies-prod-runbook-2026-08-24.md). Purga opcional historial git dev (decisión explícita). **Thaw Camino D BETA-D:** ADR-023 **Accepted** · perfil [`thaw-beta-adapted-remeasure-2026-08-25.md`](./engineering/thaw-beta-adapted-remeasure-2026-08-25.md) (P1'–P5' PASS + W2–W4) · UI AUTO on · `PAPER_D_EXECUTE` opt-in local (default off). **Deuda estricto** (60d/50/70/55 · MaxDD): [`deuda-thaw-estricto-runbook-2026-08-25.md`](./engineering/deuda-thaw-estricto-runbook-2026-08-25.md) · baseline FAIL [`thaw-p1-p5-measurement-2026-08-25.md`](./engineering/thaw-p1-p5-measurement-2026-08-25.md) (28d · 0 SEMI · prec null · recall 0% · MaxDD inválido).
